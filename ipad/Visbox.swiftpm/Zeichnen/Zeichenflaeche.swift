import SwiftUI

/// Die Zeichenfläche (später PencilKit, Apple Pencil 2).
///
/// **Platzhalter.** Die Einheit «Zeichnen» ersetzt den Inhalt dieser Datei; der Name
/// `Zeichenflaeche` bleibt, weil `Startansicht` ihn benutzt.
struct Zeichenflaeche: View {
    var body: some View {
        Platzhalter(titel: "Zeichnen",
                    satz: "Hier entsteht die Skizze. Noch nicht gebaut.")
    }
}
