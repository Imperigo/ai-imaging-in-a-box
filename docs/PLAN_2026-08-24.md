# Plan für den 24.08.2026 · Home-PC-Lane

**Geordnet nach Risiko.** Am Ende des 23.08. geschrieben, damit der Morgen nicht mit
Sortieren beginnt.

---

## Wo wir am Abend des 23.08. stehen

**Die Kette läuft von der Oberfläche bis zum Bild.** Zwei Bilder aus zwei **benannten**
Kamerastandpunkten (`Eingang`, `Übersicht`), 83 Sekunden, der Prompt kommt an. Der
Render-Knoten reagiert seit `P-DONE` auf `status: "done"` — gemessen: nach 20 Sekunden,
statt nach 8½ Minuten gar nicht.

**Sie hängt an einem Feld.** Die Oberfläche verwirft das Ergebnis, weil
`qa.geometry.geometry_fidelity` und `spearman` **null** sind und ihr Schema kein null
annimmt. Die zwei fertigen Bilder liegen auf der Platte, ohne dass ein Nutzer sie sieht.
*(auf-orbit-20260823-04, KosmoOrbit-Lane.)*

**Drei Owner-Entscheide sind gefallen** und stehen in `auftraege/von-homestation/
ENTSCHEIDE-2026-08-23.md`: Schwelle 1 m/2 m, Hüllbox auf die gebaute Substanz, Variante F
erst prüfen.

**Der Stand der drei Zeilen des sichtbaren Urteils:**

| Frage | Mass | Schwelle | Stand |
|---|---|---|---|
| Stimmt die Tiefenstaffelung? | `rho_maske` | **0.80** | geeicht, 3 Szenen, Abstand 0.4 % |
| Steht überhaupt ein Bauwerk da? | **R2 (Rang)** | **45 %** | 5 Szenen, aber **nur Blender-Renders** |
| Wie gut ist der Umriss gezeichnet? | — | — | **kein Mass, kein Einfall** |

---

## 1 · Zuerst: R2 an ERZEUGTEN Bildern messen

**Das ist die Lücke, die zählt.** Alle bisherigen R2-Werte stammen von Blender-Renders und
den drei Nullankern. Die Versatzreihe ist mein Ersatz für *«schlechter werdende
Geometrie»* — aber ein erzeugtes Bild ist etwas anderes: erfundener Vordergrund, weiche
Ränder, erfundener Kontext. **R2 ist noch nie an einem gemessen worden.**

Zu messen: die drei Bilder aus auf-28 (A/B/C) und die zwei von heute (`Eingang`,
`Übersicht`), gegen ihre eigenen Masken. Erwartung offen — und genau darum wird gemessen,
bevor irgendjemand R2 einbaut.

**Wenn R2 an erzeugten Bildern fällt, ziehe ich es zurück wie die zwei davor.**

*Braucht: nur diese Maschine. Etwa eine Stunde.*

## 2 · Dann: die sechste Szene

Bewuchs oder ein Nachbar aus **anderem** Material hinter dem Umriss. Mein Nachbar in g2
war derselbe graue Beton — mit Ziegel oder Glas hat der Schätzer mehr zu sehen, und es
ist offen, ob das R2 hilft oder schadet.

## 3 · Laufend: auf die zwei Cloud-Lanes reagieren

| Liegt bei | Was | Was ich dann tue |
|---|---|---|
| KosmoOrbit | `null` im QA-Schema zulassen | zweiter Demolauf — dann kommt das Bild erstmals bei einem Nutzer an |
| KosmoOrbit | Feld für `rho_maske` | Schwelle 0.80 gegenprüfen |
| KosmoVis | Hüllbox auf gebaute Substanz | **Kamera-Streuung messen** — die Zahl, die `MINIMUM_ABSCHLAG` braucht |
| KosmoVis | `gelaende_erwartet` durchreichen | prüfen, ob der Produktpfad endlich eine Zahl liefert |

## 4 · Wenn Zeit bleibt

* **Der Prompt ist deutsch.** Gemessen (8 von 8 Paaren), gemeldet, und der KosmoVis-Worker
  hat gestern an `sprache.py` gearbeitet — nachmessen, ob die Übersetzungsstufe den
  Sprachbefund wirklich behebt oder auf halbem Weg stehen bleibt.
* **Demolauf 5** am echten Demohaus — wartet weiter auf das Demohaus.

---

## Drei Regeln, die sich heute bewährt haben

1. **Erst eichen, dann empfehlen.** Zwei eigene Vorschläge sind heute gefallen
   (`kantenanteil`, Variante F), beide **bevor** jemand danach gebaut hat. Das ist kein
   Fehlschlag des Verfahrens, das ist das Verfahren.
2. **Vor jedem Produktbefund die eigene Messung prüfen.** Ein `up_axis="Z"` auf einer
   Y-up-Geometrie wäre heute beinahe ein Befund geworden. Aufgefallen beim Vergleich mit
   einem alten Lauf.
3. **Wenn eine Frage nicht beantwortbar ist, ist meist die Frage falsch.** «Löst ein
   grösserer Schätzer die 15 m auf?» war lizenzrechtlich unmöglich und sachlich am Kern
   vorbei. Die richtige Frage — *Rang statt Betrag* — hat ein Mass ergeben.
