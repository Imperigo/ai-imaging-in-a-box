# Ab Dienstag, 1. September 2026: der Rückstand zuerst

> **Beschlossen am Freitag, 28.08.2026**, nach zwei Fragen an den Owner. Er liegt hier im
> Repo und nicht in einer Sitzungsdatei: Was nicht in einer Datei steht, ist weg — und
> eine Sitzungsdatei ist am Dienstag keine.

## Context

Vierzehn Sitzungen in vierzehn Tagen, 478 Commits, 4620 Tests. Der Bau läuft. **Was
nicht läuft, ist das Zurückkommen.**

Owner-Entscheid vom 28.08.2026, auf zwei Fragen: Das **Tempo bleibt**, und das Ziel ist
**beides — aber der Rückstand zuerst.** Zwei Wochen nichts Neues bauen, den Rückstand
abarbeiten, die Dokumente auf lesbare Grösse bringen, dann mit sauberem Tisch weiter.

Dieser Plan sagt, was das konkret heisst, und er ruht auf vier gemessenen Befunden.

---

## Die Diagnose, gezählt

| | |
|---|---|
| Aufträge gestellt / beantwortet | **68 / 37** |
| unbeantwortet | **34**, ältester **10 Tage** |
| Einbau-Posten offen | **22 von 33** |
| `docs/PLAN.md` | **5315 Zeilen** |
| `docs/LEXIKON.md` | 3840 Zeilen |
| Sitzungsprotokolle | 9311 Zeilen in 14 Dateien |

**Und die Schere geht auf, nicht zu.** Gestellt gegen beantwortet, je Tag:

    26.08.   14  /  2
    27.08.    8  /  0
    28.08.    4  /  2

*Mehr Tempo verschlimmert das. Der Engpass ist nicht das Bauen.*

---

## Vier Befunde, die den Rückstand erklären

**1 · Sieben der 34 stammen vom allerersten Abend.**
`auf-20260818-01` bis `-07` sind eine Kette von Anläufen an einem Kompositorfehler, den
es seit dem 19.08. nicht mehr gibt — *«Zweiter Anlauf», «Dritter Anlauf», «Fünfter
Anlauf»*. Sie warten seit zehn Tagen auf eine Antwort, die niemand mehr geben kann,
**weil die Frage weg ist.** Seit heute gibt es dafür einen Zustand: `zurueckgezogen`.

**2 · Neun `local`-Aufträge mit `art: qa` sind gar keine Läufe, sondern Fragen.**
*«Welche Zahl war 0.6909?»*, *«Wie oft schlägt der Torwächter an?»*, *«Warum reicht
`/api/mcp/tools` keine Schemata durch?»* — das rechnet kein Runner aus. Seit heute lehnt
`art: qa` ab, damit nichts mehr grün-leer zurückkommt. **Damit sind diese neun
strukturell unbeantwortbar** — und sie waren es immer, es fiel nur nicht auf.

> Die Vokabel kennt drei Sorten Lauf und keine Sorte Frage. `qa` war die Ablage dafür.

**3 · Zehn Aufträge liegen bei Adressaten, die unser Repo nicht lesen** — 6 `cloud`,
4 `ui`. Sie sind seit gestern über das KosmoOrbit-Repo zugestellt, aber sie zählen
weiterhin als Rückstand, und zu Recht: zugestellt ist nicht beantwortet.

**4 · Die Dokumente wachsen monoton, und die Hausregel garantiert das.**
*«Erledigtes wird abgehakt, nicht gelöscht»* ist richtig und hat `PLAN.md` auf 5315
Zeilen gebracht. Ein Plan, den niemand mehr liest, plant nichts.

---

## Woche 1 (Di 1.9. – Fr 5.9.): den Rückstand ehrlich machen

~~**Ziel: von 34 auf unter 15 — und zwar ohne eine einzige erfundene Antwort.**~~

> **Gestrichen am 02.09.2026 (Owner-Entscheid).** In fünf Tagen ist keine einzige
> Antwort gekommen; das Ziel hätte achtzehn in drei Tagen verlangt. *Eine Zahl, die an
> fremden Antworten hängt, ist keine Planung, sondern eine Hoffnung mit Datum.* An
> ihre Stelle tritt keine neue Zahl, sondern eine Zusage: täglich messen, jede
> eintreffende Antwort am selben Tag verarbeiten.

### 1 · Die sieben vom ersten Abend durchgehen, einzeln

Je Auftrag eine von zwei Antworten, keine dritte:
* **gegenstandslos** → Ergebnis mit `art: "zurueckgezogen"`, mit Begründung, warum die
  Frage weg ist. Der Zustand dafür existiert seit dem 28.08.
* **noch gültig** → dann bleibt er offen und bekommt gesagt, worauf er wartet.

*Nicht gelöscht, nicht stillschweigend geschlossen. Ein zurückgezogener Auftrag ist ein
Befund über uns, kein Aufräumen.*

### 2 · `art: "frage"` einführen

Neuer Wert in `auftrag.ARTEN` für Aufträge, die **ein Mensch beantwortet und kein
Runner ausführt**. Der Homeworker lässt sie liegen wie fremde Aufträge — gezählt,
genannt, nicht angefasst, **kein Ergebnis geschrieben**.

Danach die neun `local`-`qa`-Aufträge einzeln durchsehen: `frage` oder `render`. Beides
ist eine Entscheidung je Auftrag, keine Umbenennung im Block.

**Dateien:** `src/aiimaging/auftrag.py` (`ARTEN`), `tools/homeworker.py` (neben
`EIGENER_WORKER`), `tests/test_auftrag.py`, `tests/test_homeworker.py`.

### 3 · Ein Deckel auf den Rückstand, und er ist zählbar

`tools/einbau.py` zählt bereits. Neu: **`aiimaging.einbau` kennt eine Obergrenze je
Adressat**, und `tools/auftrag.py` weigert sich, einen Auftrag zu schreiben, solange sie
überschritten ist — mit der Liste dessen, was zuerst zu schliessen wäre.

Vorschlag: **acht je Adressat.** Heute wären das `local` 24 → gesperrt, `cloud` 6 → frei,
`ui` 4 → frei.

*Was von Hand eingehalten wird, wird irgendwann nicht mehr eingehalten — dieselbe
Begründung wie beim Zählen des Rückstands.*

### 4 · Den einen Auftrag priorisieren, den die Arbeit braucht

`auf-20260827-61` — die Kalibrierung der Paarschwellen. **Er ist der einzige offene
Punkt, an dem der wissenschaftliche Kern hängt**, und alles dafür liegt bereit:
Rechenwerkzeug, Obergrenzenstudie, Richtungsbefund. Er bekommt in der Ansage an die
HomeStation den ersten Platz.

---

## Stand am Ende von Dienstag, 01.09.2026

**Die Zahl, an der der Tag gemessen wird:** `RUECKSTAND: 34` am 28.08. → **27** heute
früh nach den Rückzügen → **28** am Abend, weil ein neuer Auftrag dazukam. Das Tagesziel
lautete 26 und ist **nicht erreicht**.

**Erledigt:**

* ✅ **1 · Die sieben vom ersten Abend** — alle sieben zurückgezogen, je mit eigener
  Begründung (`art: "zurueckgezogen"`). `auf-01` bis `-05` waren fünf Anläufe an *einem*
  Bruch, der seit dem 19.08. behoben ist; `auf-06` prüfte genau diesen Bruch; `auf-07`
  wollte den *«ersten echten Render»*, und `auf-20260818-09` trägt seit dem 18.08. ein
  Renderergebnis mit `score 0.3592`. **33 → 27**, ältester 14 → 10 Tage.
* ✅ **2 · `art: "frage"`** — eingeführt, neun `local`-`qa`-Aufträge einzeln durchgesehen
  und umgetypt; **nicht einer** von ihnen war ein Lauf. Ein `--alle`-Lauf hätte vorher
  27 Aufträge angefasst, jetzt 8.
  *Und die ehrliche Zahl daneben:* **Umtypisieren beantwortet nichts.** Der Rückstand
  sank dadurch um **null** — es sind dieselben Fragen, sie stehen nur nicht mehr in der
  Schlange des Renderers.
* ✅ **3 · Der Deckel** — `DECKEL_JE_WORKER = 8`, `DeckelError` nennt die drei ältesten.
  Stand: `local` 17 → **gesperrt**, `cloud` 7, `ui` 4, `kern` 0.
* ✅ **4 · `auf-61` priorisiert** — und dazu die **Ersatzkalibrierung** gerechnet, die
  der Owner am Morgen entschieden hat: `docs/PAARSCHWELLEN_OBERGRENZE_2026-09-01.md`.
  **`auf-61` bleibt offen**; alle acht Kurven melden `genuegt_als_kalibrierung: false`.

**Was heute dazukam und nicht im Plan stand:**

* **`auf-20260901-67` an `cloud`** — `RenderScene` hat kein Feld für *Gelände ja/nein*.
  Der Befund kam am Morgen von der HomeStation; ohne Feld bleibt die Bauwerksmaske leer
  und ein Lauf liefert Bilder ohne Urteil. **Der Plan sagte «kein neuer Auftrag», und
  hier steht die Begründung für die Abweichung:** Ein Posten ohne Adressaten wird nie
  eingebaut, und es fällt keinem auf. Der Deckel bei `cloud` liess ihn zu (6 → 7).
* **Ein eigener Schalter ohne Wirkung**: `tools/auftragspost.py --auftrag X --nach Y`
  kehrte vor der Ablage um und druckte. Gefunden beim ersten Gebrauch, behoben, Test und
  Mutationsprobe dazu. *Genau der Befund, den wir sonst an die Oberfläche weitergeben.*
* **Doppelte Arbeit am Morgen**: Die HomeStation hat diesen Plan gelesen und einen
  Agenten auf Punkt 1 angesetzt, während er hier lief. Beide kamen unabhängig auf
  dasselbe Urteil und auf `RUECKSTAND 27` — kein Widerspruch, nur doppelte Kosten. Die
  Lehre steht in ihrer Meldung (`auftraege/von-homestation/geraetebefunde-20260901.md`):
  *Ein Plan, der in einem fremden Repo liegt, ist die Arbeit desjenigen, dem das Repo
  gehört.*

**Was von der HomeStation zurückkam:** `auf-48` R3 ist beantwortet (zehn Werkzeuge am
Draht, per echtem Tool-Call geprüft) — R1, R2 und A6 bleiben offen, der Auftrag also
auch. `auf-59` ist **nicht blockiert, sondern nicht ausgeführt**: `kosmo-worker.timer`
gibt es nicht; das ist ein Betriebsposten dort und kein Codeposten hier.

**Nachmittag, auf Owner-Auftrag «weiter mit dem Rückstand»** — und der Tag hat dabei
gefunden, dass der Rückstand **zwei verschiedene Dinge** sind:

* **Gezählt, und die Zahl stand nirgends:** `cloud` (7 offen, ältester 10 Tage) und `ui`
  (4 offen, 7 Tage) haben **nie** geantwortet; die zwei Ergebnisse bei `cloud` sind
  Weiterleitungsvermerke der HomeStation. Elf der 28 liegen also bei Adressaten, von
  denen noch nie etwas kam. Bei `local` ist der Rückstand eine Warteschlange, drüben ist
  ungeklärt, ob jemand hinsieht — und das verlangt Gegenteiliges.
* ✅ **Zustellbeleg** (Owner-Entscheid): Alle elf Dateien drüben bitten um einen Satz mit
  Datum, ohne inhaltliche Antwort. Im Erzeuger, nicht in den elf Dateien; verschwindet
  nach der ersten Antwort von selbst. Gepusht nach `kosmo-orbit`.
* ✅ **Die Zahl ist im Werkzeug:** `tools/einbau.py` schreibt `NIE GEANTWORTET` neben den
  Adressaten. Gezählt gegen den abgeleiteten Zustand, nicht gegen Ergebnisdateien.
* ✅ **Durchsicht der siebzehn** (Owner-Entscheid): **kein Rückzug.** Sie warten auf ein
  Gerät, nicht auf eine Entscheidung. Geändert haben sich zwei: `auf-48` verliert `R3`
  (beantwortet) und `R5` (dieselbe Frage wie `auf-58` V1), `auf-51` bekommt den Befund
  von heute früh samt dem Hinweis, dass er die Frage nicht beantwortet.
* ✅ **Rangfolge:** Neues freiwilliges Feld `rang`; der Homeworker sortiert danach statt
  nach Dateinamen. Alle siebzehn tragen einen. Angesagt in `auf-59`, bevor es ankommt.
* ✅ **`auf-59` ist entsperrt** — mit einem Nachweis, den die HomeStation selbst führt
  (`homeworker.py --liste`: 9 Fragen unberührt, 8 Läufe).

**Abends, und es hat die Freigabe vom Mittag eingeholt:** `darf_starten` wäre an den
Prosa-Auflagen abgestürzt — sechs der acht laufbaren Aufträge, `auf-59` auf Rang 1
eingeschlossen, und der Aufruf stand ausserhalb der Absicherung. Mit `--hoechstens 1`
wäre das **jeder Takt** gewesen, immer derselbe Auftrag.

* ✅ Beide Formen werden an einer Stelle übersetzt; fehlt die Angabe, gelten die strengen
  Vorgaben. Der ganze Rumpf je Auftrag ist abgesichert — *ein Auftrag darf sich selbst
  beenden, nicht den Durchgang.*
* ✅ Die 400-W-Auflage ist Vertrag: `pruefe_auftrag` weist einen `local`-Auftrag ohne sie
  ab. Sie war mit der Prosaform aus 15 von 17 offenen Aufträgen verschwunden.
* ✅ Der Block zeigt die **Werte** statt der Schlüsselnamen; fünf Aufträge tragen echte
  Rückgabepunkte. Drei davon sind die ältesten bei `cloud` und `ui` — *bevor man
  schliesst, dass dort niemand hinsieht, sollte das Verschickte lesbar sein.*
* ✅ Ansage in `auf-59`, **nach** dem Fix (Owner-Entscheid), mit `git pull` vor dem
  Installieren.
* ✅ Nachlauf: Kein weiteres Auftragsfeld trägt zwei Formen. In den Ergebnissen drei,
  aber folgenlos — niemand liest sie.

**Spätabends, zwei Messungen vom Gerät:** Das Gebäude stand auf dem Kopf (+90° statt
−90°), und die Geländeregel findet auf echtem Bestand nichts.

* ✅ Das Vorzeichen zog von einem Literal im Blender-Skript nach `contracts.py` und hat
  zwei Wächter — den Rundlauf und den Beleg, dass eine **Massprobe** den Fehler nicht
  sehen kann. Der Runner bricht ab, statt zu raten.
* ✅ Der neue Wächter hatte zuerst denselben Fehler wie das Bewachte: Beide Seiten hingen
  an derselben Konstanten, der Rundlauf war eine Identität. *Was du bewachst, darf nur
  auf einer Seite der Gleichung stehen.*
* ✅ Gelände: `toposolid` als **einziges** neues Wort, dazu `UMFELD_KLASSEN` für Bäume und
  Nachbargebäude — bis heute zählten sie als Bauwerk. Ausgeschlossen **und benannt**.
* ✅ `auf-51` V2 ist beantwortet und eingebaut; zwei neue Fragen daraus.

**Offen für den Rest der Woche:** die 17 `local`-Aufträge warten auf das Gerät; `cloud`
und `ui` auf Antworten drüben. **Beides lässt sich nicht planen, nur nachrechnen.**

---

## Mittwoch, 02.09.2026 — der Tag hat zwei Äste, und beide sind geplant

**Die Lage am Abend des 1.9.:** Rückstand **30** — `local` 18, `cloud` 8, `ui` 4.
**28 davon warten auf jemand anderen.** Das Wochenziel lautet «unter 15 bis Freitag»,
also fünfzehn Antworten in drei Tagen — und **keine einzige davon kann ich selbst geben.**

Zwei Dinge können morgen früh eintreffen, und beide sind seit gestern erst möglich:

* **Der Takt.** `auf-59` ist entsperrt, die Warnung vor dem Absturz ist angesagt, ein
  `git pull` genügt. Läuft er, bewegen sich die **acht** laufbaren `local`-Aufträge in
  der Rangfolge, die seit gestern in den Dateien steht.
* **Der Zustellbeleg.** `cloud` und `ui` haben ihn seit gestern Abend in allen zwölf
  Blöcken. Morgen ist der **erste Tag, an dem eine Antwort von dort ankommen kann.**

---

### Ast A · Es kommen Antworten — und dann haben sie Vorrang vor allem

*Der Rückstand sinkt nur hier.* Je Antwort: lesen, einbauen **oder** begründet nicht
einbauen, und den Einbau nachhalten. Kein Bauen daneben, solange etwas Unverarbeitetes
liegt.

Drei erwarte ich zuerst, und jede sagt etwas anderes:

1. **`auf-59` V6/V7** — die Zahlen aus ihrem eigenen `homeworker.py --liste`. Stimmen 9
   Fragen und 8 Läufe dort nicht, ist bei uns etwas offen, und das geht allem vor.
2. **`auf-69`** — ihre Regressionsprüfung nach dem gestrigen Umbau. Ist sie rot, hat
   gestern etwas gebrochen, das seit gestern niemand gemessen hat.
3. **Der Zustellbeleg von `cloud` oder `ui`.** Er ist **keine Nebensache**: Er beantwortet
   die Frage, ob zwölf Aufträge bei jemandem liegen oder an einer Stelle, die keiner
   liest. Kommt er, ist das Warten begründet; kommt er nicht, ist der Rückstand dort
   **einer beim Absender** und der Zustellweg gehört geändert.

---

### Ast B · Es kommt nichts — auch das ist zu planen

Dann steht der Rückstand still, **und ich baue nicht daran vorbei.** Was dann drankommt,
in dieser Reihenfolge:

**1 · Das Entscheidungsblatt.** Zwei Entscheidungen sind seit gestern messreif, und beide
sind Owner-Sachen, keine Messungen:

* **Die richtungsabhängige ρ-Schwelle.** Gemessen: Das fehlerfreie Fenster liegt frontal
  bei (0.2568, 0.3790], diagonal bei (0.5311, 0.9065] — sie überschneiden sich nicht. Es
  gibt **keine** einzelne Schwelle für beide Gruppen. `0.80` kostet fünf gute frontale
  Fälle von vierzig und lässt keinen schlechten durch.
* **Die dritte Grösse für die Standpunktwahl — oder keine.** Gemessen: Die Güte kennt über
  acht taugliche Standpunkte höchstens **zwei** Werte, auf einem Würfel einen. Welche
  Zahl einen Standpunkt besser macht, ist eine gestalterische Frage.

Als **Blatt im Repo**, nicht als Frage im Chat — und mit dem, was jede Antwort kostet und
was sie erlaubt. *Eine Entscheidung, die der Owner erst zusammensuchen muss, existiert
nicht.*

**2 · Die Nachbargebäude-Zahl selbst messen.** Der Vorbehalt, den die Maske seit gestern
mitführt, ruht auf einer **fremden** Messung (`auf-37`: +0.0016 gegen −0.0024). Mit
`tools/make_test_glb.py` und dem Apparat aus `tools/studie_paarmasse.py` ist er hier
nachrechenbar: dasselbe Bauwerk einmal vor Himmel, einmal vor einem zweiten Körper.
**Zuerst prüfen, ob es ohne neue Geometrie geht** — wenn nicht, wird es ein Auftrag und
kein Nachmittag.

**3 · Wenn dann noch Zeit bleibt: nichts Neues.** Lieber früher aufhören als einen Vorrat
bauen, den morgen niemand liest. *Der Engpass ist nicht das Bauen.*

---

### Was morgen NICHT passiert

* **Keine Woche-2-Arbeit.** Die Dokumente sind ab dem 8.9. dran.
* **Keine neuen Aufträge.** `local` steht bei 18 von 8, `cloud` bei 8 von 8 — beide
  gesperrt. `ui` hätte Platz, aber es wartet keine Frage. *Ein Auftrag mehr macht keine
  Antwort schneller.*
* **Nichts, was der HomeStation gehört** — die Ergebnis-Waisen und ihr Betrieb sind ihre
  Runde.

### Die Zahl, und warum morgen keine dasteht

`RUECKSTAND: 30`. Eine Zielzahl zu setzen, die an fremden Antworten hängt, wäre eine
erfundene — genau das, was dieser Plan seit Freitag vermeidet. **Planbar ist nur die
Zusage:** Jede Antwort, die eintrifft, wird am selben Tag verarbeitet und ihr Einbau
nachgehalten. Was am Abend dasteht, wird nachgerechnet und nicht vorhergesagt.

---

## Mittwoch, 02.09.2026 — was über Nacht kam, und drei Entscheide

**Ast B aus dem gestrigen Plan gilt: es kam keine Antwort.** Acht Commits der HomeStation,
**drei neue Aufträge**, und auf die dreissig offenen keine einzige Antwort. Rückstand nach
dem Zusammenführen: **33** — `local` 19, `cloud` 8, `ui` 6, ältester **12 Tage**.

Und ein Befund, der alles andere verdrängt hat: **Das Repo liess sich hier nicht mehr
einlesen.**

---

### 0 · Erledigt vor dem Plan: das Repo parst wieder

`src/aiimaging/abholer.py` trug einen **mehrzeiligen Ausdruck innerhalb eines f-Strings**.
Den gibt es erst ab Python 3.12 (PEP 701); `pyproject.toml` verlangt `>=3.11`, und hier
läuft **3.11.15**. Folge: `SyntaxError` beim Einlesen, **25 Testdateien brachen ab, bevor
eine einzige Probe lief.**

**Auf der Maschine, auf der er entstand, war alles grün — sie läuft Python 3.14.4.**

> *Drei Nebenversionen Abstand, ein Repo, und eine Testzahl, die auf einer Seite «alles
> grün» sagt, während die andere nicht einmal importieren kann.*

Behoben: Die Fallunterscheidung ist eine Funktion geworden (`_blickrichtung_satz`) — dort
gehört sie ohnehin hin, sie war im f-String nicht zu lesen. Sammlung wieder vollständig:
**4934**.

---

### 1 · Der Versionsabstand bekommt einen Wächter und eine Ansage

Der Bruch ist behoben; **die Ursache ist es nicht.** Zwei Dinge:

* **Hier:** eine Probe, die jede versionierte `.py` einliest und **beim Namen nennt**,
  wenn eine nicht parst — statt fünfundzwanzig Sammelabbrüche ohne erkennbaren Grund. Ein
  Fehler, den man erst nach dem dritten Traceback versteht, kostet mehr als er meldet.
* **Drüben:** eine **Ansage** — Mindestversion 3.11, ihre 3.14, und der Satz, der beim
  nächsten Mal Zeit spart: *Was auf 3.12+ legal ist, muss es auf 3.11 nicht sein.*
  Dazu ehrlich, **was der Wächter nicht kann:** Von 3.14 aus lässt sich diese Sorte Bruch
  nicht sehen — `ast.feature_version` deckt Sprachmerkmale ab, nicht den Tokenizer.
  *Ein Wächter, der seine blinde Stelle verschweigt, ist gefährlicher als keiner.*

---

### 2 · Owner-Entscheid: unbekannter Status heisst **offen**

Drei Ergebnisse tragen einen Status, den `baue_ergebnis` gar nicht kennt — von Hand
geschrieben: `teilweise` (*«Die übrigen Teile folgen»*), `teilweise — gerettet aus einem
abgebrochenen Lauf`, `erledigt`. **Alle drei gelten heute als beantwortet**, weil
`zustand()` alles als Antwort liest, was kein *benannter* Fehlschlag ist.

Neu: Was `baue_ergebnis` nicht kennt, ist `gerechnet, nicht beantwortet` — also **offen**
— und wird als Mangel gemeldet. *Dieselbe Entscheidung wie am 28.08. beim
Weiterleitungsvermerk, eine Ebene tiefer: Ein Ergebnis zu haben heisst nicht, beantwortet
zu sein.*

**Der Rückstand steigt dadurch**, und das ist der Zweck.

**Dateien:** `src/aiimaging/auftrag.py` (`zustand`, `_NICHT_BEANTWORTET`),
`tests/test_auftrag.py`.

---

### 3 · Owner-Entscheid: der Deckel ist **meine** Selbstbindung, und sagt es

Der Deckel sperrt mich bei `local` (19 von 8) und `cloud` (8 von 8). Die HomeStation hat
über Nacht drei Aufträge gestellt — für sie gilt er nicht, und **das soll so bleiben**:
Sie kennt ihre Lage am besten.

Dann muss er aber auch so heissen. `DECKEL_JE_WORKER` und `DeckelError` beschreiben ihn
heute als Regel für alle; sie beschreiben ihn ab heute als **Selbstbindung dessen, der
durch `schreibe_auftrag` schreibt** — mit der gemessenen Zahl daneben: seit seiner
Einführung ist der Rückstand von 27 auf 33 gestiegen. *Ein Deckel, der nur den bindet,
der ihn eingeführt hat, bremst niemanden — aber er darf wenigstens nicht so tun.*

---

### 4 · Owner-Entscheid: das Wochenziel wird gestrichen

*«Unter 15 bis Freitag»* verlangte 18 Antworten in drei Tagen. In fünf Tagen ist **keine**
gekommen. Das Ziel wird gestrichen, nicht verschoben, und der Grund steht dabei: **Eine
Zahl, die an fremden Antworten hängt, ist keine Planung, sondern eine Hoffnung mit Datum.**

An seine Stelle tritt keine neue Zahl, sondern eine Zusage: Der Rückstand wird täglich
gemessen und gemeldet, und jede eintreffende Antwort wird am selben Tag verarbeitet.

---

### 5 · Die Ränge ordnen — sie sind mein Werkzeug, und es hat zwei Löcher

Die HomeStation hat den Rang übernommen, und dabei ist er doppelt vergeben worden:

* `local` **Rang 3 zweimal** — `auf-20260827-61` und der neue `auf-20260902-71`.
* **Ohne Rang:** zwei `local`, **fünf von sechs `ui`**.

Zu tun: Kollision auflösen, die ranglosen einordnen, und ein **Wächter, dass ein Rang je
Adressat nur einmal vorkommt**. Dazu ein Satz an die HomeStation, wie die Vergabe gedacht
ist — *ein Werkzeug, dessen Regel nur einer kennt, hat keine Regel.*

---

### 6 · Wenn dann noch Zeit bleibt

Das **Entscheidungsblatt** aus dem gestrigen Ast B: die richtungsabhängige ρ-Schwelle und
die dritte Grösse für die Standpunktwahl. Beide sind messreif und beide sind Owner-Sachen.

**Und ein Posten, der heute keinen Adressaten findet:** Die HomeStation hat `V17c`
ausdrücklich offen gelassen — `herkunft.py` kennt keine Kennung `kosmoorbit`, und *«wer
sie einträgt, misst vorher»*. Messen kann das nur sie; `local` ist durch meinen eigenen
Deckel gesperrt. Der Posten wird darum **sichtbar geparkt** statt still zu verschwinden.

---

### Stand am Ende von Mittwoch, 02.09.2026

Alle sechs Punkte erledigt. `RUECKSTAND: 30 → 36` — und das ist der Zweck von Punkt 2,
nicht sein Preis:

* ✅ **0 · Das Repo parst wieder.** Sammlung von 25 Abbrüchen auf **5137** grüne Proben.
* ✅ **1 · Der Wächter** (`tests/test_pythonstand.py`) nennt eine kaputte Datei beim Namen;
  die Mindestfassung kommt aus `pyproject.toml`. Die **Ansage** steht in `auf-59` (Rang 1)
  — samt der Rückfrage, ob `python3.11` dort erreichbar ist.
* ✅ **2 · Unbekannter Status heisst offen.** `STATUS_BEKANNT` an einer Stelle,
  `baue_ergebnis` prüft gegen dieselbe, `tools/einbau.py` nennt die Funde.
  `auf-20260823-36` ist wieder offen und trägt nur noch, was aussteht.
* ✅ **3 · Der Deckel heisst Selbstbindung** — mit der Zahl daneben: 27 → 36 seit seiner
  Einführung, ohne einen einzigen Auslöser.
* ✅ **4 · Das Wochenziel ist gestrichen**, mit dem Grund im Dokument.
* ✅ **5 · Die Ränge**: 36 Aufträge, je Adressat lückenlos von 1 an, drei Wächter darauf.
* ✅ **6 · `V17c` ist beauftragt** (`auf-20260902-73`, letzter Rang) — **am eigenen Deckel
  vorbei**, weil ein Posten ohne Adressaten nie eingebaut wird und diese Hausregel meine
  eigene Bremse sticht. Der Grund steht im Auftrag, nicht in einer Fussnote.

**Nicht geschafft:** das Entscheidungsblatt. Es bleibt der erste Punkt für morgen.

**Und ein Nebenfund:** `tools/tote_kanten.py` gab einen falschen Alarm — ein Aufruf über
`getattr` war für die Namensauflösung unsichtbar. Behoben, mit Gegenprobe. *Ein Werkzeug,
das falsche Alarme gibt, wird nicht schärfer gelesen, sondern gar nicht mehr.*

---

### Was heute NICHT passiert

* **Keine Woche-2-Arbeit** — die Dokumente sind ab dem 8.9. dran.
* **Keine neuen Aufträge an `local` oder `cloud`.** Der Deckel bleibt, jetzt ausdrücklich
  als Selbstbindung.
* **Nichts, was der HomeStation gehört.**

### Die Zahl

`RUECKSTAND: 33` am Morgen, und er wird heute **steigen** — durch Punkt 2, mit Absicht.
Eine Zielzahl steht hier nicht mehr; sie ist heute früh gestrichen worden.

---

## Donnerstag, 03.09.2026 — eine Frage, ein Auftrag, ein Befund

Kurzer Tag. Der Owner fragte nach dem Stand der offenen Aufträge und liess die beiden
Sammelaufträge für den Zustellbeleg schreiben. Protokoll:
`docs/sitzungen/2026-09-03_sitzung-18.md`.

* ✅ **Der Stand, ehrlich gezählt:** 36 offen, keiner bei mir. `local` 22 (letzte Antwort
  28.08.), `cloud` 8 und `ui` 6 — beide **nie**. Vier Aufträge halten hier etwas fest:
  `auf-61` (Entscheidung B), `auf-73`/`M5` (Entscheidung A), `auf-59`/`V9`+`V10` (die
  Python-Fassung drüben), `auf-31`/`auf-37` (seit zwölf und elf Tagen).
* ✅ **Zwei Sammelaufträge, Rang 1:** `auf-20260903-74` (`cloud`) und `auf-20260903-75`
  (`ui`). Sie fragen **nur den Empfang** — keine Messung, keine Zusage, kein Termin, und
  ausdrücklich keine inhaltliche Antwort. Bei `cloud` **am eigenen Deckel vorbei**, mit
  dem Grund als Abschnitt 6 **im Auftrag**: Er ist der einzige, der die Reihe kürzen kann.
* ✅ **Befund — abgelegt ist nicht ausgeliefert.** `auf-70` und `auf-72` lagen zwei bzw.
  einen Tag im Repo und waren **nie hinausgegangen**; der letzte Postlauf war vom 01.09.
  Sie zählten als Rückstand beim Adressaten und waren einer beim Absender. Gebaut:
  `auftraege/zustellung.json` (Kennung und Zeitpunkt, **kein Pfad** — Regel 3),
  `auftragspost.unzugestellt()`, und `tools/einbau.py` meldet es als **erste** Zeile.
  Vermerk **nach** dem Schreiben; kaputter Vermerk heisst «nichts zugestellt»; `local`
  wird nicht vermerkt. Danach ausgeliefert: alle 16.
* ✅ **`ZUSTELLBELEG_STAND` nachgezogen** — er ging zwei Tage mit dem Datum seiner
  Einführung hinaus, neben einer bei jedem Lauf frisch gezählten Zahl.
* ✅ **Lexikon**: **Abgelegt gegen ausgeliefert**, **Stale Bytecode nach einer
  Mutationsprobe**.

**Der Nebenfund des Tages betrifft das Prüfverfahren selbst.** Eine gleich lange Mutation,
in derselben Sekunde zurückgestellt, lief aus dem Bytecode-Zwischenspeicher weiter und
meldete eine rote Probe, die es im Quelltext nicht mehr gab. *Eine Probe, die nach dem
Zurückstellen noch rot ist, ist erst dann ein Befund, wenn sie das auch ohne
Zwischenspeicher ist.*

**Der Rückstand steigt auf 39** (mit dem Sammelauftrag der anderen Lane), und das ist
Absicht: Die drei neuen Aufträge sind die einzigen, die ihn senken können.

* ✅ **Zusammengeführt:** Eine zweite Lane hat den Deckel **unabhängig gemessen** — er
  greift nur über `schreibe_auftrag()`, und von Hand legt jeder daran vorbei ab. Und sie
  hat für `local` denselben Sammelauftrag geschrieben, den wir für `cloud` und `ui`
  geschrieben haben. Drei Lanes, derselbe Befund, an einem Tag. Der Rangkonflikt (ihr
  Auftrag und `auf-59` beide auf 1) ist nach derselben Regel aufgelöst: Wer die Reihe
  kürzen kann, steht vorn.

**Offen für morgen:** die Antwort auf `auf-74`/`auf-75` — kommt sie nicht, ist das nach
drei Anläufen selbst eine Antwort, und dann gehört der **Zustellweg** geändert und nicht
die Bitte wiederholt. Und: Der Postlauf wird weiterhin von Hand gestartet; der Vermerk
*meldet* das Liegengebliebene, er *liefert* es nicht aus. Das gehört zum Takt.

---

## Donnerstag, 03.09.2026 — die drei Posten aus Demolauf 17

*Protokoll: `docs/sitzungen/2026-09-03_sitzung-18.md`. Alle drei erledigt, zwei davon
enden in einem Auftrag an eine andere Lane, weil die Ursache dort liegt.*

- [x] **Posten 3 · Der Bildwert-Rückschritt.** Ursache gemessen und belegt: nicht die
      Szene, nicht die Kette (beide Läufe auf vier Stellen nachgerechnet), sondern **das
      Glas im Soll**. Glas dicht gemacht ⇒ Score 0.000 → **0.2001**, rho +0.6480 →
      **+0.8057** (Lauf 16: 0.2015 / +0.7995). Drei Scheinproben trennen den Befund vom
      Eingriff. Riegel `geometrie_qa.durchsichtiges_soll` gebaut, 8 Proben.
- [x] **Posten 1 · Der Bilddeckel.** Gemessen: Er vergleicht Base64-**Zeichen**, die
      wirkliche Grenze ist **786 432 Byte**, die Meldung nennt 1.0 MB — Faktor 4/3 zu
      hoch. Kein technischer Grund auffindbar. 60 von 66 Nutzbildern liegen darüber.
      Entscheid **«beides trennen»**: `bildschreiben.blattfassung` gebaut, der Abholer
      legt sie neben jedes Bild; der Deckel selbst bleibt unberührt (fremde Lane,
      Owner-Entscheid 02.09.).
- [x] **Posten 2 · Die dritte Perspektive.** Gemessen, bevor gebaut: zwei Ursachen,
      **beide in KosmoOrbit** (unbedingter Filter im Bestellweg + `IfcSpace` wird gar
      nicht eingelesen). Hier gebaut wurde nur die Zahl, die den Fehlbetrag sichtbar
      macht: `kameras.AUGENHOEHE_BAND_M` (1.60–1.70 m über Gelände) samt Zählung, im
      Kameraurteil mitgeführt.
- [x] Zwei Aufträge an den UI-Worker: `auf-20260903-76` (Meldung des Bilddeckels),
      `auf-20260903-77` (Innenraum, beide Ursachen, plus der Widerspruch über die
      «Übersicht»).

**Offen geblieben, mit Grund:**

- [ ] **Der Tiefenpass selbst.** Der Hebel ist benannt (`transparent_max_bounces = 0`,
      aber als **eigener** Durchgang — dieselbe Einstellung ruinierte das Beauty-Bild).
      Änderung am Blender-Runner, gehört hinter einen echten Blender-Lauf.
- [ ] **Der Kantenanteil trägt keinen Szenenvergleich.** Ein zufälliger 5-%-Schnitt in
      der Maske halbiert ihn (Lauf 16: 0.1062 → 0.0457). Solange das so ist, ist jede
      Aussage «Lauf A gegen Lauf B» über dieses Mass unbelegt.
- [ ] **Der Riegel ist abgelesen, nicht kalibriert** — 0 gegen 0.29 % an zwei Läufen.

---

## Freitag, 04.09.2026 — der Bilddeckel fällt

*Owner-Entscheid: **«Deckel weg.»** Er kehrt den Entscheid vom 02.09. («Meldung
verbessern, Deckel nicht anfassen») um, und der Grund ist die Messung vom 03.09.: Der
Deckel **hiess 1.0 MB und war 0.79 MB** (786 432 Byte), **60 von 66** Nutzbildern lagen
darüber (Median 1 277 231 Byte), und ein technischer Grund für die Zahl war nirgends
auffindbar. Das Tor selbst (`vis-jobs.ts`) fällt in der KosmoOrbit-Lane; hier stand nur
die Spiegelung.*

- [x] **Der Zwang ist heraus, die Messung bleibt.** `bildschreiben` kennt **keinen
      Vorgabedeckel** mehr: `BLATT_DECKEL_BASE64_ZEICHEN`, `BLATT_DECKEL_BYTE`,
      `passt_aufs_blatt` und `blattfassung` gibt es nicht mehr. An ihrer Stelle stehen
      `bildgroesse` (misst, urteilt nicht), `passt_unter(png, deckel_byte=…)` und
      `kleinere_fassung(…, deckel_byte=…)` — **beide ohne Vorgabewert**, weil ein
      Vorgabewert der stille Deckel zurück wäre.
- [x] **Der Abholer legt keine ungefragte Kopie mehr an.** `_blatt_ablegen` →
      `_groesse_ablegen`; statt `<kürzel>_blatt.json` + `<kürzel>_blatt.png` entsteht nur
      noch `<kürzel>_groesse.json` mit `{bytes, zeichen, text}`. *Die Zahl war das
      Werkzeug, das den Deckel überführt hat — sie hat nichts verbrochen.*
- [x] **Die Einheit hört auf zu lügen.** Jede Grenze ist jetzt eine **Byte**-Zahl, weil
      die gemessene Grösse eine Byte-Zahl ist; Zeichenzahlen stehen daneben und tragen
      das Wort «Zeichen». `MB` kommt in keiner Meldung mehr vor (10⁶ gegen 2²⁰ war die
      halbe Verwirrung). Geprüft wird das **maschinell**:
      `test_jede_zahl_traegt_die_einheit_die_zu_ihr_gehoert` zerlegt den Satz wieder in
      Zahlen und hält jede gegen ihre Einheit — und
      `test_die_einheitenprobe_kann_widersprechen` baut den Fehler von gestern nach und
      verlangt, dass die Probe ihn ablehnt.
- [x] **Die Proben sind mitgewandert, nicht stillgelegt.** `tests/test_blattfassung.py`
      → `tests/test_bildgroesse.py`, 14 statt 12 Proben. Zwei davon sind neu und prüfen
      genau den Entscheid: kein Vorgabedeckel (`TypeError`, plus die alten Namen dürfen
      nicht zurückkommen) und **keine zweite Bilddatei** an der Naht im Abholer.
- [x] **`auf-20260903-76` zurückgezogen.** Der Auftrag bat den UI-Worker um eine bessere
      Meldung *bei unverändertem Deckel*. An einer Grenze, die es nicht mehr gibt, ist
      keine Meldung zu verbessern. Der Befund, der ihn ausgelöst hat, bleibt gültig — er
      ist die Begründung des neuen Entscheids.

---

## Woche 2 (Mo 8.9. – Fr 12.9.): die Dokumente auf Lesegrösse

**Ziel: `PLAN.md` unter 800 Zeilen, ohne dass etwas verloren geht.**

### 5 · Ein Archiv statt einer Löschung

Die Hausregel bleibt — **abgehakt wird, nicht gelöscht**. Was sie nicht sagt: *wo* das
Abgehakte liegt.

* `docs/PLAN.md` behält **offene Punkte und die Befunde, die noch tragen**.
* Abgeschlossene Kapitel wandern nach `docs/erledigt/PLAN_2026-08.md`, unverändert und
  mit Datum.
* Ein Wächter (`tests/test_plan.py`) hält beides gegeneinander: **kein Punkt darf beim
  Umzug verschwinden.** Gezählt wird von der anderen Seite — Summe der Häkchen vorher
  gleich Summe nachher.

*Das ist derselbe Griff wie bei den Dauerwarnungen: nicht löschen, sondern trennen. Was
alle betrifft, steht einmal; was diesen Vorgang betrifft, steht bei ihm.*

### 6 · Die Sitzungsprotokolle bekommen eine Deckelzeile

14 Dateien, 9311 Zeilen, die längste 3027. Jedes Protokoll bekommt oben **fünf Zeilen:
was entschieden wurde, was gemessen wurde, was offen blieb.** Der Rest bleibt, wie er
ist — er ist die Ablage, nicht der Vortrag.

### 7 · Die Bausteine der Arbeit von den Arbeitsprotokollen trennen

61 Dokumente in `docs/`. Ein Verzeichnis `docs/README.md`, das sagt, **welches Dokument
ein Baustein der Vertiefungsarbeit ist und welches ein Werkstattbericht.** Keine
Verschiebung, nur eine Landkarte — verschoben wird erst, wenn die Struktur der Arbeit
feststeht.

---

## Was ich in diesen zwei Wochen NICHT tue

* **Keine neue Fähigkeit bauen.** Kein neues Modul, kein neuer Messweg. Ausgenommen sind
  die drei Werkzeuge oben, und die dienen alle dem Abarbeiten.
* **Keinen Auftrag stellen, solange der Deckel greift** — ausser die Ansage einer
  Verhaltensänderung, die nach Hausregel ohnehin Pflicht ist.
* **Nichts aufräumen, was der HomeStation gehört.** Die vier Ergebnis-Waisen und die
  Umbenennung von `offen/` sind ihre Runde; sie haben um Zurückhaltung gebeten.
* **Keinen Auftrag stillschweigend schliessen.** Jeder Zustandswechsel bekommt eine
  Begründung, die ein Fremder in einem Monat noch versteht.

---

## Verifikation

Nach jedem Schritt, und am Ende jeder Woche:

```
python -m pytest -q                      # Stand: 4620 grün
python tools/einbau.py                   # Rückstand, Adressaten, Belege
python tools/vakuumprobe.py              # Stand: 13 Treffer, alle mit Gegenprobe
python tools/tote_kanten.py              # Stand: 86 nur über Tests, 0 unerreichbar
```

**Die eine Zahl, an der der Plan gemessen wird**, ist die erste Zeile von
`tools/einbau.py`:

    RUECKSTAND: 34 Auftraege ohne Antwort, aeltester 10 Tage
                ↓  Ziel Ende Woche 1
    RUECKSTAND: <15 Auftraege ohne Antwort

Und für Woche 2: `wc -l docs/PLAN.md` unter 800, bei gleicher Zahl Häkchen.

*Beides ist zählbar und keines ist Auslegung. Was von Hand beurteilt wird, wird
irgendwann nicht mehr beurteilt.*

---

## Was danach kommt, und warum es hier noch nicht steht

Mit sauberem Tisch stehen zwei Dinge an, die heute unter dem Rückstand nicht sichtbar
sind: **die Schwellenkalibrierung auswerten** (sobald `auf-61` zurück ist) und **die
Struktur der Vertiefungsarbeit festlegen**. Beides braucht Zahlen, die noch nicht da
sind — ein Plan dafür wäre heute geraten.
