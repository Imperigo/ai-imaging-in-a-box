# Sitzung 13 · 26.08.2026

*Auftrag des Owners um 08:24: «guten morgen erstelle tagesplan bis 20:00 vollautonom».
Drei Vorgaben dazu, per AskUserQuestion: Trägt `auf-41` die Rahmung nach der Bauwerksbox
→ umstellen. Doppelte Ansicht → erkennen und überspringen. Anfallende Entscheide →
selbst entscheiden, sammeln, um 20:00 vorlegen.*

**Zur Form.** Sitzung 12 ist über drei Tage auf 850 Zeilen und sechzehn Nachträge
gewachsen. Das war eine Folge des Arbeitens, keine Form, die man wiederholt — dieses
Protokoll ist am Stück geschrieben und nach Sache geordnet.

---

## 1 · Was dieser Tag im Kern war

**Der Owner hatte am 25.08. abends einen Satz gesagt, und dieser Tag ist seine
Umsetzung:**

> *«Das sollte natuerlich gar nicht so weit kommen — die Modelle muessen pruefen, ob die
> Geometrie richtig ist und richtig darstellt, **bevor** AI Imaging startet.»*

Die Kette prüfte danach. Und zwar nicht an einer Stelle, sondern an **drei**, die alle
denselben Bau haben: Die Zahlen, die über einen Lauf entscheiden, liegen vor — sie werden
nur zu spät gelesen.

Dazu kamen vier Posten aus dem ersten vollständigen Kettenlauf, drei Befunde der
HomeStation vom 24.08., und mitten am Tag ein neuer Bericht, der denselben Fehler an einer
vierten Stelle auf die Sekunde belegte.

**Sieben tote Kanten trägt diese Woche jetzt** — und seit heute abend gibt es
ein Werkzeug, das nach ihnen sucht, statt auf den nächsten Zufall zu warten (Abschnitt 8b). Die sechste und die siebte sind meine
eigenen: `bbox_bauwerk`/`rahmungsverhaeltnis` (gestern gebaut, nie angeschlossen) und
`geometrie_qa.erreichbarkeit` (seit dem 22.08. da, ausser Tests kein Aufrufer).

---

## 2 · Die Prüfung vor dem Bild — an vier Stellen

### 2.1 Die Rahmung (Posten 1, `auf-vis-20260825-15`)

Ein Auftrag, bei dem das Bauwerk **17,5 %** der Bildbreite füllt, lief bis in die
Diffusion — und das Bildmodell erfand eine Fassadendetail-Aufnahme, weil ihm die Vorlage
fehlte. Die Lösung lag **gebaut und ungenutzt** da: `bbox_bauwerk` und
`rahmungsverhaeltnis` hatten ausser Tests keinen einzigen Aufrufer.

Die Messung der HomeStation, gefüllte Bildbreite → Deckungsmass:

| Bildbreite | 17,5 % | 30 % | 50 % | 65 % | 70 % |
|---|---:|---:|---:|---:|---:|
| Deckungsmass | 0.0002 | **0.0** | 0.001 | 0.637 | 0.932 |

**Der Verlauf ist nicht monoton** — 30 % ist gemessen *schlechter* als 17,5 %. Rendern
ist dort nicht «ein schwächeres Ergebnis», sondern gar keines. `BILDBREITE_ABBRUCH = 0.65`
ist die kleinste Stützstelle, an der gemessen etwas besteht; zwischen den Stützstellen
wird nicht interpoliert.

**Und der Widerspruch blieb stehen.** `BILDBREITE_KNIE = 0.5991` stammt aus der Kniemessung
vom 24.08., die die Schwelle zwischen 0,5991 und 0,6488 fallen sah; die Kettenmessung vom
25.08. sieht bei 50 % noch 0.001. Zwei Messungen, zwei Bedingungen. Genommen ist die
vorsichtigere, und der Grundtext **sagt es**, wenn ein Lauf genau in diesem Band liegt.
Geglättet wurde nichts.

*Zwei Wege brechen ausdrücklich nicht ab: eine vorgegebene Kamera (der Deckungsgrad
beschreibt sie nicht) und eine fehlende Bauwerksbox (das sind alle Aufnahmen vor dem
25.08.). Nicht feststellbar ist kein Abbruchgrund.*

### 2.2 Die Komposition (`auf-vis-20260826-16`, mitten am Tag eingetroffen)

Derselbe Fehler an einer zweiten Stelle, und diesmal auf die Sekunde belegt:

```
08:47:12  Blender fertig, 40 Meshes, Tiefenkarte und Material-IDs liegen vor
08:47:49  das fertige Diffusionsbild wird geschrieben
08:47:58  Auftrag auf 'error': «kamerahoehe_m (77.023) liegt ueber gebaeudehoehe_m (21.3)»
```

**Der Riegel arbeitet richtig und zu spät.** Er verhindert die Rechnung nicht, er
kommentiert sie — und lässt ein plausibel aussehendes Bild liegen, das er selbst gleich
darauf für untauglich erklärt. Die beiden Zahlen lagen **37 Sekunden vor dem Bild** vor.

**Der Entscheid, der beim Bauen aufgefallen ist — und der erste Entwurf war falsch.**
Mein erster `_komposition_vor_dem_render` fing *jede* Ausnahme des Regelwerks ab und
brach ab. Ein eigener Test hat ihn gefällt: `KompositionError` trägt auch
*«Unbekannter bezugspunkt»* — ein **Eingabefehler**, kein Befund über die Aufnahme.
Pauschal darauf abzubrechen hiesse, aus *wir konnten nicht prüfen* ein *durchgefallen* zu
machen. Abgebrochen wird jetzt nur bei der **benannten** Bedingung
(`_kamera_ueber_dach`), und die wird aus dem Bericht **gerechnet** statt aus dem
Regelwerk geholt — sonst hinge der Abbruch daran, dass alles andere in Ordnung ist.

*Blosse `warnungen` brechen nichts ab. Ein Regelwerk, das jede Beanstandung zum Abbruch
macht, liefert am Ende gar keine Bilder — und sähe dabei sorgfältig aus.*

### 2.3 Die abbestellte Bestellung (Posten 2)

`skip: true` wurde gelesen und nicht beachtet. Der Abholer meldete es sogar **selbst**,
wörtlich — «BESTELLT UND NICHT AUSGEFUEHRT» — und rechnete weiter. Wer abbestellt, bekam
geliefert und zahlte die GPU-Zeit.

**Entschieden** (unter der Owner-Freigabe «entscheiden und um 20:00 vorlegen»): Überspringen
heisst **kein Bild, aber sehr wohl eine Antwort**. Gar nichts zurückzugeben liesse die
bestellende Seite hängen — sie kann *übersprungen* nicht von *abgestürzt* unterscheiden.

An der Vertragsgrenze ist das eine **vierte Lage**: `passed: false` heisst dort weder
durchgefallen noch ungeprüft. *Ungeprüft verlangt einen zweiten Lauf, abbestellt verlangt
gar nichts* — und der Unterschied überlebt nur in `verdict.reason`.

### 2.4 Die Erreichbarkeit (letzter Absatz von `auf-vis-20260826-16`)

> *«Die `geom_iou_obergrenze` ist womöglich die aussagekräftigere Zahl als der Score
> selbst, und sie steht heute nur im Befund, nicht in der Meldung.»*

Der Fall dahinter ist der eindrücklichste der Woche: **Dieselbe Geometrie ergibt aus zwei
Kameras zwei verschiedene Gebäude.** Ein elfgeschossiges Wohnhaus ohne Fassade (offener
Deckenstapel, 339 Bauteile, 30 × 30 × 35 m) wird aus der Dreiviertelansicht zu einem
**Parkhaus** mit Autos auf jeder Ebene und frontal zu einem **zweigeschossigen Haus mit
Lamellenfenster**.

`geometrie_qa.erreichbarkeit` beantwortet genau die Frage — und stand seit dem 22.08. ohne
Aufrufer da. Sie hängt jetzt am Kameraurteil, im Kurzbefund und **ganz vorn** in
`verdict.reason`, weil sie alle übrigen Zahlen einordnet.

**Gerechnet wird nur, wenn die Obergrenze auch eine ist.** Bei jeder Hintergrundstrategie
ausser `HG_KEINE` ist sie keine — von der HomeStation am 24.08. selbst gemessen. Sonst
`None`.

---

## 3 · Eine Zahl, die nicht aufgeht — und darum eine Frage statt einer Anpassung

Die HomeStation meldet «Score 0.4971 gegen Schwelle 0.65, erreichbare Obergrenze 0.6909»
und schliesst: *«Auch ein perfektes Bild käme hier gerade eben durch.»*

Nachgerechnet mit unserer eigenen Formel `score = sqrt(|spearman| · geom_iou)`:

* geom_iou-Deckel **0.6909** bei |spearman| 0.998 → höchster Score **0.8304**
* Damit der höchste Score 0.6909 wäre, müsste der Deckel bei **0.4784** liegen

0.8304 ist deutlich über 0.65, nicht «gerade eben». **Zwei Lesarten**, und der Unterschied
entscheidet, ob die Zahl eine Schwelle ist oder eine Einordnung. Verdrahtet ist die erste,
weil `geom_iou_obergrenze` im Code der Anteil der Soll-Geometrie am Bild ist. Gefragt ist
`auf-20260826-43`, mit der zusätzlichen Frage, ob die Obergrenze die beiden Kameras des
Parkhaus-Falls überhaupt **unterscheidet** — *ist sie für beide gleich, ist sie eine
Aussage über die Geometrie und nicht über die Aufnahme, und steht dann an der falschen
Stelle.*

Das ist der Grund, warum daraus eine Frage geworden ist und keine Anpassung. Es wäre der
vierte Fall dieser Woche, in dem eine Zahl ohne ihre Bedingung weitergereicht wird.

---

## 4 · Vier Dinge, die niemand las

### 4.1 Die blinde Wache

`verzeichnis_marke` zählte ausdrücklich **nicht rekursiv**, die Wache lief auf `out/` —
geschrieben wird nach `out/<kuerzel>/`. In fünf Läufen meldete sie als längsten Stillstand
**exakt die Gesamtdauer**, und ihr einziger Alarm war ein Fehlalarm.

*Eine Pause, die genau der Laufzeit entspricht, ist keine Messung, sondern das Fehlen
einer.* Dasselbe Erkennungszeichen wie bei einer Prüfung, die immer besteht.

Eine Ebene tiefer, ausdrücklich **nicht** `rglob`: Das Argument stand seit jeher im
Docstring, und es ist gut — ein rekursiver Lauf über einen Ordner, in den gerade
geschrieben wird, kostet bei jedem Blick Zeit, wird dann seltener befragt und untergräbt
seinen eigenen Zweck.

Der Altbestandstest hiess `_zaehlt_nicht_rekursiv` und prüfte die Blindheit. Er ist
**umgeschrieben statt gelöscht** und misst jetzt beide Grenzen; `tiefe=0` bleibt als
Gegenprobe nachstellbar.

### 4.2 Drei Warnungen, die jeden Auftrag gleich trafen

Der Befund ist **schärfer als gemeldet.** `tools/abholen.py` zeigte `warnungen[:3]`, und
genau drei Warnungen aus `lies_szene` feuern bei einem gewöhnlichen Auftrag. Sie füllten
**alle drei Plätze** — jede echte, auftragsspezifische Warnung, die im Code später steht,
war unsichtbar.

*Der Deckel hat nicht die Geschwätzigkeit begrenzt, sondern die Auskunft gelöscht.*

Die Trennung hängt daran, ob jemand **gewählt** hat: Geerbte 1600 × 1000 sind eine
Vertragsvorgabe, selbst bestellte 999 × 777 eine Warnung über *diesen* Auftrag. Danach hat
ein gewöhnlicher Auftrag **null** eigene Warnungen, und der Deckel konnte ganz weg.

### 4.3 Der Geräteweg (Posten 4)

*«Eine Zeile Protokoll hätte drei Stunden Untersuchung gespart»* — `lade_modell` setzte
`modell.geraet` und `modell.ladeweg` seit dem 19.08., und **kein Aufrufer schrieb sie
irgendwohin**. Darum sah der Fehlschlag vom 25.08. wie ein Rückfall im Code aus, obwohl
sich am Code nichts geändert hatte: Entschieden hatte der freie Kartenspeicher, zwei bis
vier Zehntel Gigabyte.

Das Feld steht jetzt in **jedem** Ergebnissatz, auch bei einer Ablehnung und besonders bei
einem Fehlschlag. Im Kurzbefund nur, wenn der Weg nicht `cuda` war: Er erklärt Laufzeit,
nicht Qualität — und genau diese Verwechslung hat die drei Stunden gekostet.

### 4.4 Die doppelte Ansicht

Bei einem Quader sind `sSE` und `nNW` **byte-identisch** — zweizählige Drehsymmetrie lässt
die beiden Über-Eck-Ansichten der HABS-Regel zusammenfallen. 24,5 s Diffusion für ein
Bild, das schon dalag.

**Erkannt an der Soll-Karte, nicht an der Hüllbox.** Die Hüllbox hat *immer* zweizählige
Symmetrie, auch bei einem völlig unsymmetrischen Bau; aus ihr allein schlüge die Erkennung
bei jedem echten Gebäude falschen Alarm.

**Und die Doppelung darf das Urteil nicht verbessern.** Sie fällt aus `n_gemessen`, aus der
Streuung und aus dem Abschlag. *Das Minimum fällt mit der Zahl der Ziehungen, und eine
Wiederholung ist keine Ziehung* — mitgezählt wäre es dieselbe stille Verschärfung wie am
23.08., als drei Ansichten das Gate ungefragt strenger machten.

---

## 5 · Die ControlNet-Entflechtung, und warum sie anders gebaut ist als vorgeschlagen

`ZImageControlNetPipeline` teilt **67 Parameter** zwischen ControlNet und Transformer,
darunter den **ersten**. `accelerate` prüft beim Auslagern nur den ersten — also gilt der
Transformer als erledigt, und **454 von 521** seiner Parameter bleiben auf der CPU.

Die Diagnose und der Fix stammen von der HomeStation. Zwei Dinge sind hier anders
entschieden:

1. **Nur vor dem Auslagern, nie auf dem vollen Weg.** Die Kopien kosten 1,35 GiB. Auf
   einer Karte, auf der 29,25 GiB verlangt und 28,89 frei waren, wäre das genau der
   Zuschlag, der einen **gesunden** Lauf erst in die Auslagerung drängt — die Reparatur
   richtete den Schaden an, gegen den sie gebaut ist. Beide Richtungen sind getestet.
2. **Ein Fehlschlag der Reparatur hält den Lauf nicht auf.** Ohne sie stirbt er ohnehin;
   mit einem Abbruch wäre zusätzlich die Meldung weg, an der die Ursache erkennbar ist.

Geprüft **ohne torch**, mit Attrappen: Welche Untermodule Parameterobjekte gemeinsam
haben, ist Identität und keine Algebra. *Eine Reparatur, die sich nur auf einer 5090
prüfen lässt, wird nie geprüft.* Was diese Seite nicht wissen kann — ob es die Kette
wirklich durchbringt —, fragt `auf-20260826-42`.

---

## 6 · Was heute NICHT geändert wurde

* **Die Rahmung selbst.** Ob der Runner künftig nach der Bauwerksbox rahmt statt nach der
  Szenenbox, hängt an `auf-41` (G3). Bis dahin *meldet* die Kette den Bruch und rendert
  nicht ins Leere — sie verstellt aber keine Kamera.
* **Die Schwelle 0.65.** Der Parkhaus-Fall ist ein Befund über die Aufnahme und keine
  Einladung, das Tor zu senken. Das steht auch so im Auftrag an die HomeStation.
* **`GERAETE_ZUSCHLAG` (1.25).** Aus `auf-20260818-09` gemessen; ihn zu senken, um den
  Auslagerungsweg zu vermeiden, wäre eine Anpassung an ein Symptom.
* **Posten 6, `idle_window_only`.** `_karte_frei` ist richtig gebaut (fail-closed, mit
  ehrlicher Begründung). Das Problem ist die Voreinstellung der bestellenden Seite, und
  die Frage gehört an KosmoOrbit. **Nicht unsere Entscheidung.**

---

## 7 · Prüfstand

| | Beginn des Tages | nach dem Vormittag | Ende |
|---|---:|---:|---:|
| Tests | 3335 | 3528 | **3901** |
| Vakuumprobe | 10 Treffer | 12 Treffer | 12 Treffer |
| Tote Kanten, nur über Tests erreichbar | — | 79 | 79 |
| Tote Kanten, gar nicht erreichbar | — | 2 | **1** |
| Tote Kanten **mit Urteil** | 0 | 0 | **80 von 80** |
| Hausregeln mit ausführbarem Wächter | 2 von 4 | 2 von 4 | **4 von 4** |
| Dokumente mit ausführbarem Wächter | 1 (Lexikon) | 1 | **5** (+ README, NOTICE, Aufträge, Cloudworker-Blatt) |

*Die Testzahl steht seit heute selbst unter einem Wächter (`tests/test_readme.py`) — sie
kann nicht mehr still veralten, und ihre erste Amtshandlung war, sich selbst zu fangen.*

**Die beiden neuen Vakuumtreffer sind erklärt und nicht übergangen:** Zwei Zusicherungen
über `warnungen` sind vakuumwahr *geworden*, weil die Liste jetzt leer sein **darf** —
genau das war das Ziel von 4.2. Beide haben ihre Gegenprobe **in derselben Zusicherung**
bekommen, nicht in einer anderen Datei.

**Mutationsproben** (beidseitig, wie im Tagesplan verlangt):

* Rahmungsabbruch fest auf `True` → 2 Tests fallen; fest auf `False` → 4 fallen.
* Doppelerkennung fest auf *immer* → 5 Tests fallen; fest auf *nie* → 2 fallen.

*Eine Erkennung, die immer oder nie greift, ist keine.*

---

## 8 · Was am Ende offen ist

* **Vier Aufträge ohne Antwort:** `auf-38` (negativer Prompt), `auf-39`
  (Konversionstreue), `auf-40` (Cloud-Worker), `auf-41` (Grundmessung nach dem
  Deckungsgrad-Wechsel). Dazu die drei von heute: `auf-42` (Entflechtung am Gerät),
  `auf-43` (welche Zahl war 0.6909) und `auf-44` (Azimut-Konvention, **Cloud-Worker** —
  der einzige, der heute etwas blockiert).
* **Der Commit `4a93aa7` der HomeStation liegt nicht auf `origin/main`.** Der Bericht
  `auf-vis-20260826-16.md` ist hier nie angekommen; gearbeitet wurde nach ihrer Nachricht.
  Was sonst noch in der Datei steht, ist ungelesen.
* **Alle drei Nebenbefunde sind am selben Tag geschlossen worden** (siehe
  `docs/PLAN.md`).

  *Beim dritten — `OSError: image file is truncated`, erste Kamera durchgelaufen — lag
  der Prüfstein seit jeher im Haus: `bildlesen._png_bloecke` prüft die Prüfsumme **jedes**
  Blocks. Er wurde nur nie auf die Zwischenbilder angewandt. Die Prüfung läuft jetzt vor
  dem Renderlauf, und aus einem Fehlschlag der Diffusion wird ein benannter Befund über
  eine Datei.*

  *Und sie hat sofort etwas gefunden, das seit Wochen dastand: Die Attrappen dieses
  Projekts schrieben als «PNG» die **acht Signaturbyte** — genau die Gestalt, die die
  Prüfung fangen soll. Sieben Testdateien haben damit ein Zwischenprodukt vorgetäuscht,
  das es so nie gibt. Aufgefallen ist es nie, weil bis heute niemand diese Dateien las.*

  *Schlimmer noch: Die Attrappe in `test_abholer.py` nannte zwei Pfade — `tiefe.png`
  und `beauty.png` —, unter denen **nie eine Datei lag**. Dreiundneunzig Tests haben
  die Kette damit gegen eine Welt geprüft, in der ihre Eingaben nicht existieren.*

  *Der Glossarbefund war der beunruhigendste, und seine Ursache ist in allen drei Fällen
  dieselbe: Das Glossar hatte die **Bausteine** und nicht das **Wort** — `aussen` +
  `perspektive`, `nachmittag` + `licht`, `foto` + `realistisch`. Eine zusammengesetzte
  Form fällt zwischen sie, und im Deutschen ist die zusammengesetzte Form der Normalfall.
  Am Gerät ist gemessen, dass Deutsch das Bild verändert (8 von 8 gepaarten Startwerten,
  blauerer Himmel) — «rechnet trotzdem» hiess also, dass ein halb deutscher Prompt bis
  ins Bild kommt.*

  *Und beim stillen Rückfall auf `/ai/` lag der Schaden in der Meldung: Sie nannte das
  **Modell** («Gewichte für 'z-image-turbo' unvollständig») und verschwieg, dass der Pfad
  gar nicht existiert. Wer sie liest, prüft das Modell statt die Umgebung.*
* **Zwei Freigabewege, und nur einer wird benutzt.** `jobs.freigeben` nennt sich im
  eigenen Docstring *«die einzige Tür zum Ausführungspfad»* — und hat **keinen Aufrufer**.
  Der Produktivverkehr kommt durch die fremde Warteschlange, wo `bruecke._freigabe` über
  einen fremden Token entscheidet; unser eigener Jobstore samt Freigabe wird von der Kette
  nie berührt.
  *Ein Sicherheitsriegel, der nirgends im Weg steht, schützt niemanden — und wer den
  Docstring liest, hält ihn für den Riegel, der er nicht ist.* **Das ist der einzige
  Befund des Tages, den ich ausdrücklich nicht selbst entschieden habe:** Es geht um die
  Frage, wer eine GPU freigeben darf.
* **Posten 5.2:** `denoise` und Schrittzahl werden nicht abgebildet. Der fremde Vertrag
  hat dafür **nur** `faithful`, und ein Regler von 0 bis 1 kann drei Grössen nicht
  ausdrücken — die Wirkung ist zudem nicht monoton (0.80 schneidet besser ab als 1.00).
  *Hier zu raten hiesse, aus einer Zahl zwei zu erfinden.*
* **Der Nullpunkt des Sonnen-Azimuts** (`auf-20260826-44`, Cloud-Worker). Bedient wird
  der Sonnenstand seit heute; **unter welcher Konvention**, sagt der fremde Vertrag nicht.
  Die beiden üblichen liegen 180 Grad auseinander.

---

## 8a · Der Sonnenstand — und ein Kommentar, der zwei Zahlen falsch nannte

**Der gefährlichste der stehengebliebenen Felder**, und genau darum, weil das Ergebnis
richtig *aussah*: Der Runner setzte eine feste Sonne, und ein Auftrag mit Abendstand wurde
gerendert, als wäre er nicht gestellt worden — ein sauberes, gut belichtetes, falsches
Bild.

Gebaut ist er jetzt als eigenes Modul `aiimaging/sonne.py`: reine Trigonometrie diesseits
der Prozessgrenze (Regel 4), durchgereicht über `seams.glb_zu_multipass(sonne=…)`. **Nur
was wirklich bestellt wurde** wandert mit — ein mitgeschickter Vorgabewert wäre im Bericht
von einer Bestellung nicht mehr zu unterscheiden, und genau diesen Unterschied führt dort
das Feld `bestellt`.

**Der Fund, der dabei abfiel.** Die feste Drehung stand als
`(radians(50), 0, radians(35))` da, kommentiert mit *«50° Höhe und 35° Azimut»*. **Beide
Zahlen im Kommentar sind falsch:** Eine Blender-Sonne strahlt entlang −Z, der Winkel über
dem Horizont ist also `90° − rx` — es sind **40°** und nicht 50. Und `rz = 35°` stellt die
Sonne 35° **östlich** von Süden, in der Süd-Konvention also **−35°** und nicht +35.

*Ein Kommentar ist keine Rechnung.* Aufgefallen ist es nur, weil `tests/test_sonne.py` die
Strahlrichtung aus den Eulerwinkeln **zurückmisst**, statt zu prüfen, dass die Formel
liefert, was die Formel rechnet. Ein Test gegen sich selbst hätte beide Fehler bestätigt.

Und die Vorgabe stellt **bitgenau dieselbe Drehung** wie bisher. Ohne diese Prüfung wäre
jedes Bild ab heute anders beleuchtet als alle Messungen davor — und der Vergleich über
die Wochen still kaputt.

**Was offen bleibt und nicht unsere Entscheidung ist:** ab welcher Richtung `azimuth`
zählt. Von Norden (Meteorologie) oder von Süden (Bauphysik) — **180 Grad Unterschied**,
und der vertauscht Vormittag und Nachmittag. Angenommen ist *von Süden*, weil die alte
feste Sonne so gemeint war; alles andere hätte jede Messung dieser Woche unvergleichbar
gemacht. Die Annahme steht als **Warnung an jedem Auftrag mit Sonne** und im Bericht des
Runners — *ein falsch gewählter Nullpunkt ist damit auffindbar statt unsichtbar.*

---

## 8b · Tote Kanten werden künftig gesucht statt gefunden

**Sieben tote Kanten in einer Woche, jede einzeln und jede durch Zufall** — und die
letzten beiden waren eigene, gemeldet von aussen. Das ist kein Zufall mehr, das ist eine
fehlende Prüfung.

**Die Handzählung vom 25.08. hätte beide nicht gefunden.** Ihr Kriterium war *«null
Produktrufe UND null Testrufe»*, und genau das übergeht die gefährliche Sorte: Eine
Funktion **mit** gründlichen Tests und **ohne** Aufrufer sieht nicht verdächtig aus,
sondern **fertig**.

`tools/tote_kanten.py` fragt darum nach **Erreichbarkeit von einem Einstiegspunkt aus**.
Der erste Entwurf zählte «der Name kommt in einer anderen Datei vor» und meldete 164 von
261 — unbrauchbar, und genau die Art Fehlalarm, an der ein Suchwerkzeug stirbt. Er ist
verworfen und ersetzt worden, bevor irgendetwas darauf gebaut war.

**Erste Erhebung: 261 öffentliche Funktionen, 81 nur über Tests erreichbar, 2 gar nicht
genannt.** Die 81 sind grösstenteils Studienmodule. Die zwei nicht — und einer davon war
`sonne.aus_bestellung`, **am selben Tag gebaut und am selben Tag als tot gemeldet.** Das
Werkzeug hat sich damit an seinem ersten Lauf selbst gerechtfertigt.

Der andere, `fortschritt.beobachte`, hat nach der Regel des Projekts (*ungeprüft ist
schlimmer als ungerufen*) drei Prüfungen bekommen statt gelöscht zu werden.

Und eine Zahl, die vorher niemand hatte: **`komposition.py` war zu neun Zehnteln
unerreicht.** Das war bekannt — aber es stand nirgends als Zahl.

**Das erste Stück davon ist noch am selben Abend angeschlossen worden, und es hat gelogen,
als es gefunden wurde.** `fehlende_ansichten` gab mit der heutigen Vorgabe
`("s", "sSE", "nNW")` schlicht `()` zurück — «nichts fehlt». Das stimmt nicht:
*Umgebungs-* und *Frontalansicht* liegen **beide** auf `s` und unterscheiden sich allein
im **Ausschnitt**. Eine Aufnahme aus `s` deckt genau eine von beiden ab, und aus der
Richtung ist nicht zu sagen, welche.

Ein Test hiess sogar `test_der_volle_habs_satz_laesst_nichts_fehlen` und hat die
Fehlaussage festgeschrieben. *Ein ungerufenes Stück Code wird nicht nur nicht benutzt — es
wird auch nicht widerlegt.* Beides ist berichtigt; die dritte Antwort gilt jetzt auch hier.

**Und die 79 sind eingeordnet worden, statt als Zahl stehenzubleiben** (Tabelle in
`docs/PLAN.md`). Der grösste Teil sind Analysemodule und der **Innenraum-Zweig** von
`komposition.py` — die Kette rendert Aussenansichten, und nachgeprüft liefert `aufnahme`
die Aussen-Grössen bereits am Produktpfad. Fünf warten auf `auf-40`, zwei auf `auf-39`,
zwei sind der Graph-Nachfolger `kette.py`, der gebaut und noch nicht übernommen ist. *Was
übrigbleibt, sind Einzelfälle in acht Modulen — die einzige Gruppe ohne Begründung und
damit die einzige, die noch Arbeit ist.*

Dabei fiel noch ein veralteter Wert **in Prosa** auf: Der Docstring nannte die
Projektvorgabe `("sSE",)`, seit dem 23.08. sind es drei Richtungen. Eine Sondierung über
alle Docstrings fand **genau diesen einen** echten Fall unter sieben Verdachtsfällen.
*Daraus ist bewusst kein Werkzeug geworden* — fünf Fehlalarme auf einen Treffer ist genau
das Verhältnis, an dem ein Suchwerkzeug stirbt.

---

## 8c · Acht Riegel sind noch keine Kette

Jede der acht Prüfungen dieses Tages war für sich geprüft. Sie stehen aber im selben
Durchgang, in einer festen Reihenfolge, sie schreiben in dasselbe Urteil, und mehrere
können einen Lauf abbrechen. **Ob sie sich gegenseitig im Weg stehen, zeigt kein einziger
von ihnen.**

`tests/test_kettenlauf_26august.py` fährt darum einen Auftrag durch `hole_einen` bis ins
Vertragsergebnis — und hat sofort etwas gefunden, das kein Bausteintest sah:
**`hole_einen` reichte `uebersprungen` nicht an `bruecke.schreibe_ergebnis` durch.** Im
Vertragsergebnis eines abbestellten Auftrags stand «keine QA gelaufen», ununterscheidbar
von einem vergessenen Lauf.

*Die vierte Lage war gebaut, geprüft — und an der Naht nicht angeschlossen.* Das ist
dieselbe Fehlerart wie die toten Kanten des Tages, nur eine Ebene höher: Nicht die
Funktion fehlte, sondern ihr letzter Meter.

---

## 8d · Der Nachmittag: drei Riegel, die es schon gab, liefen nicht

Der Vormittag hatte den Owner-Einwand an vier Stellen geschlossen. Beim Suchen nach der
nächsten Arbeit fiel auf, dass es eine **fünfte** gibt — und dass sie älter ist als alle
vier.

### 8d.1 Der Torwächter lief auf dem Weg, der Bilder erzeugt, nirgends

`src/aiimaging/torwaechter.py` ist der Riegel gegen Massstabsfehler, und sein eigener
Docstring nennt den Anlass: *«Eine Konversion kann sauber `ok` melden und die Geometrie
trotzdem um Faktor 1000 danebenliegen … der Runner war zufrieden, das Modell 30 km gross,
und der Fehler fiel erst am fertigen Bild auf.»*

Nachgezählt statt vermutet: `grep -c torwaechter` über `abholer.py`, `bruecke.py` und
`tools/abholen.py` ergab **0, 0, 0**. Er hing an `werkzeuge.enqueue_render` und an
`kette.py` — also am MCP-Einlass und an einem Modul ohne Aufrufer. Der Weg, den ein
Auftrag der Vis-Oberfläche wirklich nimmt, kannte ihn nicht.

*Achte tote Kante der Woche, und die teuerste: gebaut, geprüft, ausführlich begründet —
und genau darum für vorhanden gehalten.*

### 8d.2 Zwei Fehler im ersten Entwurf, beide gemessen widerlegt

Der erste Entwurf hängte den Riegel an, wie der Plan es vorsah: abbrechen, wenn
`entscheidung == ablehnen_massstab` **und** `verdacht_faktor is not None`. Beides war
falsch, und beides fiel erst beim Nachrechnen auf.

**(a) Er stand an der falschen Stelle.** Er sass in der Kameraschleife, hinter der jeder
Durchgang mit `continue` weitergeht. Der Massstab ist aber eine Eigenschaft der
**Geometrie** und nicht der Kamera: Der Riegel hätte bei sechs Kameras sechsmal dasselbe
Urteil gefällt und dafür fünfmal ~97 s Blender bezahlt, obwohl es nach dem ersten
feststand. Er steht jetzt ausdrücklich **vor** der Schleife, und im Code steht dabei,
warum.

**(b) Ein benannter Verdachtsfaktor ist kein Beweis für einen Einheitenfehler.** Das war
die Annahme, auf der die ganze Abbruchregel ruhte. Selbst nachgemessen, an den Grenzen der
Funktion:

| grösste Kante | `verdacht_faktor` | was das in Wirklichkeit ist |
|---|---:|---|
| 0,003–1,0 m | 0,001 | **jeder Bauteilrender** — eine Tür, ein Fensterdetail, ein Möbelstück |
| 3 000–10⁶ m | 1 000 | oft **ein einziges verirrtes Mesh** 4 km neben dem Bauwerk |

Ein Riegel mit dieser Regel hätte also genau die Aufträge abgelehnt, die dieses Projekt
tragen soll. **Er meldet darum, und er bricht nichts ab** — und `auf-20260826-45` fragt
die HomeStation nach der Fehlalarmrate am wirklichen Bestand, bevor daraus ein Abbruch
wird.

*Das ist dieselbe Sorte Korrektur wie Entscheid 4 vom Vormittag, aber sie geht weiter:
Dort wurde ein Abbruch auf eine benannte Bedingung eingeschränkt. Hier stellte sich heraus,
dass die benannte Bedingung selbst nichts benennt.*

### 8d.3 Welche Box gemessen wird, steht im Ergebnis

Bevorzugt `bbox_bauwerk`, Rückfall auf `bbox`, und **welche von beiden es war, steht im
Urteil**. Der Grund steht gemessen im Torwächter selbst: An 40 echten Dateien fielen zwei
Modelle mit **1002 m und 1127 m** durch — Gelände samt Umgebung, Einheit völlig in
Ordnung. Auf der Szenenbox wäre der Riegel also ein Riegel gegen grosse Grundstücke.

Eine Kleinigkeit mit Folgen: `_massstab_gemeldet` **baut** sich seinen Eingabebericht
(`{"status": "ok", "bbox": …}`), statt den Multipass-Bericht durchzureichen. `status`
bedeutet dort etwas anderes, und der Zweig, in dem das aufgefallen wäre, ist ohnehin
unerreichbar — `seams` wirft vorher `SeamError`. *Eine Zahl gehört an die Bedingung, unter
der sie gemessen wurde; ein Feldname gehört an die Bedeutung, unter der er gelesen wird.*

Ein fehlendes `bbox` ergibt `ablehnen_konversion` und bricht **nicht** ab, und die
Kurzbefundzeile schweigt dann. Nachgezählt: In den Attrappen von vier Testdateien kommt
`bbox` **null**-mal vor — ein Abbruch hier hätte dutzende Tests gefällt und, schlimmer,
jeden älteren Auftrag ohne das Feld. *Nicht feststellbar ist kein Abbruchgrund*, zum
vierten Mal an diesem Tag.

---

## 8e · Was `verarbeiter` liest, und was davon ankommt

Dieselbe Frage wie an der Naht zur Vis-Oberfläche, nur eine Ebene tiefer. Gezählt:

* `seams.glb_zu_multipass` hat **18** echte Einstellungen, durchgereicht werden **12**.
* `render.RenderAuftrag` hat **12** Felder, gesetzt werden **7**.

**Nirgends stand, welche der zehn Absicht sind und welche Lücken.** Genau dieser
Unterschied hat am 23.08. zweimal an einem Tag eine Lücke verdeckt. Die vier Tabellen
(`MULTIPASS_DURCHGEREICHT`, `MULTIPASS_STEHENGEBLIEBEN`, `RENDER_DURCHGEREICHT`,
`RENDER_STEHENGEBLIEBEN`) sagen es jetzt Feld für Feld, mit Vorgabewert, Absicht und
Grund.

Die Aufteilung, und sie ist **sechs zu fünf** und nicht fünf zu fünf — beim Zählen fehlte
zuerst `shift_y`:

| Absicht | Lücke, und wo sie schon hängt |
|---|---|
| `beauty`, `material_id` — die Kette braucht beide | **`kamera_huellbox`** — der Hebel für die Rahmung nach der Bauwerksbox, wartet auf `auf-41` |
| `shift_y`, `herzschlag_takt_s` — der Abholer führt eine eigene Wache | **`negativ_prompt`** — sieben von sieben Stilen führen einen, keiner erreicht je einen Render |
| `fuehrung`, `modell_wurzel` | **`schritte`**, **`denoise`** — warten auf `auf-44`; **`timeout`** — 900 s gegen ~97 s gemessen, bei höherer Auflösung unbelegt |

Der Test hält die Tabellen über `inspect.signature` **gegen die wirkliche Signatur**. Wer
einen Parameter hinzufügt und die Tabelle nicht anfasst, bekommt es rot. *Eine Tabelle,
die von Hand gepflegt werden muss, veraltet — an diesem Tag zweimal nachgewiesen.*

---

## 8f · Das README untertrieb in Fettschrift

Es ist die Eingangstür der Vertiefungsarbeit und war seit dem 20.08. nicht angefasst,
während sich der Stand täglich änderte. Geprüft, nicht vermutet:

| Stelle | behauptet | wirklich |
|---|---|---|
| `README.md:19` | «Ein echter Render … hat **nie** stattgefunden» (fett) | 18.08. gelaufen, `auf-20260818-09`: `status: ok`, 147,9 s, Score 0,359 |
| Bildmodell-Stufe | «nie mit echten Gewichten ausgeführt» | dieselbe Messung; am 25.08. zusätzlich bis in die Diffusion |
| Kette als Graph | «**läuft**» | gebaut und gemessen, aber **kein Aufrufer** |
| Tests | **1509** | **3595** |
| Standtabelle | — | **ohne Datum** |
| `PLAN.md:3–4` | «bis auf den ersten echten Render erledigt» | derselbe Render acht Zeilen weiter unten als erledigt vermerkt |
| `PLAN.md` Wissensschulden | GEOS/libquadmath «**nicht im `NOTICE`**» | beide stehen drin (`NOTICE:58`, `:72`) |

Zwei Untertreibungen, zwei Übertreibungen, drei veraltet. **Die Richtung ist nicht das
Problem — dass es niemand merkt, ist es.**

`tests/test_readme.py` bewacht darum ab heute drei Dinge, und nur diese drei: die
**Testzahl** (gegen `pytest --collect-only -q` im Unterprozess, exakt), die **Existenz
jeder verlinkten Datei**, und die **Existenz der drei Funktionen**, die das README als
ausführbaren Beleg der vier Regeln nennt.

**Kein Wächter auf Prosa.** Das wäre die Sondierung vom Vormittag noch einmal: ein Treffer
auf fünf Fehlalarme, und der erste Fehlalarm schaltet das Werkzeug ab.

Der Wächter fällt in fünf Mutationen — Testzahl um eins verstellt, Links tot, Beleg auf
eine gelöschte Funktion, Standdatum entfernt, Beleg aus der Prosa verschwunden. **Seine
erste Amtshandlung war, sich selbst zu fangen:** Er meldete 3587 gegen 3595, weil er acht
eigene Tests mitbringt.

Ein Missgriff dabei, und er gehört ins Protokoll, weil er wiederkehrt: Die erste
Mutationsprobe stellte die Datei mit `git checkout README.md` zurück — und warf damit die
noch nicht eingecheckte Überarbeitung desselben Tages weg. Wiederhergestellt aus dem
Sitzungsverlauf, danach über Sicherungskopien statt über Git zurückgestellt. *Eine
Aufräumzeile, die an `HEAD` zurückgeht, ist kein Aufräumen, sondern ein Verwerfen.*

`PLAN.md` führt zweierlei nach: Die Kopfzeile nannte den Render acht Tage lang als
ausstehend. Und die GEOS/libquadmath-Bringschuld ist abgetragen — nach der Hausregel
dieses Plans **durchgestrichen mit Datum, nicht gelöscht** (`PLAN:7`).

---

## 8g · Achtzig Meldungen, achtzig Urteile

Der Plan des Nachmittags war durch, und keiner der acht offenen Aufträge war beantwortet.
Die nächste Arbeit stand im Werkzeug vom Vormittag: **80 öffentliche Funktionen ohne
Aufrufer**, und sein eigener Schlusssatz sagt, was danach zu tun ist — *«Wer eine Meldung
prüft, sucht den Aufrufer selbst.»*

Bis dahin hatte das niemand getan. Eine Liste von 80 Kandidaten ohne Urteil ist aber kein
Fortschritt, sondern eine Einladung, beim nächsten Mal wieder wegzusehen — und die achte
tote Kante dieser Woche, der `torwaechter`, ist genau so acht Tage lang unbemerkt
geblieben.

### Die Frage, die trägt

Nicht *«wird sie gerufen?»* — das steht schon im Bericht. Sondern: **was läuft
stattdessen?** Eine ungerufene Funktion neben einer gerufenen Geschwisterfunktion ist etwas
ganz anderes als eine, deren Aufgabe niemand erledigt.

| Gruppe | Namen | Urteil |
|---|---:|---|
| **A · Studie und Kalibrierung** | 23 | zu Recht ungerufen. Eine gelöschte Herleitung macht aus einer abgeleiteten Schwelle wieder eine gesetzte |
| **B · Fähigkeit für eine Phase, die nicht läuft** | 19 | die Lücke ist im README, im `PLAN.md` oder in einem offenen Auftrag bereits benannt — *das ist der Unterschied zum `torwaechter`: Der galt als angeschlossen* |
| **C · Bewusst anders gelöst** | 27 | jede einzelne eine dokumentierte Entscheidung |
| **D · Offen** | 11 → **4** | drei Fäden, von denen sich der grösste beim Nachprüfen auflöste |

Der Kern von Gruppe C, in einem Satz: **Der Produktpfad misst, statt nachzuschlagen.**
`geometrie_qa.anker_fuer` schlägt den Nullanker einer Szene in einer Tabelle nach —
`abholer._nullprobe` misst ihn für *diese* Soll-Karte. Dasselbe bei `erreichbarkeit_fuer`
gegen `_erreichbarkeit_dieser_szene`. Beide Tabellenfassungen sind darum ungerufen, und
das ist die bessere Wahl und kein Versäumnis.

### Der Verdacht, der sich beim Nachprüfen auflöste

Der erste Entwurf des Berichts nannte `komposition.beurteile_kamerasatz` *«den stärksten
Kandidaten auf die neunte tote Kante»*. Die Prüfung, die derselbe Absatz verlangte,
widerlegt ihn: Die Funktion nimmt die Antwort von `kameras.kamerasatz` entgegen — den
Satz, wie die **Bibliothek** ihn rechnet. Auf dem Produktpfad rechnet ihn **Blender**, und
genau dafür gibt es `beurteile_bericht`, das der Abholer aufruft.

*Die Widerlegung steht im Bericht neben dem Verdacht und ersetzt ihn nicht.* Wer ihn morgen
wieder fasst, findet die Antwort gleich daneben. **Eine halbe Stunde Lesen hat einen Umbau
verhindert, der Fachlogik verdoppelt hätte.**

### Die eine Doppelung, die dem Owner vorgelegt wird

`gate.py` heisst **DAS DOPPEL-GATE** und trägt im Docstring den Anlass der ganzen Arbeit:
Im Vorläufer meldete das Stil-Gate allein `bestanden` auf einen Render mit halluzinierter
Kubatur. Auf dem Produktpfad ruft es niemand — das UND bildet `kosmo_szene.als_ergebnis`
selbst.

Nachgerechnet urteilen beide Fassungen in **jedem wirklich vorkommenden Fall** gleich:
`gate.py` ist fail-closed, `als_ergebnis` lässt ein `None` aus dem UND fallen — und ein
Stilurteil fehlt nur, wenn kein Stil bestellt war. Wurde einer bestellt und nicht gemessen,
steht dort `passed: false`.

*Es ist also keine Lücke, sondern zwei Fassungen derselben zentralen Entscheidung* — und
das ist dieselbe Fehlerart wie ein doppelter Lexikoneintrag. **Der Anschluss ist trotzdem
kein Aufräumen:** Er änderte das Verhalten im Fall *«kein Stil bestellt»* von *bestanden*
auf *nicht bestanden*. Das ist eine Vertragsänderung und gehört entschieden.

---

## 8h · Eine Hausregel ohne Wächter ist eine Bitte

Der eine Faden aus Gruppe D, der sofort zu schliessen war, betrifft die eigene
Arbeitsweise — und er ist unangenehmer als die anderen, weil er von heute stammt.

`auftrag.baue_auftrag` hat keinen Aufrufer: **Aufträge entstehen von Hand.** Das allein
wäre harmlos. Gemessen an den Dateien im Repo ist es das nicht:

| | Aufträge in `auftraege/offen/` |
|---|---:|
| mit einem Feld `anweisung` | **4** (alle vom 26.08.) |
| ohne | **42** |

`CLAUDE.md` verlangt seit dem **22.08.2026**, dass ein Auftrag seine Anweisung **in sich**
trägt. Aus demselben Absatz stammt das Pflichtfeld `worker` — **und das ist in
`pruefe_auftrag` gelandet, die Anweisung nicht.** Halb im Code, halb nur im Text. Vier
Aufträge sind seither ohne das Feld hinausgegangen, und **an diesem Tag hat sich das Format
zweimal still geändert** (`anweisung` kam dazu, `rueckgabe` wurde vom Wörterbuch zur
Liste), ohne dass die Bibliothek davon erfuhr. *Beide Änderungen sind meine.*

`tests/test_auftraege.py` prüft ab jetzt die **wirklichen Dateien** — dort war die Lücke,
nicht in der Bibliothek: Vertrag, Kennung gegen Dateinamen, `rueckgabe`, und ab dem
Regeltag eine Anweisung, die kein Stummel ist.

**Die Schranke ist gemessen und nicht gesetzt.** Über alle 46 Dateien:

| | kürzeste Anweisung |
|---|---:|
| seit dem Regeltag (n = 18) | **2568** Zeichen |
| davor (n = 28) | **207** Zeichen |

Die Regel hat also messbar etwas geändert. Die Schranke steht bei 1000 — weniger als die
Hälfte des kürzesten wirklichen Auftrags, mehr als das Vierfache des kürzesten Stummels.
Sie fängt einen Platzhalter und bedrängt keinen knappen, aber vollständigen Auftrag. *Eine
lange Anweisung ist damit nicht als gute nachgewiesen — nur eine sehr kurze als
unvollständige.*

Dazu die Gegenprobe, ohne die das Datum `REGEL_GILT_AB` wirkungslos wäre: Ein Test hält
fest, dass die Schranke die beiden Gruppen **wirklich trennt**. Läge auch der Altbestand
darüber, prüfte der Wächter darüber nichts und wäre grün, weil alles grün ist.

**Die alten Aufträge werden nicht umgeschrieben.** Vier von ihnen sind unbeantwortet
unterwegs; einen laufenden Auftrag umzuformatieren hiesse, die Frage zu ändern, während
jemand daran arbeitet. Sie tragen ihre Anweisung im Fliesstext, was die Regel inhaltlich
erfüllt.

---

## 8i · Regel 1 hatte keinen Wächter — und der erste Lauf fand etwas

Nach der Triage war die Frage naheliegend: Welche der vier Regeln steht sonst noch nur im
Text? **Regel 1 ist die oberste**, und ihre ausführbare Seite ist das `NOTICE`. Geprüft
war bis dahin nur die *Herkunft* einer Lizenzangabe in den drei Registries
(`tests/test_lizenzquelle.py`); die Datei, in der die Auflagen behauptet werden, prüfte
nichts.

### Der Fund

Der Eintrag **`libquadmath` (LGPL-2.1-or-later)** nennt zwei der drei Auflagen der
LGPL-Präzisierung wörtlich — *«Unveraendert verwendet, austauschbar»* — und **die erste
nicht.** Nachgezählt: `numpy` wird an genau **einer** Stelle im Produktpfad importiert
(`aiimaging.render`, verzögert, für die Umrechnung einer 16-Bit-Tiefenkarte). Damit
erreicht libquadmath den Produktprozess und liegt **nicht** hinter einer Prozessgrenze —
anders als GEOS, das ausschliesslich im `.venv-ifc` liegt.

Der Kommentar an jener Zeile lautete *«NumPy (BSD-3) — nur für diesen Fall»*. **Das ist
genau der Fehler, den die eigene Binärprüfung dieses Projekts am 18.08. benannt hat:**
*«numpy deklariert BSD-3-Clause; die LGPL-Komponente wird von der Wheel-Angabe nicht
genannt.»* Der Bericht stand da, und der Kommentar wiederholte den Fehler trotzdem.

**Zwei Sätze, die auseinanderzuhalten sind:**

* Nach der **LGPL selbst** ist die Lage unproblematisch — sie erlaubt das Linken aus
  nicht-copyleft Code, solange die Bibliothek austauschbar bleibt, und das ist sie.
* Die **Hausregel in `CLAUDE.md` ist strenger als die Lizenz.** Sie verlangt die
  Prozessgrenze für jede LGPL-Bibliothek, ohne Ausnahme für Anteile, die statisch in einem
  permissiven Wheel mitreisen.

*Entschieden wurde das nicht.* Der Import bleibt, wo er ist, der Zustand steht benannt im
`NOTICE`, und die Frage liegt beim Owner. **Ein stillschweigend entfernter Import wäre eine
Antwort auf eine Frage, die niemand gestellt hat** — und ein Verstoss gegen die Regel, dass
solche Funde ausdrücklich zu melden sind.

### Der Wächter musste zweimal gebaut werden

Der erste Entwurf suchte im Blocktext nach «Prozessgrenze», «Subprozess» und Verwandtem.
**Die Mutationsprobe hat ihn widerlegt:** Nimmt man dem `libgomp`-Eintrag seine
GCC-Ausnahme weg, bleibt der Test grün — denn derselbe Block *erklärt im Konjunktiv*, was
man täte, wenn die Ausnahme nicht griffe: *«dann waere der Ausweg derselbe wie zweimal
zuvor: ein eigenes Environment und ein Subprozessaufruf»*. Der Wortfilter hielt diesen
Konjunktiv für eine Zusage.

*Ein Wächter, der sich von einem Konjunktiv täuschen lässt, bewacht die gefährlichste
Stelle nicht: die, an der jemand ausführlich begründet, warum etwas in Ordnung sei.*

Die Lehre ist dieselbe wie an drei anderen Nähten dieser Woche: **nach einer Erklärung
fragen, statt sie aus Prosa zu erraten.** Jeder Copyleft-Eintrag trägt jetzt eine Zeile
`AUFLOESUNG:` mit genau einem von drei Werten — `Prozessgrenze`, `Lizenzausnahme` oder
`KEINE`. Der dritte ist zugelassen, damit ein ungelöster Fall **sichtbar** dastehen kann,
statt in einem Absatz zu verschwinden; er verlangt einen Eintrag in einer kurzen Liste im
Test, mit Grund. Dieselbe Bauart und dieselbe Gefahr wie `ABSICHTLICH` im
Tote-Kanten-Werkzeug — eine wachsende Liste dort heisst weggesehen statt geprüft.

Der Wächter fällt in sechs von sechs Mutationen. Und er fiel zuerst über den **eigenen
neuen Dokumentkopf**: Der nennt «GPL, LGPL, AGPL» als die Lizenzarten, die eine Marke
tragen, und die AGPL-Prüfung hielt das für einen Bestandteil. Sie liest jetzt nur die
Komponentenblöcke.

---

## 8j · Ein wirklicher Name stand in einem öffentlichen Repo — an fünf Stellen

Der ernsteste Fund des Tages, und er kam auf einem Umweg: **Das Werkzeug, das schwache
Tests sucht, hat zuerst sich selbst gemeldet.**

### Wie er auffiel

Nach dem `NOTICE`-Wächter meldete `tools/vakuumprobe.py` plötzlich *«Treffer: keine — jede
geprüfte Sammlung war gefüllt»*. Von zwölf am Vormittag auf null, ohne dass sich an den
zwölf Stellen etwas geändert hätte.

**Ein Werkzeug, das unvermittelt Entwarnung gibt, ist genau der Fall, gegen den es gebaut
ist.** Nachgesehen statt geglaubt: Die drei neuen Wächter dieses Tages lesen Dateien im
Repo — `README.md`, `NOTICE`, `auftraege/offen/`. Keine davon stand in `MITKOPIEREN`, der
Liste dessen, was in die Arbeitskopie mitgeht. Dort scheiterte darum schon das
**Einsammeln** — und ein Sammelfehler erzeugt keine `FAILED`-Zeile. Beide Läufe hatten
null rote Tests, ihre Differenz war leer, und daraus wurde *«in Ordnung»*.

*Aus **nicht gemessen** wurde **bestanden**, im Werkzeug, dessen einziger Zweck es ist,
genau diesen Schluss zu verhindern.* Es meldet jetzt `PROBE NICHT DURCHGEFUEHRT` und gibt
den Rückgabewert **2** — ein Treffer ist ein Befund (1), eine nicht durchgeführte Probe ist
gar keiner.

Erkannt wird der Sammelfehler am **Symptom** und nicht an einer Mindestzahl. Der erste
Entwurf verlangte 900 gelaufene Tests und fällte prompt die **eigenen Tests des
Werkzeugs**, die absichtlich winzige Suiten bauen. *Eine Schranke, die den Normalfall
trifft, ist keine.* Jetzt zählen `ERROR`-Zeilen und pytest-Rückgabewert 2 — beides von
einem gewöhnlichen roten Test unterscheidbar und unabhängig von der Grösse der Suite.

### Was dabei zum Vorschein kam

Weil `auftraege/` nun mitkopiert wird, lief `tests/test_regel3_kennungen.py` zum ersten Mal
über einen grösseren Dateibestand. **Er fand einen wirklichen Benutzernamen.** Nachgezählt
im eingecheckten Stand: **fünf Vorkommen in drei Dateien.**

| Datei | Form | warum der Wächter blind war |
|---|---|---|
| `auftraege/ergebnisse/auf-20260818-03.json`, `-04.json` | `e/<name>/ai-imaging-in-a-box/…` (2×) | Der Pfad war beim Einfangen eines Tracebacks **abgeschnitten**. Die Suche verlangt ein vollständiges `/home/` und sah nichts |
| `betrieb/kosmo-abholer.service` | `/home/<name>/…` (3×), vollständig | Die Datei wurde **nie gelesen**: Der Wächter führte eine Liste erlaubter Endungen, und `.service` stand nicht darin |

Regel 3 nennt das ausdrücklich, und der Docstring des Wächters auch: *«Ein Fund hier ist
kein Stilfehler: Es ist ein Name in einem öffentlichen Repo.»* Er stand dort seit dem
18.08. beziehungsweise 19.08.2026.

**Alle fünf sind geschwärzt.** In der `.service`-Datei wird daraus sogar eine Verbesserung:
Sie ist jetzt eine Vorlage mit `<nutzer>` und einem Hinweis obenan, statt einen einzelnen
Rechner festzuschreiben.

### Zwei Lehren, und die zweite ist die grössere

**Erstens: eine Erlaubnisliste vergisst das Nächste, was dazukommt.** Der Wächter zählte
acht Endungen auf, die gelesen werden sollten. Als eine neunte Dateiart ins Repo kam, war
sie unbewacht — ohne dass jemand etwas falsch gemacht hätte. Die Liste steht jetzt
**andersherum**: Sie nennt, was *nicht* gelesen wird (Binärformate, die keinen Namen tragen
können), und im Zweifel wird gelesen. *Eine Verbotsliste vergisst nichts.*

**Zweitens: der Wächter hatte zwei Löcher, und beide waren nur von aussen sichtbar.** Er
hat sich nicht selbst gemeldet — gefunden hat ihn ein anderes Werkzeug, das seinerseits nur
auffiel, weil es eine Zahl nannte, die sich unerklärt geändert hatte. **Der einzige Grund,
warum das aufgefallen ist: Die zwölf Treffer standen im Protokoll des Vormittags.**

*Eine Kennzahl, die man mitführt, ist mehr wert als eine, die man einmal misst — nicht weil
sie stimmt, sondern weil ihre Änderung eine Frage aufwirft.*

---

## 8k · Das Blatt, nach dem jemand anders baut

`docs/EINBAU_CLOUDWORKER_2026-08-22.md` ist das einzige Dokument dieses Repos, nach dem
**eine andere Seite arbeitet**. Es war vier Tage alt, und in diesen vier Tagen ist genau
das eingetreten, wogegen es geschrieben wurde: **Der Stand hat sich geändert, das Blatt
nicht.**

### Was es falsch gesagt hätte

| | |
|---|---|
| **`sun` und `skip`** standen als wirkungslos darauf | beide werden seit heute bedient. Wer danach gebaut hätte, hätte an einer geschlossenen Lücke gearbeitet |
| **`images` hat ein Bild je Kamera** — so las es sich | seit heute gibt es **drei** Gründe, aus denen eine Kamera kein Bild erzeugt, und zwar bei `status: ok`: Rahmung, Kamerahöhe, doppelte Ansicht |

Der zweite Punkt ist der, der ihre Oberfläche wirklich trifft: **Eine Oberfläche, die Bild
und Kamera paarweise nebeneinanderstellt, zeigt ab dem ersten übersprungenen Lauf die
falsche Beschriftung.** Bei der doppelten Ansicht existiert das Bild sogar — es ist
dasselbe wie das seines Zwillings und steht darum einmal in `images` und nicht zweimal.

Der Nachtrag steht **hinter** dem alten Text und ersetzt ihn nicht. Was oben steht, gilt
weiter, wo es nicht ausdrücklich widerrufen ist — und der wichtigste Punkt von damals gilt
unverändert: *Die guten QA-Zahlen kommen weiterhin nicht an.*

### Und der Wächter dazu, der sofort etwas fand

Die Liste der wirkungslosen Felder steht **maschinenlesbar im Code**
(`kosmo_szene.STEHENGEBLIEBEN`). `tests/test_uebergabe.py` hält beide gegeneinander —
**genau**, in beide Richtungen. Wer ein Feld anschliesst und das Blatt nicht nachzieht,
bekommt es rot.

Damit die Zuordnung nicht geraten werden muss, trägt jeder Tabelleneintrag jetzt auch
seinen **fremden** Namen, und eine Probe hält ihn gegen den Lesecode: Ein hübscher Name in
der Tabelle, den der Vertrag nicht kennt, fällt auf.

**Beim ersten Lauf hat der Wächter meine eigene Ungenauigkeit gefangen:** Ich hatte
`style_mode` und `style_refs` aufs Blatt geschrieben; im Vertrag ist `style` ein
verschachtelter Block, es heisst `style.mode` und `style.refs`.

### Die Mutationsprobe hat den Wächter zweimal zurückgeschickt

Der erste Entwurf bestand **eine von vier** Mutationen, und die drei Fehlschläge sind
lehrreicher als der fertige Test:

* **Er sammelte jeden Backtick-Namen im Abschnitt ein**, auch die aus dem erklärenden
  Absatz darunter. Eine Zeile aus der Tabelle zu entfernen liess ihn grün, weil der Name
  nebenan im Fliesstext weiterstand. *Ein Wächter, der eine Liste an ihrer Erwähnung statt
  an ihrem Eintrag prüft, prüft die Erwähnung.*
* **Eine Zusicherung war vakuumwahr.** Sie verglich gegen die fremden Namen der
  durchgereichten Felder — und `DURCHGEREICHT` trägt gar keine. Die Schnittmenge war immer
  leer, der Test immer grün. *Genau die Gestalt, die `tools/vakuumprobe.py` sucht, von
  Hand hineingebaut.*
* **Er prüfte «mindestens» statt «genau».** Ein Feld, das niemand mehr ignoriert, hätte
  einfach zusätzlich auf dem Blatt stehen dürfen — also genau der Fall `sun`/`skip`, gegen
  den der Test geschrieben war.

*Dreimal an einem Tag hat die Mutationsprobe einen Wächter widerlegt, der grün war.* Beim
`NOTICE` war es ein Konjunktiv, hier eine Erwähnung und eine leere Schnittmenge.

---

## 8l · Ein Riegel von heute früh sprach über einen anderen Lauf

Beim Durchsehen der neun offenen Aufträge — *hindert einer der acht neuen Riegel eine
Messung, die schon unterwegs ist?* — kamen zwei Befunde heraus, und beide betreffen Code,
der an diesem Vormittag entstanden ist.

### Der erste: eine Zahl an der falschen Bedingung

`_rahmung_vor_dem_render` rief `kameras.rahmungsverhaeltnis(...)` **ohne Deckungsgrad**,
benutzte also die Konstante der Bibliothek — und schrieb sie auch so ins Urteil:
`deckungsgrad: 0.70`, gleichgültig womit der Lauf wirklich gestellt worden war.

*Das ist ein Verstoss gegen den Satz, unter dem dieser ganze Tag steht.* Und der Fall, an
dem es teuer geworden wäre, lag als Auftrag bereit: **`auf-20260825-41` vergleicht 0,55
gegen 0,70.** Der Riegel hätte über beide Arme dasselbe gesagt.

Nachgesehen, warum er es überhaupt konnte: **Der Multipass-Bericht trug den Deckungsgrad
gar nicht.** Der Runner nimmt ihn als Schalter entgegen und benutzt ihn — und schrieb ihn
nirgends hin. Ein Bericht ohne die Bedingung seiner eigenen Messung.

Beides behoben: Der Runner meldet das Feld, der Riegel rechnet damit, und wo es fehlt
(jeder ältere Bericht) steht `deckungsgrad_quelle: "vorgabe"` und der Abbruchgrund sagt
dazu, dass die Zahl **angenommen** ist. Abgebrochen wird trotzdem — den Riegel für jeden
Altbestandslauf abzuschalten hiesse, ihn abzuschaffen.

### Der zweite: der Riegel hätte eine absichtliche Messung verhindert

`auf-41` Posten **G2** will Bildpaare bei 0,55 und 0,70, um zu sehen, **was die engere
Rahmung gestalterisch kostet**. Bei einem Bauwerk, das die Szene füllt, ist die wirksame
Bildbreite gleich dem Deckungsgrad — **0,55 liegt unter der Abbruchschwelle 0,65.** Der
halbe Vergleich hätte statt eines Bildes einen Abbruch geliefert.

*Ein Riegel, der eine absichtliche Vergleichsmessung verhindert, steht an dieser Stelle
falsch.* Er ist darum abbestellbar: `verarbeiter(..., rahmung_pruefen=False)`. Er rechnet
dann weiter und schreibt sein Urteil, bricht aber nicht ab — und das Ergebnis trägt
`rahmung.abgeschaltet = True`.

**Auf dieses eine Feld kommt es an.** Ohne es hätten ein geprüfter und ein ungeprüfter Lauf
im Ergebnis gleich ausgesehen — *ein abgeschalteter Riegel, dessen Ergebnis aussieht wie
ein bestandener, ist schlimmer als gar keiner: Er erzeugt Vertrauen, das niemand geprüft
hat.* Ein eigener Test hält die beiden gegeneinander.

### Und der Auftrag bekommt einen Nachtrag, keine Neufassung

`auf-41` trägt jetzt ein Feld `nachtrag` mit dem, was sich seit dem 25.08. an der Kette
geändert hat, und mit der Abhilfe für G2. **Die Frage des Auftrags ist unverändert** —
Entscheid 36 von heute Mittag gilt: Ein laufender Auftrag, den man umformatiert, ändert die
Frage, während jemand daran arbeitet. Eine Warnung, ohne die er ins Leere liefe, ist etwas
anderes als eine Umformulierung.

Die übrigen acht offenen Aufträge wurden auf dieselbe Frage geprüft. **Nur `auf-41` ist
betroffen:** `auf-38` und `auf-42` rendern auf der synthetischen Geometrie mit der Vorgabe
0,70, die drei `qa`-Aufträge rechnen kein Bild, und die beiden `multipass`-Aufträge laufen
gar nicht erst durch die Diffusion.

---

## 8m · Zwei Fragen standen nur in einer Auftragsdatei

Dasselbe noch einmal, eine Ebene weiter: `docs/UEBERGABE_VIS_2026-08-19.md` ist die
**ausführliche** Fassung der offenen Fragen an KosmoOrbit, und das Kurzblatt verweist
ausdrücklich darauf. Es führte **vierzehn**.

Die beiden Vertragsfragen von heute — **ab welcher Richtung `render.sun.azimuth` zählt**
und **was `render.faithful` steuern soll** — standen in `auf-20260826-44` und **sonst
nirgends**.

*Ein Auftrag ist die Anweisung an einen Worker, kein Ersatz für die Übergabe.* Die andere
Seite liest das Übergabeblatt, nicht unser Auftragsverzeichnis. Beide Fragen sind
nachgetragen, mit ihrem Inhalt und nicht als Verweis — die Hausregel gilt für Dokumente
wie für Aufträge.

Bei der Sonne gehört ein Nebenbefund dazu, weil er alte Läufe betrifft: Die feste Sonne
stand als `rotation_euler = (radians(50), 0, radians(35))` mit dem Kommentar *«50° Höhe
und 35° Azimut»*. **Beide Zahlen im Kommentar sind falsch** — gerechnet stand sie auf 40°
über dem Horizont und 35° **östlich** von Süden. Wer Bilder von vor dem 26.08. vergleicht,
muss das wissen.

### Und wieder eine Zahl in einem Fliesstext

Das Kurzblatt sagte «inzwischen 14 Stück». Mit dem Nachtrag sind es 16, und der Satz wäre
still falsch geworden — *dieselbe Fehlerart wie die Testzahl im README, nur zwischen zwei
Dokumenten*. Der Wächter hält jetzt beide Zahlen gegeneinander und prüft zusätzlich, dass
die Nummerierung lückenlos ist: Wer auf «Frage 13» verweist, muss dieselbe Frage meinen
wie der, der sie beantwortet.

Beim Bauen fiel auf, dass eine naive Zählung **17** ergibt statt 14: Das Dokument beginnt
mit drei gleich nummerierten Fragen («Was schicke ich euch?»), die seine **Gliederung**
sind und keine offenen Punkte. Der Wächter zählt darum erst ab der Überschrift der Liste.
*Eine Zahl, die man aus dem falschen Abschnitt liest, ist schlimmer als keine — sie sieht
gemessen aus.*

---

## 8n · Drei Kameraparameter, die der Runner kennt und die Naht nie setzte

Die Zählung von heute Mittag lief in **eine** Richtung: Was nimmt `seams.glb_zu_multipass`
entgegen, und was davon reicht der Abholer durch? Achtzehn Einstellungen, zwölf
durchgereicht, sechs benannt.

Die Gegenrichtung war nie gezählt: **Was kennt der Runner, das die Naht gar nicht kennt?**
23 argparse-Schalter gegen den Text von `seams.py` ergaben drei:

`--deckungsgrad`, `--augenhoehe`, `--bias`.

*Eine Tabelle über die Durchreichung kann eine Einstellung nicht vermissen, die es an der
Naht nicht gibt.* Die sechs benannten Lücken vom Mittag waren vollständig — für die
achtzehn, die es gab.

### Warum das nicht bloss unbequem ist

* **`--deckungsgrad`** ist der Wert, den `auf-20260825-41` in Posten G2 vergleicht: 0,55
  gegen 0,70, an Bildpaaren. **Diese Messung war über den Produktivweg gar nicht
  durchführbar** — man hätte den Runner selbst aufrufen müssen, unter Umgehung aller acht
  Riegel von heute.
* **`--augenhoehe`** ist genau die Grösse, um die **Frage 7 des Übergabeblatts** mit
  KosmoOrbit kreist: 1,30 / 1,60 / 1,70 m, und ab wo gemessen. *Über einen Wert zu
  verhandeln, den die eigene Naht nicht einstellen kann, ist müssig.*
* **`--bias`** stellt den Winkel der Über-Eck-Ansicht (35°) — dieselbe Ansicht, deren
  Verdopplung heute früh die doppelte Soll-Karte erzeugte.

Alle drei reichen jetzt durch, von `verarbeiter` bis zum Runner, und stehen in der
Durchreichungstabelle. Aus 18 Einstellungen wurden 21, aus 12 durchgereichten 15. `None`
heisst wie überall *nicht angefasst* — ein mitgeschickter Vorgabewert wäre im Bericht von
einer Bestellung nicht mehr zu unterscheiden.

### Und ein Auseinanderlaufen, gegen das der Code ausdrücklich gebaut war

Beim Anschliessen fiel auf: **`baue_kommando_multipass` — die Trockenlauf-Fassung — kannte
`kamera_huellbox` gar nicht.** Der Trockenlauf zeigte also ein Kommando, das der echte Lauf
so nie startete.

Der Docstring von `_multipass_argumente` benennt genau diese Gefahr: *«Wären sie zweimal
geschrieben, könnten `glb_zu_multipass` und `baue_kommando_multipass` auseinanderlaufen —
und dann prüfte der Test ein Kommando, das so nie gestartet wird.»*

**Der Helfer war geteilt, die Aufrufe waren es nicht.** Ein gemeinsamer Helfer schützt
nicht davor, ihn verschieden aufzurufen — und die Signaturen der beiden öffentlichen
Funktionen waren nirgends gegeneinander gehalten. Jetzt sind sie es, mitsamt einer
Gegenprobe, die beide mit denselben Angaben aufruft und die Kommandos vergleicht. Die
Ausnahmeliste (`timeout`, `starte`, `_starte`, `stillstand_frist_s`) trägt für jeden
Eintrag den Grund — *eine Ausnahme ohne Begründung ist ein Loch mit Namen.*

---

## 8o · Und eine Behauptung, die ich selbst eine Stunde zuvor geschrieben hatte

Die nachgetragene **Frage 16** des Übergabeblatts sagte: *«Das Feld `render.faithful`
steht in eurem Vertrag, wir reichen es durch und lesen es nicht.»*

**Das ist falsch, und zwar seit langem.** `kosmo_szene.py:390` liest es und bildet es auf
`controlnet_staerke` ab, und der Abholer meldet bei **jedem** Auftrag dazu, was dabei
nicht abgebildet wird. Der Auftrag `auf-20260826-44`, in dem dieselbe Frage steht, sagt es
sogar richtig — ich hatte beim Nachtragen aus dem Gedächtnis geschrieben statt aus dem
Code.

*Eine Stunde von der Behauptung bis zur Widerlegung, und beide sind meine.* Die Frage ist
jetzt berichtigt und dabei **besser** geworden: `faithful` wird abgebildet, aber auf
**eine** von drei Grössen, die die Treue bestimmen — und die Wirkung ist nachweislich
nicht monoton (0,80 schlägt 1,00, `auf-20260818-13`). Das ist eine schärfere Frage als
«ihr Feld läuft ins Leere».

### Die Nachbarfrage stimmte — war aber irreführend

Frage 8 sagt, `engine` und `style` blieben ungelesen. Für `engine` stimmt es (der Runner
rendert immer mit Cycles — weil er so gebaut ist, nicht weil der Laufzettel es sagt).

**Bei `style` gibt es aber zwei Felder dieses Namens**, und nur eines bleibt ungelesen:

* `style: "lineart"` im **Laufzettel** der Brücke — ungelesen.
* `style: {mode, refs}` im **Szenenvertrag** — wird gelesen und steht als *wirkungslos*
  in der Tabelle der bestellbaren Felder.

Die andere Seite hätte daraus schliessen können, wir sähen ihren Stilblock gar nicht an.
*Zwei gleich benannte Felder mit verschiedenem Schicksal sind eine Verwechslung, die man
besser im Übergabeblatt auflöst als in einem Ergebnis.*

### Was daraus folgt, und was nicht

**Kein Wächter.** Ob ein Satz über den Code noch stimmt, ist nicht maschinell entscheidbar,
und die Sondierung von heute früh hat gezeigt, wohin ein Suchwerkzeug dafür führt: ein
Treffer auf fünf Fehlalarme.

Was folgt, ist eine Arbeitsregel, und sie ist an diesem Tag zweimal bezahlt worden:
**Wer eine Aussage über den Code in ein Dokument schreibt, sieht vorher in den Code** —
auch und gerade dann, wenn man ihn selbst geschrieben hat.

---

## 8p · Blender läuft in diesem Container — und das ändert etwas

Beim Nachsehen, ob sich der Multipass zwischenspeichern liesse, fiel etwas auf, das den
ganzen Tag hindurch falsch angenommen war: **`/opt/blender/blender` liegt hier.** Die
Geometrieseite dieser Kette ist in diesem Container messbar; nur die Diffusion braucht die
GPU der HomeStation.

*«Dieses Environment hat keine GPU» hiess in meinem Kopf «hier lässt sich nichts messen».
Das eine folgt aus dem anderen nicht.*

### Was daraufhin gemessen wurde, und was dabei herauskam

Zwei Fähigkeiten dieses Tages waren gründlich geprüft — **die Sonne mit 34 Tests reiner
Arithmetik, der Deckungsgrad an der Naht** — und keine von beiden war je durch Blender
gelaufen. Dasselbe Muster, das dieser Tag achtmal gefunden hat, in meiner eigenen Arbeit
von heute früh.

**Die Sonne trägt.** Ein Lauf mit `elevation: 8, azimuth: 75` ergibt im Bericht genau diese
Zahlen, `bestellt: ["hoehe", "azimut"]` und einen anderen Eulerwinkel als die Vorgabe.

**Der Deckungsgrad erreicht den Bericht — und änderte nichts.** Vier Läufe mit 0,20 / 0,55
/ 0,70 / 0,95 ergaben **denselben Kameraabstand** (14,0014 m) und **denselben Füllgrad**
(0,1944). Im Bericht stand jedes Mal `massgebend: "untergrenze"`.

### Der Befund dahinter, und er trifft den Riegel von heute früh

Nachgerechnet über Kantenlängen von 4 bis 100 m:

| Bauwerkskante | massgebend | Füllgrad bei Soll 0,70 |
|---:|---|---:|
| 4 m | **untergrenze** | **0,553** |
| 8 m | breite | 0,699 |
| 12–100 m | breite/höhe | 0,700 |

**Ab etwa 8 m hält die Kamerarechnung den Deckungsgrad exakt ein. Darunter übernimmt der
Mindestabstand.** Der Deckungsgrad ist eine *Vorgabe* und kein *Ergebnis*.

Mein Rahmungsriegel rechnete mit dem Sollwert: `wirksam = deckungsgrad × anteil`. Für ein
kleines Bauwerk nimmt er damit **0,700 an, wo 0,553 gemessen ist** — und 0,553 liegt unter
dem Knie 0,5991. *Ein Pavillon wäre durchgegangen, dessen Rahmung nachweislich nicht
trägt.*

Der Riegel liest jetzt den **gemessenen** Füllgrad, wenn der Bericht ihn trägt, und sagt
im Urteil, worauf er sich stützt (`grundlage`, `basis`, `massgebend`). Am echten
Blender-Bericht der Testgeometrie: **19,4 % statt der angenommenen 70 %** — der Lauf wird
jetzt abgebrochen, und die Messung der HomeStation sagt, dass er bei 17,5 % einen Score von
0,0002 ergeben hätte.

### Die Gegenprobe, die vor dem Einchecken nötig war

*Bricht der schärfere Riegel jetzt die eigenen Aufträge ab?* Gemessen an der Geometrie aus
`tools/make_test_ifc.py` (8 × 5 × 3,3 m): Füllgrad **0,700**, `massgebend: "breite"`, kein
Abbruch. Die offenen Aufträge, die auf dieser Geometrie laufen — `auf-38`, `auf-42` —, sind
nicht betroffen. *Die schärfere Prüfung trifft genau den Fall, für den sie gedacht ist.*

Fünf neue Tests fahren beides ab jetzt **durch Blender**, nicht nur an der Naht.

---

## 8q · Der Riegel des Tages war auf seinem eigenen Anlassfall blind

**Der schwerste Befund dieser Sitzung**, und er wurde erst möglich, weil Blender in diesem
Container liegt (8p). Die Kette wurde zum ersten Mal über eine Geometrie **mit Gelände**
gefahren — `tools/make_test_ifc.py --gelaende`, eine Platte in 2,5-facher Gebäudespanne.

### Was herauskam

| | vorher | richtig |
|---|---:|---:|
| Szenenbox | 20 × 20 m | 20 × 20 m |
| **Bauwerksbox** | **20 × 20 m** | 8 × 5 m |
| Breitenanteil | **1,00** | 0,40 |
| wirksame Bildbreite | **0,70** | 0,28 |
| Abbruch | **nein** | ja |

Das Bauwerk füllt **28 % der Bildbreite**. Die HomeStation hat für 30 % einen Score von
**0,0** gemessen (`auf-vis-20260825-15`, Posten 1). *Der Riegel hätte diesen Lauf
durchgewinkt — und er ist genau für diesen Fall gebaut.* Der Docstring der Bauwerksbox
nennt ihn wörtlich: *«Der grösste gemessene Fehler dieser Woche: Die Kamera rahmt die
Hüllbox der GANZEN SZENE, die Maske deckt nur das Bauwerk.»*

### Die Ursache: zwei Filter, beide blind, jeder aus einem anderen Grund

Die Geländeplatte ist ein **`IfcSlab` mit dem Namen `Gelaende`**. Und:

* **Auf der IFC-Seite** filtert der Runner nach **Typ** — `GELAENDE_TYPEN = ("IfcSite",)`.
  Die `IfcSite` unserer Testdatei trägt gar keine Geometrie; die sichtbare Platte ist ein
  Slab. Der Typfilter fasst sie also nicht.
* **Auf der Blender-Seite** entscheidet `maske.ist_gelaende` über den **Objektnamen** —
  weil nach dem glb-Export kein IFC-Typ mehr dasteht. Der Knotenname lautete aber
  `IfcSlab_<GlobalId>`. **Der IFC-Name war weg.**

Nachgelesen im rohen glTF-Block der Datei: Die Knoten hiessen
`IfcSlab_2eYuY4S81HqRN8GZ4SZVcP` — Typ und Kennung, kein Name. *Der Export warf die eine
Auskunft weg, auf die die nächste Stufe angewiesen ist.*

### Die Behebung, und warum sie an dieser Stelle sitzt

Der Knotenname trägt jetzt den IFC-Namen mit: `IfcSlab_Gelaende_2eYuY4S8…`. Eine Zeile im
IFC-Runner, und die Namensregel greift wieder — sie war nie falsch, sie bekam nur nichts
zu lesen.

Der Name steht **vor** der GlobalId, und das ist kein Geschmack: **Blender kürzt
Objektnamen bei 63 Byte.** Stünde er hinten, fiele er bei langen Namen genau wieder weg.
Was überläuft, ist die Kennung — die braucht auf der Blender-Seite niemand.

Gegenprobe in die andere Richtung, damit der Anschluss kein neues Loch reisst: `Gelaender`,
`Bodenplatte` und `Gelaendemodell` gelten weiterhin **nicht** als Gelände. Die Wortgrenzen
von `ist_gelaende` tragen auch in der neuen Form.

### Die Nebenwirkung, und sie ist keine Kleinigkeit

Dieselbe Regel baut die **Bauwerksmaske**. Gemessen: Vor der Änderung galten alle sechs
Objekte als Bauwerk, danach fünf — die Geländeplatte fällt heraus.

**Damit sind Geometrie-QA-Zahlen aus Läufen über unsere IFC-Kette vor dem 26.08.2026 mit
späteren nicht vergleichbar**, sofern die Szene benanntes Gelände enthielt: Die Maske
deckte es mit ab. Das gehört in jeden Vergleich, der über diesen Tag hinweggeht.

### Was offen bleibt, und es wird nicht stillschweigend mitrepariert

Die **IFC-seitige** `bbox_bauwerk` filtert weiterhin nur nach Typ und meldet für diese
Datei unverändert 20 × 20 m. Die Regel dort auch auf Namen auszuweiten hiesse, dieselbe
Regel an einer **dritten** Stelle zu führen — und eine Zahl, die an zwei Stellen steht, ist
an einer davon bereits falsch. Die Kette benutzt die Blender-seitige Box; die IFC-seitige
ist damit eine zweite Meinung, die auseinanderlaufen kann. *Das ist ein Befund und kein
Versehen* — er gehört auf die Liste, nicht in einen schnellen Zusatzfilter.

### Und warum kein Test das gefunden hat

Die Bauwerksbox war seit dem 25.08. geprüft — an **Attrappen**, in denen die Namen
stimmten. Über die wirkliche Kette IFC → glb → Blender lief keiner.

*Eine Attrappe, die den Fehler nicht kennt, kann ihn nicht finden.* Zwei neue Tests fahren
die ganze Kette ab, mit Geländegeometrie, und prüfen die Bauwerksbox **und** das Urteil des
Riegels.

---

## 8r · Ein Auftrag, den wir gar nicht hätten stellen dürfen

Aus 8p folgt eine Arbeitsfrage, und sie ist unangenehmer als der technische Befund: **Was
haben wir beauftragt, das hier zu erledigen gewesen wäre?**

Nachgesehen an `auf-20260826-45`, Posten **M0**: *«Die Gegenprobe zuerst, an synthetischer
Geometrie — fahrt den Torwächter auf `make_test_ifc.py` und auf derselben Box mal 1000 und
geteilt durch 1000.»* Das braucht keine GPU. Es braucht `.venv-ifc`, und das liegt hier.

**Hier gerechnet, über die echte Kette IFC → glb:**

| Fall | grösste Kante | Entscheidung | `verdacht_faktor` |
|---|---:|---|---:|
| wie erzeugt | 8,0 m | `annehmen` | — |
| mal 1000 | 8000,0 m | `ablehnen_massstab` | 1000,0 |
| geteilt 1000 | 0,008 m | `ablehnen_massstab` | 0,001 |

Der Torwächter ist nicht kaputt. Was der HomeStation bleibt, ist **M1 bis M4: die
Häufigkeit am echten Bestand** — und genau das kann hier niemand messen, weil echte Dateien
nach Regel 3 nicht in diesem Repo liegen.

*Das ist der ganze Sinn der Zweiteilung, und M0 hätte nie in einen Auftrag gehört.* Er
steht jetzt als Test da und ist dem Auftrag als erledigt vermerkt, damit dort keine
GPU-Stunde für eine beantwortete Frage draufgeht.

### Die Regel, die daraus folgt

Bisher hiess die Faustregel: *misst es, rendert es, prüft es → HomeStation.* Sie war zu
grob. Genauer:

> **Alles Geometrische wird hier gemessen. Zur HomeStation geht, was `torch` braucht —
> Diffusion und Tiefenschätzer — oder was ohne ihre echten Dateien nicht zu haben ist.**

Nachgesehen, damit die Regel nicht auf einer zweiten Vermutung steht: `torch`,
`diffusers`, `transformers` und `scipy` fehlen hier; `numpy` und Pillow sind da. Die
Geometrie-Treue-Zahl selbst braucht den Schätzer und bleibt darum drüben — die
**Rahmung**, die **Hüllboxen**, die **Kamerastellung** und der **Massstab** nicht.

Das steht seit heute auch im README, weil die Kurzform *«dieses Environment hat keine
GPU»* genau zu der falschen Folgerung verleitet, die diesen Tag über gegolten hat.

---

## 8s · Ein ganzer Auftrag, hier beantwortet

Nach M0 (8r) die naheliegende Frage: **Gilt das für einen ganzen Auftrag?**

`auf-20260824-39` — *«Stimmt die umgewandelte Geometrie, oder ist sie bloss
durchgelaufen?»* — trägt vier Posten, und **alle vier brauchen nur synthetische Geometrie
und den IFC-Runner.** Kein Bildmodell, keine echten Dateien, keine GPU. Er liegt seit dem
24.08. bei der HomeStation.

Hier gerechnet, in **zwei Sekunden**:

| Posten | Ergebnis |
|---|---|
| **G1** der gerade Fall | 8,0 × 5,0 × 3,25 m — `stimmt`, keine Abweichung, 5 Bauteile, 60 Dreiecke |
| **G2** der Millimeterfall | **dieselben Meter.** Der Runner liest den `IfcSIUnit`-Vorsatz MILLI und rechnet selbst um |
| **G3** Gegenprobe am Werkzeug | alle drei Fälschungen gefangen — «MASSSTAB: Faktor 1000», «ACHSEN VERTAUSCHT», und das fehlende Bauteil als Abweichung |
| **G4** mit Räumen | Hüllbox unverändert, `n_uebersprungen == 2` (`IfcSpace`) |

G2 bestätigt dabei eine Annahme, die dieses Projekt schon einmal teuer korrigiert hat:
*ArchiCAD über IFC4 braucht keine Einheitenumrechnung.* Wer hier von Hand mal 1000 nähme,
träfe genau daneben.

Das Ergebnis liegt als `auftraege/ergebnisse/auf-20260824-39.json`, und es sagt in seinem
eigenen Feld `gerechnet_von`, dass **nicht** die HomeStation es gerechnet hat — ein
Ergebnis, dem man das nicht ansieht, wäre eine stille Falschaussage über seine Herkunft.

**Was offen bleibt, ist die Kernfrage:** *«Kein Mass vergleicht die 27 000 Dreiecke mit
dem, was in der IFC steht.»* Das betrifft **echte** Dateien. Was jetzt vorliegt, ist ein
**geprüftes Werkzeug** dafür — kein Ergebnis darüber. Und die Grenzen stehen im
Ergebnis: Eine synthetische Datei kennt keine Bogenkanten, keine `IfcOpeningElement`, keine
verschachtelten Aggregationen — und nur *einen* Erzeuger, unseren eigenen. Der häufigste
reale Fehlerfall, ein Exporter mit eigener Achskonvention, kommt darin gar nicht vor.

---

## 9 · Die Entscheide dieses Tages — zur Vorlage an den Owner

Vierundsechzig Entscheide, alle unter der Freigabe vom Morgen getroffen, alle rückgängig
zu machen. Die Nummern 24 bis 64 sind vom Nachmittag.

| # | Entscheid | Warum | Rückgängig |
|---|---|---|---|
| 1 | `BILDBREITE_ABBRUCH = 0.65`, kein Rendern darunter | kleinste gemessene Stützstelle, an der etwas besteht; bei 30 % ist das Ergebnis *schlechter* als bei 17,5 % | eine Konstante |
| 2 | Im uneinigen Band 0,50–0,65 gilt die **vorsichtigere** Messung | zwei Messungen widersprechen sich; wo eine 0.001 sagt, ist ein Lauf GPU-Zeit gegen ein unverteidigbares Ergebnis | dieselbe Konstante |
| 3 | `skip: true` → kein Bild, **aber** eine Antwort mit Grund | gar nichts zurückzugeben liesse die bestellende Seite hängen; sie kann *übersprungen* nicht von *abgestürzt* unterscheiden | ein `if`-Block |
| 4 | Abbruch nur bei **benannter** Bedingung, nicht bei jeder Ausnahme des Regelwerks | `KompositionError` trägt auch Eingabefehler; sonst würde aus *nicht prüfbar* ein *durchgefallen* | eine Funktion |
| 5 | Entflechtung **nur** vor dem Auslagern | 1,35 GiB Zuschlag könnten einen gesunden Lauf erst in die Auslagerung drängen | zwei Zeilen |
| 6 | Doppelansicht an der **Soll-Karte**, nicht an der Hüllbox | die Hüllbox ist immer zweizählig symmetrisch → falscher Alarm bei jedem echten Bauwerk | eine Funktion |
| 7 | Doppelung zählt **nicht** als zweite Ziehung | ein Minimum fällt mit der Zahl der Ziehungen; eine Wiederholung ist keine | ein Filter |
| 8 | Drei Dauerwarnungen nach `vertragsvorgaben`, Deckel `[:3]` entfernt | sie füllten alle drei Plätze und verdeckten jede echte Warnung | ein Feld |
| 9 | Bildmasse-Hinweis je nachdem, ob **gewählt** wurde | ein Beschnitt aus eigener Bestellung ist etwas anderes als die Vorgabe des fremden Vertrags | eine Bedingung |
| 10 | Wache **eine** Ebene tief, nicht `rglob` | eine Wache, die mit dem beobachteten Ordner langsamer wird, wird seltener befragt | ein Vorgabewert |
| 11 | Erreichbarkeit nur bei geltender Obergrenze | bei drei Hintergrundstrategien ist sie gemessen **keine** Schranke | ein `return None` |
| 12 | Ihre Zahl 0.6909 **nicht** übernommen, sondern gefragt | nachgerechnet ergibt sie 0.8304 und nicht «gerade eben durch» | — es wurde nichts geändert |
| 13 | Fehlende Modellwurzel → **abgelehnt**, geprüft erst unmittelbar vor dem Laden | nichts geladen, nichts gerechnet; in `pruefe_auftrag` hätte die Prüfung die halbe Testsuite stillgelegt | eine Bedingung |
| 14 | Acht zusammengesetzte Wörter ins Glossar, Meldung bleibt | die Lücke schliessen, **nicht** die Meldung abschaffen — sonst fiele der nächste fehlende Begriff nirgends mehr auf | acht Zeilen |
| 15 | Zwischenbilder werden **vor** dem Render auf Vollständigkeit geprüft (CRC je Block) | aus einem Fehlschlag der Diffusion wird ein Befund über eine Datei; die GPU wird nicht angefasst | eine Bedingung |
| 16 | Test-Attrappen schreiben ein **gültiges** Minimal-PNG statt acht Signaturbyte | eine Attrappe, die eine Datei vortäuscht, die es so nie gibt, prüft die Kette gegen eine Welt, in der sie nicht läuft | `MINI_PNG` in `conftest.py` |
| 17 | Azimut-Konvention **von Süden** — als benannte Annahme, nicht als Wahrheit | die alte feste Sonne war so gemeint; von Norden hätte jede Messung dieser Woche unvergleichbar gemacht | eine Konstante |
| 18 | Negative Sonnenhöhe **zugelassen** | Nacht ist eine gültige Bestellung; sie stillschweigend auf null zu heben lieferte etwas anderes als bestellt | eine Bedingung |
| 19 | `tools/tote_kanten.py` **meldet**, es prüft nicht | Studienmodule sind zu Recht ungerufen; ein Test darauf wären dutzende Fehlalarme, und der erste schaltet das Werkzeug ab | eine Datei löschen |
| 20 | `fortschritt.beobachte` **geprüft statt gelöscht** | die Regel des Projekts lautet *ungeprüft ist schlimmer als ungerufen*; über Löschen entscheidet nicht, wer sie zufällig findet | drei Tests |
| 21 | `fehlende_ansichten` gibt ein **Urteil statt einer Namensliste** | «nichts fehlt» war falsch: zwei Ansichten teilen sich eine Richtung und unterscheiden sich im Ausschnitt | die alte Signatur |
| 22 | **Kein Werkzeug** aus der Prosa-Sondierung | ein Treffer auf fünf Fehlalarme — daran stirbt ein Suchwerkzeug; der eine Fall ist von Hand berichtigt und bewacht | — nichts gebaut |
| 23 | Ein **Kettenlauf-Test** über alle acht Riegel statt weiterer Bausteintests | acht Prüfungen, die einzeln greifen, sind noch keine Kette; er fand die Lücke sofort | eine Datei |
| 24 | Der Massstabs-Riegel **meldet, bricht nicht ab** | ein benannter Verdachtsfaktor beweist keinen Einheitenfehler: 0,003–1,0 m ergibt Faktor 0,001 (**jeder Bauteilrender**), 3000–10⁶ m ergibt 1000 (oft ein einzelnes verirrtes Mesh) | ein `abbruch`-Feld, sobald `auf-45` die Fehlalarmrate nennt |
| 25 | Er steht **vor** der Kameraschleife | der Massstab ist eine Eigenschaft der Geometrie, nicht der Kamera; in der Schleife hätte er fünfmal ~97 s Blender für dasselbe Urteil bezahlt | eine Einrückung |
| 26 | Bevorzugt `bbox_bauwerk`, Rückfall auf `bbox`, **Quelle steht im Urteil** | an 40 echten Dateien fielen zwei Geländemodelle mit 1002 m und 1127 m durch — Einheit in Ordnung; auf der Szenenbox wäre der Riegel einer gegen grosse Grundstücke | ein Vorrang |
| 27 | Der Eingabebericht wird **gebaut**, nicht durchgereicht | `status` bedeutet im Multipass-Bericht etwas anderes als im Konversionsbericht; der Zweig, an dem es aufgefallen wäre, ist unerreichbar | drei Zeilen |
| 28 | Vier Durchreichungstabellen mit **Absicht oder Lücke** je Feld | «steht nirgends» ist die bequemste dritte Möglichkeit, und genau sie hat am 23.08. zweimal eine Lücke verdeckt | vier Wörterbücher |
| 29 | Der Test hält sie gegen `inspect.signature` | eine handgepflegte Tabelle veraltet — an diesem Tag zweimal nachgewiesen | eine Zusicherung |
| 30 | README-Wächter **nur** auf Zahl, Datei und Funktion | ein Wächter auf Prosa wäre die Sondierung vom Vormittag: ein Treffer auf fünf Fehlalarme, und der erste schaltet ihn ab | eine Datei |
| 31 | Die Testzahl **exakt**, kein Toleranzband | ein Band hält still, solange sich wenig ändert, und schweigt genau dann, wenn sich über Wochen viel geändert hat | ein `abs(a-b) < n` |
| 32 | **Alle 80** toten Kanten bekommen ein Urteil, auch «kein Handlungsbedarf» | eine Liste, in der nur die dringenden markiert sind, sagt über den Rest nichts — und genau im Rest sass der `torwaechter` acht Tage lang | ein Dokument |
| 33 | **Keine** davon wird gelöscht, **keine** kommt in `ABSICHTLICH` | über Löschen entscheidet nicht, wer eine Funktion zufällig ungerufen findet; und eine wachsende Ausnahmeliste heisst weggesehen statt geprüft | — nichts entfernt |
| 34 | Der Wächter prüft die **Auftragsdateien**, nicht die Bibliothek | dort war die Lücke: `baue_auftrag` hat keinen Aufrufer, die Dateien entstehen von Hand | eine Datei |
| 35 | Schranke **1000 Zeichen**, gemessen zwischen 207 (davor) und 2568 (danach) | eine gesetzte Zahl wäre genau das, wogegen dieses Projekt antritt; und die Gegenprobe hält fest, dass sie wirklich trennt | eine Konstante |
| 36 | Die vier alten Aufträge werden **nicht** umgeschrieben | sie sind unbeantwortet unterwegs; ein laufender Auftrag, den man umformatiert, ändert die Frage, während jemand daran arbeitet | — nichts geändert |
| 37 | Der LGPL-Fund an `numpy` wird **gemeldet, nicht behoben** | die Hausregel ist strenger als die LGPL selbst; ob sie auch für eine Komponente gilt, die in einem BSD-3-Wheel mitreist, entscheidet nicht, wer sie findet | — nichts geändert |
| 38 | Jeder Copyleft-Eintrag trägt eine **erklärte** Auflösung statt einer im Fliesstext | der Wortfilter hielt einen **Konjunktiv** für eine Zusage — nachgewiesen an `libgomp`, bevor die Marke kam | sieben Zeilen |
| 39 | `AUFLOESUNG: KEINE` ist **zugelassen** | ein ungelöster Fall soll sichtbar dastehen; ihn zu verbieten hiesse, ihn in einen Absatz zu drängen | ein Wert |
| 40 | …aber nur mit Eintrag in einer kurzen Liste **mit Grund** | sonst wäre die Marke ein Schlupfloch: `KEINE` hinschreiben, grün, Fund verschwunden | eine Liste |
| 41 | Fünf Vorkommen eines wirklichen Namens **sofort geschwärzt**, ohne Rückfrage | ein Name in einem öffentlichen Repo ist kein Stilfehler, und Regel 3 lässt hier keinen Ermessensspielraum | ein `git revert` |
| 42 | Der Endungsfilter des Regel-3-Wächters wird zur **Verbotsliste** | eine Erlaubnisliste vergisst das Nächste, was dazukommt — `.service` war unbewacht, ohne dass jemand etwas falsch gemacht hatte | eine Menge |
| 43 | Zweite Suche am **Repo-Namen** statt am `/home/` | der gefundene Pfad war abgeschnitten; was vor dem Repo-Namen steht, ist das Heimatverzeichnis, und das gilt auch für ein Fragment | eine Regex |
| 44 | Die Vakuumprobe meldet `PROBE NICHT DURCHGEFUEHRT` mit Rückgabewert **2** | aus *nicht gemessen* war *bestanden* geworden — im Werkzeug, dessen Zweck es ist, genau diesen Schluss zu verhindern | ein `raise` |
| 45 | Das Cloudworker-Blatt bekommt einen **Nachtrag**, keine Neufassung | vier Tage alte Aussagen bleiben nachvollziehbar; ein überschriebenes Übergabeblatt macht ältere Absprachen unauffindbar | ein Abschnitt |
| 46 | Jedes stehengebliebene Feld trägt seinen **fremden** Namen im Code | sonst muss der Wächter die Zuordnung raten — und ein geratener Name ist genau die Sorte Zahl ohne Deckung, gegen die dieses Projekt antritt | ein Schlüssel |
| 47 | Der Wächter prüft **genau**, nicht «mindestens» | ein Feld, das niemand mehr ignoriert, hätte sonst einfach zusätzlich auf dem Blatt stehen dürfen — der Fall `sun`/`skip`, gegen den er geschrieben ist | eine Zusicherung |
| 48 | Der Rahmungsriegel rechnet mit dem **gemeldeten** Deckungsgrad, der Runner meldet ihn | eine Zahl gehört an die Bedingung, unter der sie gemessen wurde — `auf-41` vergleicht 0,55 gegen 0,70, und der Riegel sprach über beide Arme gleich | ein Feld |
| 49 | Der Riegel ist **abbestellbar** (`rahmung_pruefen=False`), aber nicht unsichtbar | er hätte `auf-41` G2 verhindert, eine absichtliche Messung dessen, wie eine schlechte Rahmung aussieht; `abgeschaltet: True` unterscheidet den Lauf von einem bestandenen | ein Schalter |
| 50 | `auf-41` bekommt einen **Nachtrag**, die Frage bleibt unangetastet | eine Warnung, ohne die der Auftrag ins Leere liefe, ist etwas anderes als eine Umformulierung (Entscheid 36) | ein Feld |
| 51 | Die zwei jüngsten Vertragsfragen wandern **ins Übergabeblatt**, nicht nur in den Auftrag | die andere Seite liest das Blatt, nicht unser Auftragsverzeichnis — ein Auftrag ist die Anweisung an einen Worker, kein Ersatz für die Übergabe | zwei Absätze |
| 52 | Der Fragenzähler wird bewacht, und die Zählung beginnt **erst bei der Liste** | eine naive Zählung ergibt 17 statt 14, weil die Gliederung mitgezählt würde — eine Zahl aus dem falschen Abschnitt sieht gemessen aus und ist es nicht | eine Konstante |
| 53 | Die drei Kameraparameter reichen **durch**, statt den Riegel nur abzuschalten | `rahmung_pruefen=False` hätte `auf-41` G2 laufen lassen — aber mit dem falschen Deckungsgrad. Ein Riegel weniger ersetzt keinen fehlenden Hebel | drei Parameter |
| 54 | Die Signaturen von echtem Lauf und Trockenlauf werden gegeneinander gehalten | der gemeinsame Helfer schützt nicht davor, ihn verschieden aufzurufen — `kamera_huellbox` fehlte im Trockenlauf | eine Zusicherung |
| 55 | Gezählt wird ab jetzt in **beide** Richtungen | eine Durchreichungstabelle kann keine Einstellung vermissen, die es an der Naht nicht gibt; die Lücke war nur von der Runner-Seite sichtbar | eine Zählung |
| 56 | Der Rahmungsriegel liest den **gemessenen** Füllgrad statt des Sollwerts | durch Blender gemessen: unter ~8 m Kantenlänge übernimmt der Mindestabstand, und bei 4 m ist der Füllgrad 0,553 statt 0,700 — der Riegel war dort zu milde | ein Parameter |
| 57 | Sonne und Deckungsgrad werden **durch Blender** geprüft, nicht nur an der Naht | beide waren heute gebaut, gründlich geprüft und nie ausgeführt — dasselbe Muster, das dieser Tag achtmal gefunden hat | fünf Tests |
| 58 | Der Knotenname trägt den **IFC-Namen** mit | er ist die einzige Auskunft, die den glb-Export überlebt; ohne ihn war die Bauwerksbox gleich der Szenenbox und der Rahmungsriegel auf seinem Anlassfall blind | eine Zeile im Runner |
| 59 | Der Name steht **vor** der GlobalId | Blender kürzt bei 63 Byte — hinten fiele er bei langen Namen wieder weg, und die Kennung braucht drüben niemand | eine Reihenfolge |
| 60 | Die **IFC-seitige** Bauwerksbox wird **nicht** mitrepariert | dieselbe Regel an einer dritten Stelle zu führen, ist der Anfang der nächsten Abweichung; der Befund gehört auf die Liste, nicht in einen schnellen Zusatzfilter | — nichts geändert |
| 61 | M0 aus `auf-45` wird **hier gerechnet** statt beauftragt | es braucht keine GPU, nur `.venv-ifc` — und das liegt hier. Eine GPU-Stunde für eine beantwortbare Frage ist verschenkt | ein Test |
| 62 | Die Arbeitsteilung wird **präzisiert**: alles Geometrische hier, zur HomeStation nur, was `torch` braucht oder echte Dateien | *«dieses Environment hat keine GPU»* hat den ganzen Tag zu der falschen Folgerung verleitet, hier lasse sich nichts messen | ein Absatz |
| 63 | `auf-39` wird **hier beantwortet** statt bei der HomeStation | alle vier Posten brauchen nur synthetische Geometrie und den IFC-Runner; er lag zwei Tage dort und war in zwei Sekunden gerechnet | ein Ergebnis löschen |
| 64 | Das Ergebnis nennt in einem eigenen Feld, **wer es gerechnet hat** | ein Ergebnis, dem man nicht ansieht, dass es nicht von der Messmaschine kommt, ist eine stille Falschaussage über seine Herkunft | ein Feld |

**Zwei Fragen, die nur der Owner beantworten kann:**

1. **Entscheid 37 — die LGPL-Präzisierung und `libquadmath`.** Gilt die Auflage
   «nur hinter einer Prozessgrenze» auch für eine LGPL-Komponente, die niemand direkt
   einbindet, sondern die statisch in einem BSD-3-Wheel mitreist? Betroffen ist **eine**
   Zeile (`aiimaging.render`, ein verzögerter `import numpy` für eine 16-Bit-Tiefenkarte).
   Lautet die Antwort ja, ist der Ausweg klein: die Umrechnung ohne numpy schreiben.
   Lautet sie nein, gehört die Präzisierung in `CLAUDE.md` um diesen Fall ergänzt — dann
   steht die Regel künftig da, wie sie gemeint ist.
2. **Die Doppelung in `gate.py`** (Abschnitt 8g): Sie anzuschliessen ändert das Verhalten
   im Fall «kein Stil bestellt» von *bestanden* auf *nicht bestanden* — eine
   Vertragsänderung, keine Aufräumarbeit.

**Eine Bitte an den Owner:** Entscheid 12 ist der einzige, bei dem ich einer Messung der
HomeStation widerspreche. Wenn das Auftragsformat nicht der richtige Weg dafür ist, sagen
Sie es — dann frage ich künftig anders.
