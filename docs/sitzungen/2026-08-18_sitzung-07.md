# Sitzung 07 · 2026-08-18

**Auftrag:** Nachsehen, was der lokale Arbeiter gepusht hat, und fortfahren.
**Ergebnis:** Die Blender-5.2-Sperre ist gelöst — nicht durch einen Flicken, sondern
durch eine **verschobene Naht**. Die Normalisierung der Tiefenkarte hat Blender
verlassen. Dazu ist `art: "render"` gebaut und der erste echte Render beauftragt.

---

## 1 · Der Befund von der HomeStation, und warum er mehr wert ist als der Fehler

Auftrag `auf-20260818-05` lief zum ersten Mal durch den Kompositor: `tiefe_.exr`,
`beauty_.png`, `material_id.png`, Materialtabelle. Beide gestellten Fragen sind
beantwortet:

- Der Tiefenkanal heisst auf 5.2 **`tiefe_.V`** — der Ebenenname mit Punkt als Präfix.
  Die Vermutung aus Sitzung 06 stimmte.
- `aiimaging.bildlesen` liest die Multilayer-EXR **ohne Blender-Rückfall**:
  `ohne_blender_lesbar=True`, 11 151 Geometriepixel zwischen 17,934 und 26,625 m.

Der lokale Arbeiter hat dabei zwei Dinge am Runner geändert, beide begründet und beide
am Gerät gemessen statt geraten:

1. **Slot-Anlage.** Die Kandidatenliste enthielt `new("COLOR", name)` — und `COLOR` ist
   auf 5.2 ausgerechnet einer der **ungültigen** Werte. Gemessen gültig: `RGBA`,
   `VECTOR`, `FLOAT`; ungültig `COLOR` und `VALUE` (`TypeError`), `IMAGE`
   (`RuntimeError`). `FLOAT` steht jetzt zuerst, weil eine Tiefe **ein** Wert ist und
   kein Farbwert.
2. Eine Prüfung auf 0×0 vor der Rechnung, mit der Ursache in der Meldung.

**Der eigentliche Ertrag ist aber der strukturelle Befund:**

> Blender 5.2 kann die Datei, die es schreiben **muss**, selbst nicht wieder einlesen.
> Der File-Output-Knoten lässt dort nur `OPEN_EXR_MULTILAYER` zu, und eine so
> geschriebene Datei lädt `bpy.data.images.load` als **0×0 mit 0 Kanälen**. Eine
> einschichtige EXR lädt im selben Lauf als 64×64 mit 4 Kanälen.

Die alte Meldung „kein einziger Geometriepixel" zeigte damit auf ein Kamera- oder
Schwellenproblem, wo keines ist. Die Schwelle (1e7) war unschuldig, die Kamera auch, die
Datei war in Ordnung — nur Blenders eigener Leseweg nicht. **Eine Fehlermeldung, die auf
die falsche Ursache zeigt, kostet mehr als gar keine.**

---

## 2 · Die Entscheidung: die Naht verschieben, nicht flicken

Der lokale Arbeiter hat eine Empfehlung ausgesprochen und sie ausdrücklich **nicht**
gebaut, weil sie eine Vertragsänderung an der Prozessgrenze ist. Sie ist übernommen:

**Die Normalisierung gehört auf die Produktseite.**

Bis heute schrieb der Runner die EXR, lud sie in Blender wieder ein und rechnete sie zu
`tiefe_norm.png` um. Ab jetzt tut das `aiimaging.bildschreiben` auf dieser Seite der
Prozessgrenze; `seams.glb_zu_multipass` ruft es nach dem Blender-Lauf auf.

**Drei Gründe, in aufsteigender Wichtigkeit:**

1. Es umgeht die 5.2-Sperre — der geringste der drei.
2. Es gibt nur noch **einen** Weg zum PNG statt eines je Blender-Fassung. Zwei Wege
   wären eine stille Abweichung, die niemand bemerkt, weil beide plausible Bilder
   liefern.
3. **Regel 4 in ihrer schärfsten Lesart:** Eine Normalisierung ist Arithmetik auf einem
   Zahlenfeld. Was ohne Blender geht, geht ohne Blender — und ist damit ohne GPU, ohne
   Oberfläche und ohne Prozessgrenze prüfbar. Ein GPL-Programm zu starten, dessen
   Leseweg sich zwischen zwei Fassungen ändert, bringt hier keinen Gewinn und ein Risiko.

Der Runner tut ab jetzt nur noch, was **allein Blender kann**: rendern.

**Ein Nebeneffekt, der die Entscheidung stützt:** Der Runner braucht jetzt gar kein
NumPy mehr — es kam nur in der Normalisierung vor. Damit hängt der Blender-seitige Teil
an nichts mehr ausser `bpy` selbst.

### Was der Wechsel an den Zahlen ändert — gemessen, nicht behauptet

Gegen die letzte in Blender 4.2 erzeugte Referenz (256×256, dieselbe Szene):

| Grösse | Blender 4.2 | Produktseite | Befund |
|---|---|---|---|
| `min_m` | 17,933635711669922 | 17,933635711669922 | identisch |
| `max_m` | 26,625198364257812 | 26,625198364257812 | identisch |
| `n_geometriepixel` | 11 151 | 11 151 | identisch |
| abweichende Bildpunkte | — | **18 von 65 536** | je genau **eine** Quantisierungsstufe |
| grösste Abweichung | — | 7,7 · 10⁻⁶ | eine **halbe** Stufe |

Die Ursache ist bekannt und harmlos: Blender rechnet in float32, Python in float64. Wo
ein Zwischenwert genau auf der Rundungsgrenze liegt, fällt er mal so, mal so.

Eine float32-Nachbildung wäre möglich gewesen. Sie ist bewusst **unterlassen**: Sie
würde die Reihenfolge von NumPy-Operationen nachahmen, die niemand zusichert — falsche
Genauigkeit statt echter. Ab jetzt ist **das Produkt die Referenz**, nicht mehr Blender.

### Ein Nebenbefund, der mehr trägt, als er kostet

Die 5.2-Zahlen des lokalen Arbeiters (17,934–26,625 m, 11 151 Punkte) stimmen **exakt**
mit den hier auf 4.2 gemessenen überein. Zwei Blender-Fassungen, zwei Rechner, zwei
Dateiformate (einschichtig und multilayer) — dieselbe Tiefe. Das ist eine
Kreuzvalidierung, die niemand geplant hatte, und sie ist mehr wert als der Test, der sie
nebenbei erzeugt hat.

---

## 3 · Was gebaut wurde

**`src/aiimaging/bildschreiben.py`** — die Gegenseite zu `bildlesen.py`, reine stdlib:

- `schreibe_graustufen_png` — 16- und 8-Bit-Graustufen-PNG über `zlib`. **Kein Pillow.**
  Das Paket hat bewusst null Laufzeitabhängigkeiten (`pyproject.toml`); für ein gelöstes
  Problem eine Abhängigkeit samt Binäranteil und ungeprüfter Lizenzlage aufzunehmen wäre
  ein Rückschritt unter Regel 1.
- Der Schreiber wählt den PNG-Zeilenfilter je Zeile nach der MSAD-Heuristik. Das spart
  Platz (10 750 statt 12 974 Bytes gegenüber Blender), aber der eigentliche Nutzen ist
  ein anderer: Ein Schreiber, der **alle fünf** Filter benutzt, prüft nebenbei den
  Entfilterer in `bildlesen` an echten Daten.
- `normalisiere_tiefe` — Meter → Grau, *nah = hell*, mit ausdrücklicher
  Hintergrundschranke. (Blenders `Normalize`-Knoten normalisiert über **alle** Punkte,
  also auch über den Hintergrund mit seinen ~1e10 m; das Gebäude wäre gleichmässig
  schwarz.)
- `tiefe_exr_zu_png` — der ganze Weg, mit `_leser` als Test-Naht.

**`seams._tiefe_nachbearbeiten`** — bewusst **nicht tödlich**. Scheitert die
Normalisierung, bleibt der Blender-Lauf gültig und der Grund steht in
`depth_png_fehler`. Die EXR mit den echten Metern ist das massgebliche Artefakt; das PNG
ist ihre verlustbehaftete Ableitung für das Bildmodell.

Die Naht ist zugleich der **einzige** Erzeuger von `depth_png`. Ein Wert, der schon im
Report stand, wird überschrieben statt ergänzt — er stammte sonst aus einem Runner, der
nicht mehr normalisiert. Dieselbe Lehre wie beim Abräumen der Altdateien in Sitzung 05:
**Ein Feld im Report ist kein Beleg für eine Datei auf der Platte.**

**`tools/homeworker.py` — `art: "render"` ist gebaut.** Bis heute meldete es
`uebersprungen`. Der Ablauf ist jetzt IFC → glb → Multipass → Bildmodell →
Tiefenschätzung → Geometrie-Score, und jede Stufe berichtet **einzeln**: Bricht es in
der Mitte, bleibt die Erkenntnis der ersten Hälfte erhalten.

Zwei Test-Nähte (`_render_modell`, `_tiefen_modell`) machen den Pfad ohne GPU und ohne
ein einziges Gewicht prüfbar. Ohne sie wäre ausgerechnet der Teil, der unbeaufsichtigt
an einer 400-W-Hardwareschranke läuft, faktisch ungeprüft geblieben.

---

## 4 · Zwei Fehler, die der neue Schreiber selbst mitbrachte

Der PNG-Schreiber wurde von einem eigenen Prüfer gegen seinen Docstring gemessen. Zwei
Zusagen hielten nicht — beide behoben, beide lehrreich:

**1. `inf` wurde nicht beschnitten, sondern riss den Aufruf ab.** Der Docstring sagte
zu, Werte ausserhalb 0..1 zu beschneiden. Für `inf` warf `int(round(inf * 65535))` einen
`OverflowError`. Das ist kein exotischer Eingabewert: `bildlesen.tiefen_aus_png` gibt in
seiner Vorgabe für **jeden** Hintergrundpunkt genau `inf` zurück — wer eine gelesene
Tiefenkarte zurückschreibt, trifft es sofort. Der Fehler sass genau auf dem Weg, für den
das Modul gebaut wurde.

**2. `nan` schlug als blankes `ValueError` durch**, ohne Bezug zum Bild.

Bei der Korrektur fielen die beiden Fälle **auseinander**, und das ist der eigentliche
Ertrag: `inf` ist der Grenzfall von „ausserhalb" und bekommt dieselbe Regel wie 1e9 — es
wird beschnitten. `nan` ist dagegen *kein Wert*. Es zu beschneiden hiesse zu entscheiden,
ob ein Loch in den Daten schwarz oder weiss ist — und diese Entscheidung gehört dem
Aufrufer. Darum jetzt ein `SchreibError`, der die **Fundstelle** nennt (Index, Zeile,
Spalte).

Dass ein Docstring etwas zusagt, ist eben auch kein Beleg dafür, dass der Code es tut.

---

## 5 · Neun Befunde aus zwei unabhängigen Prüfungen

Zwei Prüfer wurden losgeschickt, einer gegen die neue Naht, einer gegen `homeworker.py` —
das Skript, das **unbeaufsichtigt an einer 400-W-Hardwareschranke** läuft und bis heute
**keinen einzigen Test** hatte. Sie fanden neun Dinge; alle behoben, die Tests bewachen
sie jetzt.

### Die zwei, die wehtun

**Ein vergifteter Cache-Eintrag, für immer.** Die neue Naht ist bewusst nachsichtig:
Scheitert die Normalisierung, bleibt der Blender-Lauf gültig. Was ich übersah: Der
Multipass-**Knoten der Kette** galt damit als `ok` und wanderte in den Zwischenspeicher.
`_fehlende_ausgabedateien` fängt ihn nicht ab — es prüft nur Felder mit einem nicht-leeren
Text als Wert, und `depth_png` ist `None`. Der Cache-Schlüssel hängt an Parametern und
Vorgänger-Hashes, nicht an der Umgebung. Also: Ursache behoben, Schlüssel unverändert,
Treffer bleibt, Ausführer wird **nie wieder gerufen**.

Der Prüfer hat es gemessen, nicht befürchtet: drei Läufe, ein einziger Blender-Start, der
dritte hätte gelingen müssen. **Vor** meiner Änderung wäre der Knoten gescheitert — und
Gescheitertes wird nicht gespeichert. Ich hatte eine Rückstufung von *Abbruch* auf *Feld
im Report* vorgenommen und nur die eine Hälfte der Folgen bedacht.

Behoben dort, wo die Entscheidung hingehört: Die Bibliotheksfunktion bleibt nachsichtig,
der **Knoten** nicht. Was sein Nachfolger braucht, darf bei ihm nicht als gelungen gelten.

**Dieselbe Nachsicht hätte die Untersuchung verhindert, die sie ausgelöst hat.** Der
`multipass`-Zweig im `homeworker` meldete jetzt `status: ok`, auch wenn die Tiefenkarte
fehlte. `auf-01` bis `auf-05` sind allesamt `art: "multipass"` und haben `fehler`
zurückgemeldet — **genau daran wurde die Blender-5.2-Sperre überhaupt gefunden.** Mit dem
neuen Verhalten wäre `auf-05` als `ok` zurückgekommen, und die einzige Spur wäre ein
fehlender Dateiname in einer Liste gewesen.

Die Lehre: **Ein Auftrag ist ein Bericht an jemanden, der nicht danebensteht. Er muss
lauter sein als eine Bibliotheksfunktion, nicht leiser.**

### Vier fail-open-Löcher in der Hardwareschranke

Der Docstring sagte „Fail-closed" zu. In drei Zweigen stimmte das nicht:

- `nur_bei_leerlauf: false` schaltete **auch die Prüfung der 400-W-Grenze** ab. Die
  beiden Auflagen stehen im README als getrennte Zeilen, und die abgeschaltete ist die,
  an der der Rechner hängt.
- Mit `nur_bei_leerlauf: false` wurde auch bei **völlig unbekanntem** GPU-Zustand
  gestartet.
- Fehlte die Leistungsgrenze im Zustand, rechnete die Prüfung mit `0` weiter und gab
  frei. **Ein unbekannter Wert ist kein niedriger Wert** — dieselbe Verwechslung wie
  „Datei vorhanden heisst Datei richtig", nur an anderer Stelle.
- Eine leere Ausgabe von `nvidia-smi` (Code 0, nichts darin — kommt bei Treiber-Neustart
  vor) stürzte ab, statt „Zustand unbekannt" zu melden. Ein Absturz ist nicht
  fail-closed, sondern gar kein Verhalten.

Die Reihenfolge ist jetzt umgedreht: erst der Zustand, dann die Leistungsgrenze (**nicht
abschaltbar**), zuletzt das Leerlauf-Gate, das als einziges ein Auftrag abschalten darf.

### Und ein Regel-3-Verstoss, den ich heute selbst eingebaut habe

Vier Felder trugen den **vollen Pfad des Arbeitsverzeichnisses der HomeStation** ins
öffentliche Repo — `geometrie_qa.bild_png` sowie `depth_png`, `beauty_png` und
`ausgabe_png` unter `render.parameter`. Für `render.bild` hatte ich denselben Pfad
ausdrücklich gekürzt; die anderen vier lagen tiefer verschachtelt und blieben ganz.

Das ist die verräterischste Sorte Verstoss: kein Bild, keine Geometrie — nur ein Pfad.
Aber ein Pfad trägt Benutzernamen und Ordnerstruktur, und sobald einmal mit echten
Projekten gearbeitet wird, Büro- und Kundennamen. `CLAUDE.md` nennt das wörtlich. In
`auf-20260818-05.json` steht bereits `/home/andrin-baumann/…` — über einen Fehlertext,
andere Ursache, gleiche Bauart.

Behoben mit `_nur_dateinamen`: rekursiv, aber als **Positivliste**. Eine allgemeine
Bereinigung würde irgendwann etwas kürzen, das ein Pfad zu sein scheint und keiner ist.

### Zwei kleinere

Die Lizenzprüfung des Tiefenschätzers fiel **hinter** der GPU-Stunde. Die Korrektur ging
weiter als der Befund: Regel 1 entscheidet jetzt **vor** dem Render, dieselbe Reihenfolge,
die `render.rendere` schon hat. Und `glb_zu_multipass` startete verdeckt einen zweiten
Blender-Prozess — der stdlib-EXR-Leser fällt bei exotischen Kompressionen auf Blender
zurück — **ohne Naht und mit fremdem Zeitlimit** (300 s statt der 900 s des Aufrufers).
Es war die einzige Prozessgrenze des Projekts ohne injizierbare Naht. Jetzt durchgereicht.

**1151 Tests grün.** `homeworker.py` hat erstmals welche.

---

## 6 · Ein Fund im eigenen Anhang

Beim Nachführen des Lexikons kam ein Fehler zutage, der zum Muster dieses Projekts passt:
Das **Änderungsverzeichnis versprach zwei Einträge, die es nie gab** — „Leistungsgrenze"
und „Rauchprobe" kamen in der ganzen Datei nur in dieser einen Tabellenzeile vor.

Das ist die Papierform derselben Lehre, die dieses Projekt schon dreimal am Code gelernt
hat: **Die Existenz eines Eintrags ist kein Beleg für seinen Inhalt.** Und es ist der
teurere Fall — ein Verzeichnis, das Einträge behauptet, die es nicht gibt, macht die
Lücke *unauffindbar*, weil sie als erledigt abgehakt aussieht.

Beide Begriffe sind nachgetragen (dazu „Leerlauf-Torwächter"), und die Korrektur steht
als eigene Zeile im Verzeichnis — nicht als stille Berichtigung der alten.

---

## 7 · Phase 4 angefangen: die Schwelle ist jetzt gemessen, nicht geraten

Der Render blockiert nur einen Strang. Die Schwellenstudie — laut `PLAN.md` das, was
*„die Arbeit über einen Werkstattbericht hinaushebt"* — braucht keine GPU, solange sie
die **Metrik** kalibriert und nicht die Kette.

`aiimaging.schwellenstudie` verfälscht eine Soll-Tiefenkarte gezielt: acht Störungsarten
über je sieben Stärken, jede mit einer **prüfbaren Erwartung**, welchen Anteil des Scores
sie treffen soll. Alle 48 auswertbaren Zeilen erfüllen ihre Erwartung — die
Messinstrumente sind also in Ordnung, bevor über die Zahlen geredet wird.
Volle Auswertung: `docs/SCHWELLENSTUDIE_2026-08-18.md`.

### Drei Befunde

**Die Rangbasiertheit ist bestätigt.** Massstab, Nullpunkt und Potenz lassen den Score
bei **exakt 1,000**. Das ist die einzige Prüfung der ganzen Studie, die die Metrik hätte
*umwerfen* können — alles andere beschreibt nur. Sie ist bestanden.

**0,65 ist zu mild, und das ist jetzt gemessen.** Von 36 gestörten Fällen lässt die
Schwelle **18 durch** (von 32 auswertbaren), Trefferquote 0,44. Bestes Ergebnis bei
**0,90** (0,84–0,89), über 48², 64² und 96² dieselbe Zahl. Der auffälligste Wert: Bis 0,85 wird **kein einziger
treuer Fall gesperrt** — anheben kostete auf dieser Szene nichts.

**Die Metrik bestraft verlorene Gliederung praktisch nicht** — acht Mittelungsdurchgänge
kosten vier Tausendstel. Und ein *hinzugefügter* Baukörper von der Fläche des Baus selbst
besteht mit 0,698 — er geht also durch. Das steht in Spannung zur bisherigen Projektangabe „halluziniert 0,24"
(die selbst falsch war — der Testfall misst **0,199** bei `geom_iou` 0,040; 0,24 stammte
aus einer früheren Fassung der Testdaten und stand seit Sitzung 05 in `PLAN.md`):
Der frühere Fall **versetzte** die Geometrie, dieser **ergänzt** sie. Anbauen wird
deutlich milder bestraft als versetzen — ein Unterschied, den bisher niemand gesehen hat.

### Die Schwelle bleibt trotzdem bei 0,65

0,90 wäre auf dieser Szene besser begründet. Aber die Studie kalibriert die Metrik, nicht
die Kette: Im Betrieb liegt zwischen Soll und Ist ein Tiefenschätzer, dessen Fehler in
keiner dieser Zahlen steckt. Er senkt jeden Score, und wie weit, weiss niemand. 0,90
könnte im Betrieb **jeden** Render sperren — genau die Falle, in die der `geom_iou`-Deckel
in Sitzung 06 schon einmal geführt hat.

**0,65 ist damit nicht verteidigt, sondern beibehalten.** Der Unterschied steht jetzt im
Konstanten-Kommentar von `geometrie_qa`, nicht nur in einem Dokument.

### Die Szene musste zweimal gebaut werden — und das war der lehrreichste Teil

Der erste Lauf lieferte zwei Zeilen, die wie Aussagen über die Metrik aussahen und
Aussagen über die **Szene** waren:

- `glaettung` war völlig wirkungslos. Die erste Szene war eine reine Rampe, und der
  Mittelwert einer linearen Folge ist wieder dieselbe Folge — die Störung hatte nichts
  zu zerstören.
- Die Monotonie-Kontrolle scheiterte scheinbar (0,999997 statt 1,0). Ursache: **1837
  gleiche Werte auf 1936 Punkte**, weil das Gefälle aus zwei gleichgewichteten
  Achsenanteilen entstand. Mit einem inkommensurablen Verhältnis (√2) fällt die
  Bindungszahl auf **0** und die Kontrolle steht exakt.

> **Ein Messinstrument kann saubere Zahlen über nichts liefern.** Beide Fehler hätten als
> Ergebnis durchgehen können — „Glättung schadet nicht", „das Verfahren ist nicht ganz
> rangbasiert" —, und beide wären falsch gewesen.

### Und dann waren zwei meiner eigenen Zahlen falsch

Die Testabnahme der Studie fand sechs Befunde, zwei davon trafen die **veröffentlichte
Auswertung**:

- **Das Stärkeraster schnitt mitten durch die Grenze.** Die räumlichen Störungen rechnen
  in ganzen Bildpunkten; auf 64² sind Stärke 0,2 und 0,3 **punktgleich**. Weil
  `grenzstaerke` genau dazwischen liegt, standen vier Zeilen mit *derselben Messung* auf
  verschiedenen Seiten der Grenze — keine Schwelle kann sie trennen, jede zählt
  zwangsläufig einen Fehler. Diese vier gingen als `falsch_frei` in die Zahl ein, also
  als Aussage über die Metrik, wo eine Aussage über das Raster stand. Sie werden jetzt
  entdoppelt, und *wieviele* steht im Ergebnis.
- **Der Zusatzkörper war 40 % so gross wie angekündigt** — der Bau schnitt das Quadrat
  weg. Die Rechnung stimmte, die Beschriftung der Achse nicht, und die Auswertung hat der
  Beschriftung geglaubt.

Berichtigt: **18 von 32** statt 22 von 36, Zusatzkörper **0,698** statt 0,844. Die
Aussagen bleiben, eine wird schärfer.

> Dieselbe Gestalt wie die zwei Fehler aus Kapitel 2, nur eine Ebene höher: dort die
> Szene, hier die Störung. Und der Zusatzkörper ist der unangenehmere Fall — seine Zahlen
> waren **richtig**, nur ihre Beschriftung war falsch.

---

## 8 · Connectors: die Annahme, die den Bau auslöste, war falsch

Phase 4 nennt „Connectors: ArchiCAD über IFC4, Rhino über glTF". Ein Connector ist hier
**kein Import-Filter** — IFC und glTF liest das Projekt längst. Was fehlte, ist die
Antwort auf die zwei Fragen, die zwischen zwei Autorenprogrammen wirklich verschieden
ausfallen: **In welcher Einheit sind die Zahlen, und wo ist oben?**

Der Bau begann mit einer klaren Vermutung: ArchiCAD exportiert in Millimetern, niemand im
Projekt liest die Einheitenzeile, also käme eine ArchiCAD-Datei als Gebäude von
8000 × 5000 × 3250 Metern an.

**Die Vermutung ist falsch.** Eine realistische ArchiCAD-Datei — `.MILLI.` in der
Einheitenzuweisung *und* tausendfach grössere Koordinaten — läuft durch `ifc_zu_glb` und
kommt bei exakt **8,0 × 5,0 × 3,25 m** heraus. IfcOpenShell wendet den Faktor selbst an,
der Torwächter nimmt an. Für ArchiCAD über IFC4 braucht es keine Umrechnung.

Der Irrtum fiel auf, weil die erste synthetische Prüfdatei zu billig gebaut war: Sie setzte
nur die Einheit auf `.MILLI.` und liess die Zahlen metergross. Heraus kam ein Bauwerk von
acht Millimetern — was ich zunächst für die Bestätigung hielt. Erst die *richtig* gebaute
Datei zeigte, dass die Kette in Ordnung ist.

**Und damit war der billige Fehlversuch der eigentliche Fund:** Eine Datei, die Millimeter
*erklärt* und metergrosse Zahlen *trägt*, ist ein kaputter Export — und den gibt es
wirklich. Der `torwaechter` fing ihn schon, aber nur als *Verdacht*
(„Verdacht auf Einheitenfehler um Faktor 0,001"), weil er die Masse sieht und nicht den
Kopf. Mit beidem nebeneinander wird daraus eine **Diagnose**: Der erklärte Faktor
herausgerechnet ergibt ein plausibles Bauwerk, also standen die Zahlen in Metern, während
die Einheitenzuweisung Millimeter behauptet — nicht der Konverter ist schuld, sondern der
Export. Das leistet `pruefe_einheit_gegen_masse`.

Ein Verdacht kostet jedes Mal einen Menschen, der nachsieht. Eine Diagnose sagt ihm, wo.

### Rhino ist der ehrlichste Eintrag der Registry

Für glTF bleibt die Up-Achse offen, und das mit Absicht. Die Registry unterscheidet drei
Stufen — **belegt**, **vermutet**, **unbekannt** —, und Rhino steht auf *unbekannt*, nicht
weil zu wenig bekannt wäre, sondern weil die Frage an der Datei nicht entscheidbar ist:
Rhinos Modellraum ist Z-up, die glTF-Norm schreibt Y-up vor, und der Exporter hat dafür
einen **Schalter**, den die Datei nicht mitteilt.

`fordere_up_axis` nimmt eine Vermutung darum **nicht** an. Sie steht in der
Fehlermeldung, damit ein Mensch sie bestätigen kann — aber sie winkt sich nicht selbst
durch. Genau hier wollte Phase 0 keinen Default, und eine Vermutung, die sich selbst
durchwinkt, *ist* ein Default mit besserer Begründung.

IFC ist der Gegenfall: Dort ist Z-up **Norm** (ISO 16739), nicht Herstellergewohnheit.
Das ist die einzige Stelle im Projekt, an der `up_axis` aus der Datei folgt statt aus
einer Zusage — und darum die einzige, an der sie nicht angegeben werden muss.

### Und die Testabnahme fand zwei Fehler in genau diesem Modul

**Wer eine Datei umbenannte, änderte ihre Herkunft.** Die erste Fassung sammelte alle
nichtleeren Zeichenketten aus `FILE_NAME` und nahm die letzten drei. In der Testgeometrie
sind nur drei Felder gefüllt — **Dateiname**, Zeitstempel, Beschreibung —, also landete der
Dateiname in der Erkennung: `rhino-haus.ifc` galt als von Rhino erzeugt, obwohl das
Erzeugerfeld Rhino nirgends nennt. Das war keine ungenaue Erkennung, sondern eine falsche.

Von der anderen Seite dasselbe: Ein Autorenfeld `Blenderweg 12` — eine Strasse — ergab
„Blender", weil auf Teilzeichenketten in Freitextfeldern gesucht wurde.

Gelesen wird jetzt **nach Position**: `preprocessor_version` und `originating_system`,
Index 4 und 5 nach ISO 10303-21. Dateiname, Autor und Organisation bleiben draussen. Dafür
braucht es einen klammertreuen Feldzerleger — Autor und Organisation sind selbst Listen,
und ein Autorenname „Muster, Anna" trägt ein Komma, das jedes Feld danach verschöbe.

Dass der Schaden bei IFC begrenzt gewesen wäre (die Up-Achse ist dort ohnehin Norm), macht
es nicht harmloser: **Bei glTF entschiede derselbe Fehlgriff darüber, welche Achse einem
Menschen zur Bestätigung vorgeschlagen wird.** Das ist die Sorte Hilfestellung, die man
bestätigt, ohne nachzusehen.

---

## 9 · Eine Uneinheitlichkeit im Vollzug von Regel 1 — sichtbar gemacht, nicht entschieden

Beim Aufräumen des Lizenzvokabulars kam heraus, dass **zwei Registries desselben Projekts
Regel 1 verschieden streng anwenden**:

| Eintrag | Lizenz | kommerziell? | Urteil |
|---|---|---|---|
| `einbetter["dinov3"]` | Meta-Sonderlizenz | **erlaubt** | ⛔ ausgeschlossen |
| `backbone["sdxl-juggernaut"]` | CreativeML OpenRAIL-M | erlaubt, mit Auflagen | ✅ zugelassen |
| `backbone["sd35-large"]` | Stability Community License | erlaubt, unter 1 Mio USD | ✅ zugelassen |

Die Begründung für den DINOv3-Ausschluss steht wörtlich im Code: *„Regel 1 verlangt
permissiv, nicht bloss erlaubt."* Genau dieser Satz trifft auf die anderen beiden ebenso zu.
Dieselbe Klasse von Lizenz, entgegengesetztes Urteil.

Das ist keine Auslegungsfrage, sondern eine **Uneinheitlichkeit im Vollzug**. Aufgelöst
wird sie hier nicht: Ein Ausschluss nähme dem Projekt den Rückfall-Backbone, und das ist
ein Owner-Entscheid. Stillschweigend stehenlassen wäre aber ebenso falsch — darum steht
der Widerspruch ab jetzt **in jeder Antwort von `pruefe_lizenz`**, im Feld
`regel_1_spannung`, statt nur in einem Bericht. Ausführbar statt als Fussnote, wie Regel 1
selbst.

*Betrifft den Vorgabe-Backbone nicht: Qwen-Image-Edit ist Apache-2.0.*

---

## 10 · LoRA-Training: wo Regel 1 und Regel 3 sich treffen

Der letzte Phase-4-Punkt, der ohne GPU baubar war. Ein LoRA ist eine kleine Zusatzschicht
zu einem Bildmodell, die einen Stil trägt — für ein Architekturbüro der eigentliche Wert
der ganzen Kette: Das Modell malt dann im **eigenen** Haus-Stil.

Genau darum ist diese Naht die heikelste im Projekt. Sie ist die **einzige Stelle, an der
beide harten Regeln gleichzeitig greifen.**

**Regel 1 — ein LoRA erbt die Lizenz seiner Grundlage.** Er ist eine Differenz zu deren
Gewichten und ohne sie nutzlos. `CLAUDE.md` zieht die Folge ausdrücklich: Non-Commercial
gilt „auch für daraus abgeleitete LoRAs". Ein auf FLUX-dev trainierter Haus-Stil ist also
nicht verkaufbar — und **daran ändert weder das Eigentum an den Trainingsbildern noch die
Rechenzeit etwas.** Normalerweise merkt man das, wenn die GPU-Stunden bezahlt sind;
`pruefe_auftrag` lehnt es vor der ersten ab.

**Regel 3 — Trainingsdaten sind das genaue Gegenteil von synthetisch.** Ein Stil-LoRA wird
auf echten Bürobildern trainiert. Das ist sein Zweck, und genau das darf nie ins Repo.
Datensatz **und** Ausgabe innerhalb des Repos werden abgewiesen, aufgelöst über
`Path.resolve`, damit ein `..`-Umweg nicht daran vorbeikommt. Und mit der Begründung, die
zählt: *Auch späteres Löschen hilft nicht — die Git-Historie behält die Bilder.*

### Zwei Dinge, die dabei nebenbei besser wurden

**Die Trainerlizenzen sind jetzt gemessen statt abgeschrieben.** Die Lagebeurteilung
führte kohya (Apache-2.0) und ai-toolkit (MIT) als „nur Sekundärquelle". Beide am Original
nachgelesen — beide stimmen. Nach dem Krita-Fund von heute Morgen ist das keine Formalie:
Dort lag die Sekundärquelle in die gefährliche Richtung falsch.

**Für `ai-toolkit` verweigert `baue_kommando` die Auskunft.** Es wird über eine
YAML-Konfiguration gesteuert, nicht über Flaggen. Eine erfundene Kommandozeile wäre
schlimmer als eine ehrliche Absage — sie sähe richtig aus und wäre erst auf der GPU als
falsch zu erkennen.

*Ehrliche Grenze:* Hier wurde **nie ein Training ausgeführt**. Die Lizenzen sind gemessen,
die Kommandozeilen aus der Dokumentation übernommen und im Feld `beleg` als solche
gekennzeichnet — derselbe Vorbehalt wie bei der Connector-Registry, und er wird genauso
aufgelöst: von jemandem, der das Gerät hat.

---

## 11 · Der Satz, der viermal falsch dastand

Die Lexikon-Durchsicht fand eine Behauptung, die dieses Projekt an **drei** Stellen über
sich selbst machte — in `pyproject.toml`, im `LEXIKON` und, seit heute Nachmittag, in
meinem neu geschriebenen `README`:

> „Alles Schwere liegt jenseits einer Prozessgrenze in eigenen Environments."

**Das ist falsch.** `render.lade_modell` und `tiefenschaetzer.lade_modell` importieren
`torch`, `diffusers`, `transformers` und `Pillow` **in diesen Prozess** — verzögert
innerhalb der Ladefunktion, aber in-process. Und mit `torch` kommen auf Linux über ein
Gigabyte NVIDIA-Binärdateien in genau dieses Environment, wie die Binärprüfung von heute
belegt.

Der wahre Satz lautet: **Die Prozessgrenze trennt nach Lizenz, nicht nach Gewicht.**
Jenseits von ihr liegt, was copyleft ist — Blender (GPL) und IfcOpenShell (LGPL mit
statisch gelinktem GPL-CGAL). `torch` ist BSD-3-Clause und braucht sie nicht.

**Regel 1 ist dadurch nicht verletzt**, und das ist wichtig festzuhalten, damit die
Korrektur nicht grösser klingt als sie ist: Die NVIDIA-Laufzeit ist proprietär, aber
nicht copyleft, und steckt Apache-2.0-Code darum nicht an. Eine Grenze bräuchte es erst,
wenn CUDA und eine GPL-Komponente in denselben Prozess müssten — genau das verhindern die
zwei bestehenden Grenzen. Die Schlussfolgerung des Binärberichts („eine
Architekturänderung folgt nicht") bleibt also stehen; falsch war nur die Begründung, die
das Projekt sich selbst gab.

Bemerkenswert ist, **wie** der Fehler überlebt hat: Er stand seit der Paketierung in
`pyproject.toml`, wurde von dort ins Lexikon übernommen und von mir heute in den README
weitergeschrieben — ohne dass jemand ihn gegen den Code hielt. Ein plausibler Satz, der
zur Architektur passt, wird nicht geprüft; er wird zitiert.

> Dieselbe Lehre wie beim Lexikon-Änderungsverzeichnis heute Morgen, nur andersherum:
> Dort behauptete ein Verzeichnis Einträge, die es nicht gab. Hier behauptete eine
> Architekturbeschreibung eine Grenze, die es nicht gab.

Ein Nebenbefund derselben Durchsicht: Der Lexikon-Eintrag „Runner" nannte als Beispiele
`glb_export_runner.py` und `export_ifc_runner.py`. Beide Dateien gibt es in diesem Repo
nicht — sie gehören zu KosmoDraw und waren Vorbild, nicht Bestand.

---

## 12 · Die LoRA-Naht hatte ein Loch in genau der Regel, die sie ausführbar machen sollte

Die Testabnahme prüfte nicht nur, ob die Tests grün sind, sondern ob sie **etwas fangen**:
Das Modul wurde in einer Kopie **dreizehnmal mutiert** — Regel-1-Prüfung ausgebaut,
`liegt_im_repo` immer `False`, `resolve` durch `absolute` ersetzt, Rückfall auf
`sys.executable` eingebaut, Abbruch beim ersten Mangel, und so weiter. **Jede** Mutation
wurde gefangen. Das ist der Unterschied zwischen einer grünen Testsammlung und einer
wirksamen.

Und dann fand sie zwei Wege, an Regel 1 vorbeizutrainieren:

**`modell_wurzel` wurde nie gegen `basis` gehalten.** `lizenz_des_ergebnisses` urteilt
über den *Namen*. `modell_wurzel` zeigt auf die *tatsächlichen Gewichte*. Also lief das
hier anstandslos durch:

```
baue_kommando(LoraAuftrag(basis="qwen-image-edit-2511", …),
              modell_wurzel="/ai/flux1-dev")
```

Eine vollständige Kommandozeile, die auf einem Non-Commercial-Modell trainiert — und ein
Ergebnis, das sich als `verkaufbar=True` ausweist. Regel 1 war gegen das Versehen
abgesichert, aber nicht gegen jemanden, der `modell_wurzel` kennt.

**`zusatzflaggen` durfte alles überschreiben.** `("--pretrained_model_name_or_path",
"/ai/flux1-dev")` erzeugte ein Kommando, in dem die Flagge zweimal steht; bei einer
`argparse`-Kommandozeile gewinnt die spätere. Derselbe Ausgang, ohne `modell_wurzel` zu
berühren.

Beide sind geschlossen. **Die Grenze der Behebung steht mit im Code und im Test**, weil
sie sonst überschätzt wird: Es ist eine **Namensprüfung, keine Inhaltsprüfung**. Wer ein
FLUX-Verzeichnis in `qwen-image-edit-2511` umbenennt, kommt weiterhin durch. Der Riegel
richtet sich gegen das Versehen und die bequeme Abkürzung, nicht gegen Vorsatz.

Dazu ein dritter, kleinerer: `pruefe_auftrag` sagt eine **Mängelliste** zu und warf bei
`datensatz=None` stattdessen einen `TypeError` — ein Aufrufer, der wie zugesagt auf Mängel
prüft, bekam einen Absturz.

> Eine Regel ausführbar zu machen, ist nicht dasselbe wie sie durchzusetzen. Der
> Unterschied fällt erst auf, wenn jemand ernsthaft versucht, daran vorbeizukommen — und
> genau danach war die Testabnahme gefragt worden.

---

## 13 · Das Ökosystem hat unsere Landestelle längst gebaut — und wir bedienen sie nicht

Die letzte grosse Wissensschuld ist abgetragen: `KosmoPrepare`, `ArchitekturKosmos-Codex`,
`KosmoPublish`, `KosmoDesign`, `architekturkosmos-control-hub` gelesen
(`docs/OEKOSYSTEM_2026-08-18.md`). Zwölf Befunde; drei zählen.

**KosmoVis führt bereits ein Werkzeug, das genau unser Doppel-Gate liest.**
`kosmovis_query_qa_verdict` erwartet aus einer `render-result.json`:
`released`, `passed`, `style_status`/`geometry_status`, `style_score`,
`geometry_fidelity`, `style_threshold`, `geometry_threshold`, `fail_reasons[]` — und es
leitet das Urteil **fail-closed neu ab**, statt einem gespeicherten `released` zu trauen.
Dieselbe Haltung wie unsere.

Unser `gate.gesamturteil` liefert dieselbe Sache unter `bestanden`, `score`, `schwelle`,
`maengel`. **Dieselbe Sache, andere Namen — und KosmoOrbit verdrahtet über
Feldnamen-Gleichheit, ohne Fehlermeldung.** Der Phase-0-Befund in seiner teuersten
Ausprägung: eine tote Kante, die niemand meldet.

Behoben mit `gate.als_kosmovis_verdikt` — einer **Übersetzung an der Naht**, keiner
Umbenennung. Unsere Felder heissen deutsch und bleiben es; die Begriffe sind Teil der
Arbeit, und an ihnen hängen Begründungen. Übersetzt wird dort, wo schon `mcp_schemas`
übersetzt.

**Und ihre Dreiteilung ist besser als unsere.** Das fremde Werkzeug kennt je Gate `ok`,
`fehlt` und `degeneriert`. Genau der Fall, den die Schwellenstudie interessant macht: Ein
Score von `None` heisst *nicht messbar*, nicht *durchgefallen*. Bei uns ging das in
`maengel` unter — jetzt hat es einen eigenen Zustand.

### Zwei weitere, die dem Owner gehören

**Der Auftragsvertrag ist verbindlich, und wir verfehlen ihn in acht Feldern.**
`VIS-JOB-INFRA-KONTRAKT-2026-06-17.md` fixiert Ablageort, Kennung, Statuswerte und
Satzfelder; `KosmoVis/01_workflow/render_job_store.py` setzt ihn zeilengenau um. Status,
Token-Präfix und `vis-`-Präfix treffen wir exakt — `art`/`kind`, `erstellt`/`created_at`,
`ergebnis`/`outputs`, `fehler`/`error` und ein fehlendes `progress`/`phase` nicht.

Der Kern ist aber die **Freigabe**: Der Vertrag erwartet das Token **in der Datei**, wir
legen bewusst nur `freigegeben: true` ab — *„Das Token landet nie auf der Platte"* war
eine Sicherheitsentscheidung aus Phase 2. Der fremde Scheduler kann unsere Aufträge damit
nicht freigeben. **Das ist ein echter Zielkonflikt und kein Versehen; er gehört dem Owner
vorgelegt, nicht einseitig entschieden.**

**Eine dritte, widersprüchliche Up-Achsen-Angabe** — und sie steht in KosmoVis' eigener
Doku gegen dessen eigenen Code: `RENDER_SCENE_CONTRACT.md` §1a sagt „Z = oben", während
`ifc_to_glb.py` `up_axis: "Y"` schreibt. Die Phase-2-Entscheidung, `up_axis` zum
Pflichtfeld zu machen und **nichts** zu raten, ist damit besser begründet als vorher.

*Nebenbefund:* Unser `readonly: True` hat **keinen Leser**. Das Read-only-Tor des Backends
wendet seine Heuristik nur auf vier eingebaute Server an; für Dritt-Server zählt eine fest
verdrahtete Allowlist. Alle drei unserer Werkzeuge stehen heute hinter `require_admin`.

---

## 14 · Ein Gate, das bei Unsinn „bestanden" sagte

Die Stilstudie hat 0,30 weder bestätigt noch widerlegt — sie hat gezeigt, **wovon die
Bedeutung der Zahl abhängt**, und dabei einen Defekt gefunden.

**`kosinus` lief still über.** Ab Komponenten der Grössenordnung 10¹⁵⁴ ging `sum(x*x)`
nach `inf`, `inf/inf` ergab `nan`, und die abschliessende Klammerung zog daraus **1,0** —
den höchstmöglichen Score. Zwei **rechtwinklige** Vektoren bekamen einen vollen Treffer;
aus einem echten 0,8 wurde ebenfalls 1,0. Der Gegenfall, sehr kleine Komponenten, endete
im `StilError` „das Bild wurde nicht gelesen" — ein lauter Abbruch mit **falscher
Diagnose**.

Betrieblich kommt keins von beidem vor. Aber es ist genau die Gestalt, gegen die
`StilError` angetreten ist: *nicht* ein Abbruch, sondern eine bedeutungslose Zahl, die ein
Gate passiert. **Ein Gate, das bei Unsinn „bestanden" sagt, ist schlimmer als eines, das
abstürzt.** Behoben durch Normierung auf die grösste Komponente vor dem Quadrieren —
mathematisch folgenlos, weil der Kosinus längeninvariant ist, und für jede endliche
Eingabe überlauffrei. Beide Fälle sind damit weg.

### Der schärfste Fund der Studie

Der Boden — die Ähnlichkeit zweier **zusammenhangloser** Vektoren — liegt bei isotroper
Streuung und 768 Dimensionen bei 0,000 ± 0,036. Von dort sind es acht Streuungen bis 0,30;
keine von 2000 Proben kam so weit.

**Aber das ist der kleinstmögliche Boden.** Besetzt ein Einbetter einen Kegel — und reale
Einbetter tun das —, steigt er: bei Kegelanteil 0,6 auf 0,36. Dort läge der Boden *über*
der Schwelle, und **jedes beliebige Bildpaar bestünde das Gate.** Je nachdem, wo SigLIP 2
liegt, ist 0,30 zu hoch oder wirkungslos — aus derselben Messung.

Und daraus folgt der Verdacht, der die Zahl entwertet: Der überlieferte Fehlbereich
0,06–0,13 aus den DINOv3-Läufen **deckt sich mit dem Boden eines Kegels von rund 0,3**. Er
könnte der Boden *jenes Einbetters* gewesen sein statt eine Messung von Stilunähnlichkeit —
und dann ist er auf SigLIP 2 nicht übertragbar. Der Einbetter hat in Sitzung 06
gewechselt, die Zahl nicht.

Was die Schwelle braucht, ist darum **kein besserer Wert, sondern ein Verfahren**: den
Boden des eingesetzten Einbetters an zusammenhanglosen Bildpaaren messen und relativ
setzen — „Boden plus k Streuungen". Das überlebt einen Modellwechsel; genau daran ist 0,30
gescheitert.

*Und ein Befund über die eigenen Instrumente:* `tests/test_stil_qa.py` baut alle
Prüfvektoren in **zwei** Dimensionen. Dort erreichen 40 % aller Zufallspaare den Wert 0,30,
in 768 Dimensionen keines von 5000. Als Arithmetikprüfung richtig — als Anschauung für die
Schwelle irreführend.

---

## 15 · Owner-Entscheid: Das Token bleibt, wo es hingehört — nirgends

Der Zielkonflikt aus Kapitel 13 ist entschieden. Der Vertrag
`VIS-JOB-INFRA-KONTRAKT-2026-06-17` erwartet das Freigabe-Token **in der Auftragsdatei**;
unsere `jobs.py` legt bewusst nur `freigegeben: true` ab. **Entscheid vom 18.08.2026: Wir
bleiben bei unserer Regel und bauen einen Adapter.**

Die Begründung ist eine Asymmetrie der Kosten: Ein Token auf der Platte ist eine Klasse
Sicherheitsproblem — eine Auftragsdatei ist für jeden lesbar, der das Verzeichnis sieht,
und ein Token ist eine Befugnis. Wer es dort hinschreibt, verteilt die Befugnis mit. Ein
Adapter ist dagegen nur Arbeit.

`kosmo_naht.als_kosmo_auftrag` nimmt das Token als **Argument** entgegen und setzt es
allein in den übersetzten Satz — im Augenblick des Übergangs, aus dem Gedächtnis des
Aufrufers. Auf unserer Seite entsteht dadurch keine Datei, die es enthält.

**Der Preis ist benannt und nicht wegdefiniert:** Der übersetzte Satz *ist* eine Datei mit
einem Token darin, sobald ihn jemand schreibt. Die Naht macht die Weitergabe **möglich und
nicht unvermeidlich** — sie verschiebt die Entscheidung dorthin, wo sie hingehört, statt
sie durch eine Vorgabe vorwegzunehmen.

### Und der Typunterschied, den eine Umbenennung nicht gelöst hätte

`aufloesung` gegen `resolution` ist nicht nur ein anderer Name: Unseres ist eine **ganze
Zahl** (Kantenlänge, quadratisch), ihres eine **Zeichenkette** `"BxH"` — und ihr
Vorgabewert `"1920x1440"` ist **nicht quadratisch**. Eine der beiden Zahlen geht also
verloren.

`resolution_zu_aufloesung` nimmt die **kleinere** Kante und sagt es: Wer ein Bild in eine
kleinere Fläche zwingt, verliert Rand; wer es vergrössert, erfindet Fläche. Von beidem ist
das erste ehrlicher. Der Hinweis nennt beide Kanten, damit niemand die Wahl übersieht.

Ebenso benannt statt behoben: **Die Kennungen sind einseitig unverträglich.** Deren Muster
nimmt unsere vierzehnstellige Kennung an; unseres nimmt deren zehnstellige Unix-Zeit
nicht. `aus_kosmo_auftrag` liest den fremden Satz trotzdem — aber **schreibt die Kennung
nicht um** und sagt in `kennung_bei_uns_gueltig`, dass er bei uns nicht ablegbar ist. Eine
Kennung stillschweigend umzuschreiben gäbe zwei Systemen dieselbe Sache unter zwei Namen.

---

## 16 · GPU war nie blockiert, nur delegierbar

Ein Denkfehler von mir, vom Owner korrigiert: Ich hatte die fünf offenen Punkte in
`PLAN.md` als „braucht GPU, also blockiert" geführt. Richtig ist **„braucht GPU, also
delegierbar"** — genau dafür gibt es das Auftragsprotokoll seit Sitzung 06.

Beauftragt:

- **`auf-20260818-10` — Schwellenstudie, zweite Hälfte.** Dieselben acht Störungen, aber
  die Ist-Karte durch den Tiefenschätzer aus einem *gerenderten* Bild statt durch direkte
  Verfälschung. Der Unterschied zur ersten Hälfte **ist** der Schätzerfehler, und der ist
  die gesuchte Grösse. Die wichtigste Einzelzahl darin ist die **Nullprobe**: um wieviel
  senkt der Schätzer den Score bei *ungestörter* Geometrie? Sie sagt, wieviel von den
  0,90 übrig bleibt.
- **`auf-20260818-11` — den Boden von SigLIP 2 messen.** Die eine Zahl, ohne die 0,30
  nicht deutbar ist. Mit der ausdrücklichen Auflage, **keine Architekturserie** zu nehmen:
  sonst misst der Lauf Motivähnlichkeit statt den Boden.

---

## 17 · Die Naht hatte genau die Kante, gegen die sie gebaut wurde

Die Abnahme fand zwei Löcher, und das zweite ist das lehrreichste des ganzen Tages.

**Das Token konnte über `params` doch auf die Platte.** `jobs.baue_job` kopierte `params`
unbesehen, `schreibe_job` schrieb sie. Wer das Token als *Parameter* durchreichte, schrieb
es in die Auftragsdatei — und `freigegeben` blieb trotzdem `False`. **Das Schlimmste
beider Welten: Die Befugnis lag offen, und wirken tat sie nicht.**

Der Weg dorthin war der normale, nicht der ausgefallene: Der MCP-Vertrag des Ökosystems
führt `owner_approval_token` als **Eingabefeld**, und Eingabefelder landen bei uns in
`params`. Geprüft wird jetzt auf **beides** — den Schlüsselnamen (jemand nennt das Feld
so) und den Wert (jemand nennt es anders und schreibt trotzdem ein Token hinein), rekursiv
durch verschachtelte Strukturen.

**Und der Rundlauf verlor die Freigabe still.** Ein Auftrag mit `status="queued"` ist per
Definition freigegeben — sonst stünde er auf `awaiting_approval`. Kam er aus
`als_kosmo_auftrag` → `aus_kosmo_auftrag` ohne Token zurück, sagten Status und Freigabe
einander widersprechende Dinge, und das Ergebnis **schwieg dazu**, mit leerer
`hinweise`-Liste.

> Das ist strukturell exakt der Fehler, gegen den dieses Modul gebaut wurde: nicht ein
> Abbruch, sondern eine Angabe, die einfach nicht ankommt. Ihn ausgerechnet in der Naht zu
> haben, die ihn verhindern soll, war die unangenehmste Art, ihn zu lernen.

**Repariert wurde er nicht durch Setzen von `freigegeben`** — das erfände eine Befugnis
aus einem Statuswort. Repariert wurde er durch *Sagen*: `satz_ist_freigegeben_laut_status`
prüft den Widerspruch, und der Hinweis nennt beide möglichen Ursachen.

*Der Preis der Naht bleibt daneben stehen und ist jetzt selbst ein Test:* Wer den
übersetzten Satz schreibt, schreibt das Token. `test_der_benannte_preis_ist_echt` nagelt
das fest, damit der Docstring nicht unbemerkt falsch wird.

---

## 18 · ArchiCAD liefert IFC2X3 — und dagegen war nie geprüft worden

Der Owner beginnt den nächsten Lauf mit einer echten **ArchiCAD-28-Datei**. Aus
`auf-20260818-08` wissen wir über genau dieses Programm schon einiges, und ein Punkt
davon war unbequem:

| | |
|---|---|
| Erzeugerzeichenkette | `DDS_IFC v3.0 \| Graphisoft - Archicad - 28.4.0` |
| Schema | **IFC2X3**, alle zehn Dateien |
| Einheit | METRE (Faktor 1,0) |
| Up-Achse | Z_UP, belegt |

**Alle zehn ArchiCAD-Dateien waren IFC2X3.** Dieses Projekt hat bis heute ausschliesslich
gegen **IFC4 in Metern** geprüft — also gegen ein Format, das das verbreitetste
Autorenprogramm gar nicht liefert. In `seams`, im Runner und im MCP-Schema stand
durchgehend „IFC4", und das war keine Vereinfachung, sondern eine Behauptung über eine
Datei, die niemand gesehen hatte.

Die Logik war nie betroffen — IfcOpenShell liest beides. Aber die Testgeometrie konnte
nur IFC4, und was man nicht erzeugen kann, kann man nicht prüfen.

`tools/make_test_ifc.py` erzeugt jetzt **vier Kombinationen**: IFC4 und IFC2X3, je in
Metern und Millimetern. Der einzige Unterschied im Entitätensatz ist `IfcWall` — IFC4 hat
dort neun Attribute, IFC2X3 acht (kein `PredefinedType`). Ein Attribut zuviel machte die
Datei für einen strengen Leser ungültig.

**Alle vier durch den echten Konverter gemessen:** identische bbox, 8,0 × 5,0 × 3,25 m.
Und die ganze Kette bis zur Tiefenkarte auf der IFC2X3-Variante durchgelaufen —
`status: ok`, 6275 Geometriepixel, Soll-Karte lesbar.

**Die Millimeter-Variante skaliert die Koordinaten mit**, und das ist kein Detail: Wer nur
die Einheitenzeile umstellte und die Zahlen liesse, erzeugte einen **kaputten Export** —
genau den Fall, den `pruefe_einheit_gegen_masse` diagnostiziert und den zwei von 40
echten Dateien tatsächlich hatten.

### Und meine IFC2X3-Variante war kein gültiges IFC2X3

Ich hatte den Prüfer ausdrücklich gefragt, ob ich nur das eine Attribut angefasst habe,
das mir aufgefallen ist. Er hat nachgesehen — nicht aus dem Gedächtnis, sondern mit
`ifcopenshell_wrapper.schema_by_name(...).all_attributes()` für beide Schemata.

Die **Attributzahl** weicht tatsächlich nur bei `IfcWall` ab. Aber die **Optionalität**
weicht an fünf weiteren Stellen ab, und eine davon war verletzt:

```
IFC2X3:  IfcRoot.OwnerHistory : IfcOwnerHistory;            <- PFLICHT
IFC4:    IfcRoot.OwnerHistory : OPTIONAL IfcOwnerHistory;   <- optional
```

Die Fixture schrieb dort durchweg `$`. **Weil die Attributzahl gleich bleibt, fällt es
beim Zählen nicht auf** — und IfcOpenShell liest die Datei anstandslos. Erst
`ifcopenshell.validate` meldete **dreizehn** Fehler „Attribute not optional".

> Dass etwas gelesen wird, ist kein Beleg dafür, dass es gültig ist. Der Konverter war
> nachsichtig, der Prüfer nicht — und ein IFC-Leser beim Empfänger könnte es auch nicht
> sein. Das ist dieselbe Lehre wie beim Blender-Report und beim Cache-Eintrag, eine
> Ebene tiefer: **Nachsicht des Lesers ist keine Eigenschaft der Datei.**

Behoben mit einer echten `IfcOwnerHistory` samt `IfcPerson`, `IfcOrganization` und
`IfcApplication` — für **beide** Schemata, weil zwei Wege eine Abweichung wären, die
niemand bemerkt. Gemessen: **0 Meldungen** für IFC4 *und* IFC2X3, alle vier Kombinationen
weiterhin bei 8,0 × 5,0 × 3,25 m.

*Der Zeitstempel ist fest verdrahtet, nicht `time.time()`:* Die Fixture ist
deterministisch, und ein Zeitstempel wäre die einzige Stelle, an der zwei Läufe
auseinanderliefen.

---

## 19 · 40 echte IFC-Dateien — und die Erkennung war Glück, keine Eigenschaft

Der Auftrag `auf-20260818-08` liess den Connector zum ersten Mal auf echte Dateien los:
40 IFC aus Wettbewerbs- und Ausführungsprojekten. **Er hat gehalten** — 40 von 40
gedeutet, 40 von 40 Erzeuger erkannt, kein einziges `herkunft: null`, kein einziger
`HerkunftError`. Drei Erzeuger in freier Wildbahn: IfcOpenShell (28), ArchiCAD (10),
Revit (2). Schemas: IFC4 30-mal, IFC2X3 10-mal, und die zehn kamen alle aus ArchiCAD.
Zwei echte kaputte Exporte gefunden — Millimeter erklärt, Meter geliefert. Nicht
theoretisch: 2 von 40.

**Der eigentliche Befund liegt daneben.** In zwei von drei Fällen trägt Feld 5 des
`FILE_NAME`-Eintrags — `preprocessor_version` — einen *fremden* Namen: die
Exportbibliothek statt des Programms.

```
'DDS_IFC v3.0 | Graphisoft - Archicad - 28.4.0'      → ArchiCAD, aber Feld 5 sagt DDS_IFC
'ODA SDAI 24.3 | Autodesk Revit 24.1.0.66 (ENG)'     → Revit, aber Feld 5 sagt ODA SDAI
```

Erkannt wurde trotzdem alles — weil beide Felder in derselben zusammengeklebten
Zeichenkette standen und die Suche darin fündig wurde. **Das ist Glück und keine
Eigenschaft.** Hiesse eine Exportbibliothek einmal „Rhino-Export-Bridge", ergäbe Feld 5
eine *falsche* Herkunft. Und eine falsche Herkunft ist schlimmer als keine, weil sie eine
Up-Achse zur Bestätigung vorschlägt: Der Torwächter bekäme eine Zustimmung, wo er eine
Frage stellen müsste. Erkannt wird jetzt aus Feld 6 (`originating_system`) zuerst, Feld 5
nur als Rückfall; beide bleiben in der Auskunft.

**Eine Lehre über das Protokollieren selbst.** Das Ergebnisdokument des Auftrags sagt
unter `was_ich_geaendert_habe` wörtlich: *„nichts an aiimaging.herkunft — es hat
gehalten."* Der zugehörige Commit `6c6aa09` ändert `herkunft.py` um 79 Zeilen. Beides ist
richtig gemeint — die Ergebnisdatei entstand während des Laufs, die Änderung danach —,
aber wer nur die Ergebnisdatei liest, geht mit einer falschen Vorstellung heraus. Ich bin
selbst darauf hereingefallen und habe die Verbesserung ein zweites Mal begonnen, bevor
ich sah, dass sie längst dastand. *Ein Protokoll, das vor der letzten Änderung
geschrieben wird, ist ein Zwischenstand und kein Protokoll.*

---

## 20 · Der alte Add-on-Code existiert — 83 Module, nicht fünf

Der Owner hat zweimal gesagt, es gebe älteren KosmoVis-Code als Blender-Add-on mit
automatischer Kameraplatzierung. Beim ersten Nachsehen fanden sich **fünf Dateien**, und
ich stand kurz davor, „gibt es nicht mehr" zu melden.

Der Klon war **unvollständig ausgecheckt**: 5 Dateien im Arbeitsbaum, **314 in HEAD**,
alles im Index als gelöscht. Nach `git reset --hard HEAD` standen **83 Module mit rund
37 000 Zeilen** da. Die Spur, die daraufführte: Ein Skript importierte Module, die es
„nicht gab".

> **Die Existenz einer Datei ist kein Beleg für ihren Inhalt — und ihr Fehlen ist keiner
> für ihre Abwesenheit.** Dieselbe Lektion wie immer, nur andersherum.

### Was gefunden wurde

`archviz_pipeline_v6_camera_math.py` rechnet den Kameraabstand **analytisch aus dem
Bildwinkel**, nicht über einen Faktor: Ausdehnung, Deckungsgrad 0.55, Augenhöhe 1.70 m,
Blickziel bei einem Fünftel der Gebäudehöhe. Die schwierige Frage — passt das Gebäude ins
Bild — löst `ensure_bbox_in_frustum` über **alle acht Hüllbox-Ecken** mit perspektivischer
Division. **Kein `bl_info`, kein `register()`**: zwei der vier Funktionen sind reine
Rechnung. Regel 2 steht dem nicht im Weg.

Die zwölf Richtungen mit `bias_deg` als Regler für das Fassadenverhältnis (30° = 2/3 zu
1/3, 45° = Eckblick) sind **architektonisches Bildwissen, keine Programmierung**. So
etwas erfindet man nicht neu, man erbt es.

### Drei Befunde gegen die Erwartung

1. **Der Node-Tree hat keine Verdrahtungslogik.** Beim Aufbau wird `links.clear()`
   gerufen; `links.new` kommt in keinem der 83 Module vor. Die Knoten reden über
   Szenen-Eigenschaften als Seitenkanal. Der Tree ist eine Werkzeugpalette mit
   Reihenfolge, kein Graph — unsere `graph.py`/`kette.py` sind hier weiter. Zu erben
   gibt es die **vier Sockeltypen** und die Reihenfolge der fünf Ränge, nicht den Code.
2. **`camera_presets.json` liest niemand.** Über sieben Repos eine Erwähnung ausserhalb
   der schreibenden Datei, und die steht in einem Tutorial. Der Vertrag ist da, aber
   einseitig — und das Verfahren dahinter ist das schwächste im Bestand.
3. **Drei unvereinbare Kameraverträge und drei Augenhöhen** — `look_at` gegen
   `rotation_euler` gegen `azimut`/`elevation`; 1.70 absolut gegen 1.70 über `zmin` gegen
   1.65. Das ist keine Rundung, sondern eine andere Annahme über den Menschen im Bild.

Und der ehrlichste Punkt des Berichts: **Dass die zwölf Kameras registriert sind, ist kein
Beleg dafür, dass die zwölf Bilder je gut aussahen.** Ein Stress-Test belegt
Fehlerfreiheit, keinen Bildeindruck.

---

## 21 · Die Kamerarechnung, diesseits der Grenze — und an zwei Stellen besser

`src/aiimaging/kameras.py`, 87 Tests. Kein `bpy`; die Verdeckungsprüfung wird als Funktion
hereingereicht, damit auch die Schrittlogik des Heranziehens ohne Blender prüfbar bleibt.
**Diesen Schnitt macht der Bestand nicht** — dort steht `import bpy` im Modulkopf, und
damit ist die halbe Datei unprüfbar.

### Die drei Entscheidungen

* **`blick_auf` ist die führende Form**, nicht `rotation_euler`. Der Weg zurück ist nicht
  eindeutig, weil ein Blickvektor keine Zielentfernung kennt — wer die verlustbehaftete
  Form führt, gewinnt die andere nie zurück.
* **Die Augenhöhe ist absolut und misst 1.70 m.** Absolut, weil der Betrachter auf dem
  Gelände steht und nicht auf der Hüllbox-Unterkante; bei einem Gebäude mit Untergeschoss
  sind das zwei verschiedene Orte.
* **Der Bias bleibt bei 35°** als Vorgabe, mit 30/45 als benannte Enden.

### Zwei Stellen sind besser als die Vorlage

**Die sichtbare Breite.** Der Bestand nimmt `max(breite, tiefe, diagonale)`. Weil die
Diagonale eines Rechtecks *immer* mindestens so gross ist wie jede seiner Seiten, gewinnt
sie ausnahmslos — **das `max` ist toter Code**, und die Frontale steht auf
Diagonalabstand. Beim 30 × 30 m Kubus ist das Faktor 1,41 zu viel. Hier wird
richtungsabhängig gerechnet, `|dx·cos a| + |dy·sin a|`, und der Fehler verschwindet ganz.

**Die Schrittweite des Rückschubs.** Der Bestand tastet mit
`(überstehen − 1) · abstand · 0.6 + 3.0` und veranschlagt dafür zwanzig Durchläufe. Das
ist an beiden Enden schlecht: Der Faktor 0.6 dämpft auf 60 % des Nötigen, und bei einer
Kamera nahe der Fassade wird das Überstehen zweistellig, worauf derselbe Ausdruck einen
Sprung über Kilometer erzeugt. Der nötige Rückschub **lässt sich aber ausrechnen statt
tasten**: Ein Schub entlang der Blickachse erhöht die Tiefe jeder Ecke um genau diesen
Betrag und lässt ihre seitlichen Anteile unberührt, also folgt aus
`seitlich / (tiefe + Δ) ≤ grenze` unmittelbar `Δ ≥ seitlich / grenze − tiefe`. Ein
Durchlauf statt zwanzig.

**Die Schranke dazu steht als eigener Test**, weil sie sonst niemandem auffiele: *Zu weit
weg meldet der Eckentest nie.* Eine Kamera, die das Gebäude auf einen Fleck schrumpft,
passt bestens.

Ich bin bei zwei eigenen Testerwartungen danebengelegen, und beide Male hatte das Modul
recht. Der lehrreichere Fall: Beim Gartenhaus greift **nicht** der Mindestabstand, sondern
die Höhe. Wer auf 1,70 m steht, hat bei einem 2,5 m hohen Bau 2,2 m unter und 0,3 m über
dem Blick — massgebend ist der Weg nach unten. Das ist die Folge einer absoluten
Augenhöhe, keine Fehlfunktion, und steht jetzt als Test da.

---

## 22 · Ein Depth-ControlNet ist immer zwei Modelle — die Registry kannte nur eines

Die Suche nach einem Backbone mit echter ControlNet-Naht hat einen strukturellen Befund
mitgebracht: **Die Registry führt eine Lizenz je Eintrag, eine ControlNet-Naht besteht
aber aus zwei Repos mit zwei Lizenzen.** Damit war bei jedem Eintrag mit
`KOND_DEPTH_CONTROLNET` systematisch die halbe Naht geprüft — und die andere Hälfte galt
als geprüft, weil niemand sie vermisste.

Das ist nicht theoretisch: Bei FLUX sind **alle drei** verbreiteten Depth-ControlNets
selbst nicht-kommerziell lizenziert. Ein permissives FLUX-Basismodell hätte „zulässig"
ergeben, und die fertige Kette wäre trotzdem unverkäuflich gewesen. *Eine Naht ist so frei
wie ihr unfreiestes Glied.*

`Backbone` trägt jetzt `controlnet_id`, `controlnet_lizenz`, `controlnet_lizenz_quelle`.
Das Urteil über die zweite Hälfte ist **dreiwertig**: `True`, `False`, und `None` für
„nicht benannt, also nicht beurteilbar". **`None` ist nicht dasselbe wie `True`** — genau
diese Gleichsetzung war der Fehler.

**Eine Falle beim Ergänzen, gefangen von zwei Tests:** Meine erste Fassung *überschrieb*
die Begründung, wenn das ControlNet ablehnt. Bei `flux1-dev` verdeckte das den älteren und
schwereren Grund — das Basismodell selbst — und damit den Satz über die abgeleiteten
LoRAs, der beim Stil-Training zuschlägt. Zwei Gründe sind zwei Gründe.

### Zwei Fehler im Adapter, beide aus der Familie von `auf-09`

**Die Führung.** `_baue_parameter` kannte kein `guidance_scale`, also griff die Vorgabe
von `diffusers`. Für ein destilliertes Turbo-Modell ist die falsch — und beim richtigen
Wert 0.0 schaltet `diffusers` die klassifikatorfreie Führung ab, **womit der negative
Prompt wirkungslos wird**. Er stünde weiter im Protokoll und hätte nie ein Bild
beeinflusst.

**Die SDXL-Falle, und die ist das Spiegelbild von `auf-09`.**
`StableDiffusionXLControlNetPipeline` nennt ihr Steuerbild schlicht `image` und kennt
`control_image` nicht — dort heisst so das Ausgangsbild der Img2Img-Variante, *dieselbe
Zeichenkette mit anderer Bedeutung*. Der Adapter hätte daraufhin gemeldet, die
Konditionierung sei „Bildbearbeitung, nicht ControlNet": **eine tragende Naht als kaputt
gemeldet.** In `auf-09` wurde eine fehlende Naht für vorhanden gehalten; hier wäre es
andersherum gegangen.

> Beides kostet dasselbe — einen Menschen, der nachsieht. Und beim zweiten Mal sieht er
> nicht mehr nach.

Entschieden wird jetzt an der Pipeline selbst (`ist_controlnet_naht`): wer
`controlnet_conditioning_scale` annimmt oder ein `controlnet`-Attribut trägt, hat ein
ControlNet. Der Name entscheidet nicht — er hat sich in diesem Projekt schon zweimal
geirrt.

**Die Empfehlung** lautet `z-image-turbo` + `alibaba-pai/Z-Image-Turbo-Fun-Controlnet-Union`:
der einzige geprüfte Kandidat, bei dem *beide* Hälften Apache-2.0 sind — womit die
Regel-1-Spannung offen bleiben kann, statt stillschweigend zugunsten von „erlaubt genügt"
entschieden zu werden. 22,0 GiB, resident auf der Karte, 8 Schritte statt 148 Sekunden.
**Der Vorgabe-Backbone bleibt vorerst unverändert**: Die Empfehlung braucht eine Weiche in
`lade_modell`, und die gehört gemessen und nicht behauptet.

**Ehrliche Grenze:** Kein Kandidat wurde ausgeführt. Alles über die Naht steht auf
Signaturen im Quelltext von `diffusers v0.39.0` — belegt, nicht gemessen. Und die
Tiefenkonvention (nah = hell) ist an keinem ControlNet geprüft; ist sie invertiert,
erklärt das einen schlechten Score vollständig.

---

## 23 · Zum zweiten Mal an einem Tag: ein Änderungsverzeichnis, das Einträge behauptet

Beim Begleichen der Lexikon-Schuld habe ich vor dem Schreiben die dreizehn Begriffe
nachgezählt, die eine frühere Zeile desselben Tages als nachgetragen führte. **Zwölf
standen, einer nicht:** *tote Kante* stand nur in der Prosa eines anderen Eintrags, nie
als eigener.

Vorhin in dieser Sitzung war es *Leistungsgrenze* und *Rauchprobe*. Zweimal dieselbe
Ursache: **Der Verzeichniseintrag entsteht aus der Absicht, nicht aus dem Text.** Ein
Verzeichnis, das Lücken als geschlossen führt, macht sie unauffindbar — es ist schlimmer
als keines.

Die Gegenmassnahme ist billig und steht ab jetzt fest: *vor* dem Schreiben der Zeile jeden
genannten Begriff im Text nachzählen, nicht danach.

---

## 24 · Die Stil-Schwelle war kein strenges Gate und kein mildes — sie war gar keines

`auf-20260818-11`, 4950 Paare aus 100 zusammenhanglosen Bildern. **Der Boden von SigLIP 2
base liegt bei 0,526 ± 0,070.** Die Schwelle stand bei 0,30 — also **3,24 Streuungen
unter dem Boden**. Alle 4950 Paare bestanden, 100 Prozent; selbst das unähnlichste Paar
des Korpus lag mit 0,3097 darüber. Auch ohne die 5 % ähnlichsten Paare bleibt das Maximum
bei 0,647.

> **Ein Gate, das nie zugeht, ist gefährlicher als gar keines.** Es sieht aus wie Schutz,
> und niemand sucht dahinter.

Die Herkunft der Zahl ist damit geklärt: Der überlieferte Fehlbereich 0,06–0,13 stammt
aus den DINOv3-Läufen und liegt nicht einmal in der Nähe des SigLIP-2-Bodens — das
kleinste je gemessene Paar liegt bei 0,310. Er war der **Boden jenes Einbetters**, keine
Messung von Stilunähnlichkeit. Beim Wechsel des Einbetters in Sitzung 06 ist die Zahl
stillschweigend mitgewandert.

*Der Fehler war nie die Zahl, sondern dass sie einen Modellwechsel überlebt hat.*

Die Schwelle ist jetzt **abgeleitet statt gesetzt** (`Boden + k · Streuung`, k = 2 →
0,666), der Schlüssel ist Einbetter **und Ausleseort**, und `stil_gate` wirft, wenn eine
Schwelle unter dem gemessenen Boden liegt. Ein *unbekannter* Einbetter ist dagegen kein
Fehler, sondern ein Mangel in der Antwort — ein ungemessener Boden ist keine falsche
Schwelle, sondern eine unbekannte.

**Ehrlich zur Hälfte:** Der Boden ist gemessen, `k` ist gesetzt. Diese Messung sagt, wo
Unähnlichkeit *aufhört* — nicht, wo Ähnlichkeit *anfängt*.

**Die Tests hielten eine falsche Prämisse fest, nicht bloss eine alte Zahl.** „Getroffene
Bilder lagen im Vorläufer bei ~0,5–0,6" stand dort als Anforderung — das ist genau der
Bereich, in dem sich zwei *zusammenhanglose* Bilder befinden.

Und die Stilstudie hatte recht und hat sogar untertrieben: Sie sagte einen Kegelboden von
0,36 als ungünstigsten Fall voraus; gemessen sind 0,526. *Eine synthetische Studie kann
eine Zahl nicht widerlegen, aber sie kann sagen, woran sie hängt und wo zu messen ist.*

---

## 25 · Die Kette hat kein Signal mehr — und die Warnung zeigte auf die falsche Ursache

`auf-20260818-10`, eine Stunde GPU. Die Nullprobe durch die **ganze** Kette:

```
score 0.0333    spearman +0.0047    geom_iou 0.2380
```

Das ist nicht „weniger von den 0,90", sondern **kein Signal mehr**. 22 von 24 *gestörten*
Zeilen schneiden besser ab als die ungestörte Geometrie. Drei verschieden gestörte
Tiefenvorgaben (mittlere Abweichung 0,059 / 0,119 / 0,296 m, verschiedene Prüfsummen)
ergeben auf **zwölf Stellen denselben Score** — das Bildmodell hat drei verschiedene
Vorgaben in dasselbe Bild übersetzt.

### Die Messung, die der Auftrag nicht verlangt hat

Derselbe Schätzer auf **Blenders eigenem Beauty-Pass** — gleiche Szene, gleiche Kamera,
kein Bildmodell dazwischen:

```
spearman -0.990    geom_iou 0.261    score 0.509
```

**Der Schätzer ist es nicht.** Seine Tiefenordnung ist nahezu fehlerfrei. Die Kette
verliert an **zwei getrennten Stellen**: `geom_iou` deckelt schon beim perfekten Bild bei
0,261 (Score-Obergrenze 0,509), und das Bildmodell drückt |ρ| von 0,990 auf 0,005.

Nachgesehen statt vermutet: Nur **34 %** der ausgewählten Punkte liegen auf dem Bauwerk;
der Rest bildet einen Keil in der oberen rechten Bildecke, im leeren Hintergrund. Das ist
das Verhalten eines monokularen Schätzers, der auf Naturfotos trainiert wurde — in eine
flache, gleichmässige Fläche legt er eine Bodenebene hinein, die zur Bildecke hin auf die
Kamera zuläuft.

### Die Korrektur, die hier zählt

Die Warnung für dieses Muster nannte bis heute **eine** Ursache: „Muster einer erfundenen
Kubatur". Sie hätte an diesem perfekten Bild mit voller Sicherheit auf einen
Geometriefehler gezeigt, wo keiner war.

> Eine Warnung, die eine von zwei möglichen Ursachen als die einzige ausgibt, schickt
> jemanden an die falsche Stelle. Das kostet mehr als gar keine Warnung.

Sie nennt jetzt beide Ursachen, sagt, dass die Metrik sie nicht trennen kann, und sagt,
woran ein Mensch es kann: am Ort der überzähligen Punkte im Bild.

**Die Schwelle wird nicht gesenkt.** 0,65 ist durch die Kette unerreichbar, das Gate
sperrt derzeit alles — auch das Treue. Eine Schwelle an eine kaputte Kette anzupassen
hiesse aber, das Gate an das anzupassen, wogegen es schützen soll. Solange die Nullprobe
0,033 liefert, misst kein Wert etwas; die Schwelle ist derzeit weder Problem noch Lösung.

**Der Prompt war diesmal nicht die Ursache** — der Auftrag hat den bereinigten Prompt
ohne Bauteile übernommen. Damit ist ausgeschlossen, dass der `auf-09`-Befund (erfundenes
Dach) die niedrigen Scores erklärt.

---

## 26 · Registriert in Odysseus — und der erste fremde Blick fand unseren Fehler

Der MCP-Server ist registriert (`id d99fcf67`), alle drei Werkzeuge antworten. Der Weg
dorthin hat vier Dinge zutage gefördert; **eines davon ist ein echter Fehler bei uns**:

```
aiimaging_query_render {"job_id": "probe"}
  → Output validation error: None is not of type 'string'
```

Das `outputSchema` sagte `status: string` zu, die Antwort auf einen unbekannten Auftrag
liefert `null`. **Der naheliegende Flicken wäre falsch gewesen:** Ein Ersatzwert wie
`"unbekannt"` hätte das Schema erfüllt und eine Lüge eingetragen — `status` trägt die
Zustände des Auftrags-Automaten, und `satz_ist_freigegeben_laut_status` liest genau dieses
Feld, um über eine Freigabe zu entscheiden. *Nicht der Wert war falsch, sondern die Zusage
darüber.*

**Der Test war wichtiger als der Fix.** Aufgefallen ist das erst an einem fremden Cockpit:
Der glückliche Weg war geprüft, der Fehlerweg nicht — und Schemaverletzungen leben genau
dort. Eine Prüfung, die jedes Werkzeug gegen sein eigenes `outputSchema` hält, fand sofort
**drei weitere Fälle derselben Klasse**. Bemerkenswert am schlimmsten davon: Der Kommentar
an `_fehler` beschreibt die richtige Absicht wörtlich — „sonst sähe der Benutzer einen
Schemafehler statt der Ursache" — und genau das ist trotzdem passiert. *Die Absicht stand
im Kommentar, nicht im Schema.*

### Drei Befunde, die nicht bei uns liegen

1. **Registrieren genügt nicht.** Ein zur Laufzeit hinzugefügter Server meldet
   `connected: true, tool_count: 3`, erscheint in der Werkzeugliste — und ist bis zum
   Neustart unbenutzbar.
2. **Der qualifizierte Name trägt die Server-Kennung, nicht den Namen.**
   `mcp__aiimaging__…` wird mit `MCP server not connected: aiimaging` abgewiesen — eine
   Meldung, die auf einen toten Server deutet, während nur der Name falsch geformt ist.
3. **Alle drei Werkzeuge liegen hinter `require_admin`**, weil Odysseus nur vier eingebaute
   Server als lesend kennt (`_BUILTIN_SERVERS`). Auch unser rein lesendes
   `check_geometry` gilt dort als mutierend.

Und ein vierter, der die Demo-Planung betrifft: **`pipelineReadiness` kann für uns gar
nichts melden.** Die Synonymtabelle `FIELD_ALIAS_GROUPS` hat drei Gruppen, alle zu Flächen
und Ausnützungsziffer — **nichts zu Dateipfaden**. Ein `ifc_path` trifft also nur auf ein
exakt gleichnamiges Feld. Das bestätigt den Phase-0-Befund von der anderen Seite.

---

## 27 · Die Kameras hängen an Blender — und der echte Lauf fand drei Fehler in 87 grünen Tests

Der Runner stellte bis heute immer dieselbe Notkamera; sein eigener Kommentar sagte, die
zwölf Automatikkameras kämen später. Sie sind jetzt angeschlossen, über zwei Wege, und der
Bericht sagt, welcher gegriffen hat: fertige Koordinaten oder ein Richtungskürzel, das der
Runner aus der **dort** gemessenen Hüllbox ableitet. Das Kürzel ist der sichere Weg — ob
die Hüllbox aus dem IFC nach Export, Import und einer möglichen Z-up-Drehung noch dieselbe
ist, gehört gemessen und nicht angenommen.

**87 Tests am Modul waren grün, und die Szene rendert 2,4 % Tiefe.** Drei Fehler, die keine
Rechnung gezeigt hätte:

### Die Augenhöhe war absolut — und damit 400 m unter dem Erdgeschoss

Das ist die Korrektur einer Entscheidung, die ich in Kapitel 21 selbst getroffen und
begründet habe. Das Argument stimmte: Der Betrachter steht auf dem Gelände, nicht auf der
Hüllbox-Unterkante — bei einem Untergeschoss zwei verschiedene Orte. **Die Folgerung war
falsch.** „Absolut" heisst über `z = 0`, und das ist nur dort Gelände, wo das Modell
zufällig auf Meereshöhe sitzt. Ein Bauwerk mit Fuss auf 400 m über Meer bekam eine Kamera
auf 1,70 m.

> Beide Bezüge sind falsch, wenn man sie zum Naturgesetz macht. Richtig ist der
> Geländestand — und den kennt die Hüllbox nicht.

`gelaende_z` ist jetzt ein Parameter; ohne Angabe gilt die Unterkante. Im Zweifel eine
Geschosshöhe daneben statt vierhundert Meter. Der Untergeschoss-Einwand ist damit nicht
verschwunden, sondern **beantwortbar**: Die Bibliothek rät nicht, sie fragt.

### Das Blickziel lag über dem Dach

`augenhoehe + dz · 0.2` ergibt bei einem 2-m-Körper 2,1 m. Die Kamera schaute darüber
hinweg, der Rahmen war zur Hälfte Boden und Himmel. Das Ziel darf das Bauwerk nicht
verlassen. Für Gebäudemasse ändert die Schranke nichts. **Die Vorlage aus dem Bestand hat
dieselbe Lücke** — sie ist dort nie aufgefallen, weil nur echte Gebäude gerendert wurden.

### Zu klein fällt keiner Prüfung auf, die nur nach „passt es hinein" fragt

Der Eckentest meldete brav „alle acht Ecken im Bild", während das Bauwerk ein Fleck in der
Bildmitte war. `kamerasatz` rechnet jetzt den erreichten **Füllgrad** und warnt, wenn er
deutlich unter dem angeforderten Deckungsgrad liegt. *Ein Bild, auf dem das Bauwerk ein
Fleck ist, sieht wie ein Fehler des Bildmodells aus — die Ursache liegt in der Kamera, und
dort würde niemand suchen.*

Am Füllgrad selbst habe ich mich zweimal vertan, und beide Male hat eine Zahl es gezeigt:
zuerst nur die Breite gemessen (ein 30 × 30 × 20 m Kubus meldete 27 % bei 46 %
Höhenfüllung — eine Warnung für jedes Hochhaus), dann in der Gebäudemitte statt an der
nahen Fassade (ein 60-m-Riegel von der Schmalseite erschien winzig, obwohl seine
Stirnfassade den Rahmen füllt). An der nahen Fassade kommt jetzt über vier sehr
verschiedene Baukörper **genau der angeforderte Deckungsgrad von 0,55** heraus — das ist
die Probe darauf, dass es das richtige Mass ist.

Nebenbei belegt statt vermutet: `aiimaging` ist von Blenders eigenem Python aus erreichbar.
Der Rückfall hätte lautlos gegriffen, wenn nicht — darum steht der Weg als Zusicherung im
Bericht. Und der Rückfall selbst blieb unverändert: Meine erste Fassung hatte ihm die
Brennweite von 50 auf 28 mm verstellt und damit **jede daran kalibrierte Zahl rückwirkend
verschoben**. Ein Test hat es gefangen.

---

## 28 · Der geom_iou-Deckel ist gehoben — mit der Regel, die gemessen wurde

`auf-20260818-12` hat sechs Auswahlregeln gegeneinander gemessen, an Blenders eigenem
Beauty-Pass:

```
wie_soll (bisher)      geom_iou 0.256   Score 0.504    40.7 % auf dem Bauwerk
groesste_flaeche       geom_iou 0.000    n/a            0.0 %
ohne_randberuehrung    geom_iou 0.406   Score 0.635    99.2 %
nur_spearman_in_soll   geom_iou 1.000   Score 0.997   100.0 %
```

**Der Verlierer ist der lehrreiche.** `groesste_flaeche` — der naheliegendste Filter und
genau der, den ich ohne Messung gebaut hätte — trifft **null Prozent**. Die grösste
zusammenhängende Fläche der „nächsten n" *ist* der Hintergrundkeil. Eingebaut statt
gemessen wäre die Lage schlechter gewesen als vorher, und der Grund unauffindbar.

**Und der Sieger auf dem Papier ist nicht der Sieger.** `nur_spearman_in_soll` erreicht am
perfekten Bild 0,997 — aber auch 0,79 und 0,65 an den *gestörten* Karten, der kleinste
Abstand von allen sechs. Das ist die Sorte Verbesserung, die alles nach oben schiebt statt
zu trennen. `ohne_randberuehrung` gewinnt auf **beiden** Achsen und verweigert im gestörten
Fall das Urteil, statt eines zu erfinden.

Warum es wirkt: Der Fehler war nie der Schätzer, sondern die **Ortlosigkeit** der Auswahl.
`wie_soll` nimmt die n nächsten Punkte, gleich wo sie liegen. Die Randberührung ist genau
das Merkmal, das die hineingelegte Bodenebene vom freistehenden Baukörper unterscheidet.

**Vierer-Nachbarschaft, nicht Achter:** Mit Achter genügte ein einziger diagonaler Kontakt
zwischen Bauwerk und Bodenkeil, damit beide gemeinsam verworfen würden — die Regel wäre an
einer einzigen Pixelecke zerbrechlich.

**Die Vorgabe löst auf, statt zu erzwingen.** Die bessere Regel ist nicht immer *möglich*:
Ohne Breite und Höhe ist nicht entscheidbar, welcher Punkt am Rand liegt. `HG_VORGABE`
wählt sie, wenn die Masse bekannt sind, sonst `wie_soll` — mit der gewählten Regel im
Ergebnis und einer Warnung beim Rückfall. Eine ausdrückliche Wahl des Aufrufers wird nie
überstimmt, auch nicht zu seinem Besten: Sonst wäre die Vergleichsmessung selbst unmöglich,
die den Befund gebracht hat.

**Die Schwelle bleibt bei 0,65.** Der Deckel ist gehoben, nicht beseitigt — auch der beste
Kandidat erreicht am perfekten Bild nur 0,635.

---

## 29 · Ein Beleg für eine Fassung, die niemand installiert, ist kein Beleg

Die HomeStation hat die Fassungsbindung des MCP-SDK umgedreht: `pyproject` verlangte
`mcp>=2.0` — **genau die Fassung, unter der `baue_server()` nicht startet**, weil 2.0.0
`list_tools`/`call_tool` von `Server` entfernt hat. Jetzt `>=1.27,<2`.

Damit war aber ein Loch entstanden, das im Kommentar sogar benannt stand: *„Die
Lizenzprüfung galt der 2.0.0, der Code nie."* Die neue Spanne schliesst 2.0.0 aus — und
damit war **die einzige Fassung mit geprüfter Lizenz ausgeschlossen.**

> Dieselbe Fehlerart wie bei der Stil-Schwelle: Evidenz für das eine, angewendet auf das
> andere. Dort war es ein anderer Einbetter, hier eine andere Fassung.

Nachgeholt an den PyPI-Metadaten **beider Enden** der Spanne: `mcp 1.27.0` und `mcp 1.29.0`
tragen beide `license: MIT` und den Classifier `License :: OSI Approved :: MIT License`.

Zwei weitere Befunde der HomeStation aus derselben Runde verdienen es, hier zu stehen:

* **Sie hat denselben Schemafehler gefunden wie ich, unabhängig, und dieselbe Folgerung
  gezogen** — nicht der Code war falsch, sondern die Zusage. Zwei Wege zum selben Urteil
  sind ein besserer Beleg als einer.
* **Vier Tests massen die Umgebung statt den Code.** Sie sicherten zu, dass ein Import
  keinen GPU-Stack nachzieht, und prüften das an `sys.modules` — grün im Container, rot auf
  einer Maschine *mit* Stack. Also unprüfbar genau dort, wo die Zusicherung zählt. Sie
  messen jetzt in einem frischen Interpreter.

---

## 30 · Die Prompt-Bibliothek — sieben Kategorien, die kein Bauteil nennen

Der Owner wollte vorgefertigte Prompts nach Kategorien und ein freies Feld im Knoten,
und fragte, ob das an die HomeStation gehört. **Nein** — Prompts sind Text und Taxonomie,
das braucht keine GPU. Die HomeStation braucht es erst für die andere Frage: *ob* ein
Prompt an einem Backbone landet, und die ist vor einer tragenden ControlNet-Naht sinnlos.

Der Bestand hatte die Kategorien, nicht die Inhalte (`archviz_prompt_library.py`; die
JSON lag ausserhalb des Repos):

    vegetation, people, atmosphere, light_time, sky, material_detail, composition

**Keine davon nennt ein Bauteil.** Sie beschreiben ausnahmslos, was um das Gebäude herum
und auf seinen Oberflächen liegt. Das ist genau die Lehre aus `auf-09`, in Fächer
gegossen — *ein Prompt, der Bauteile nennt, die die Geometrie nicht hat, ist eine
Aufforderung zur Halluzination.* Die Einteilung wurde nicht erfunden, sondern erkannt.

Daraus die Arbeitsteilung: **Die Tiefenkarte sagt, WAS gebaut ist. Der Prompt sagt, WIE
es aussieht.** Es gibt keine Kategorie für Bauteile und wird keine geben.

Der `bauteilwaechter` **meldet und verbietet nicht**: Manchmal *hat* die Geometrie das
Genannte, und das kann ein Textmodul nicht wissen — ein Verbot wäre eine Behauptung über
eine Datei, die gar nicht vorliegt. Deutsch und englisch, weil das Eingabefeld keinen
Sprachzwang hat; Wortgrenzen statt Textsuche, sonst schlüge „towering trees" auf „tower"
an und der Wächter wäre in jedem zweiten Satz laut — und damit stumm.

Bewusst *nicht* drin: `masterpiece, best quality, 8k, trending on artstation`. Das ist
SD-1.5-Vokabular aus Schlagwortdatenbanken; die heutigen Modelle sind an natürlicher
Sprache trainiert, dort ist eine Adjektivkette wirkungslos oder ein Stilbefehl Richtung
Fantasy. Ein Test hält es fest.

**Drei Mängel im eigenen Entwurf**, und alle drei erst sichtbar, als die Prompts als
fertiger Text nebeneinander standen: Morgennebel verlangte Sonne *und* geschlossene
Wolkendecke *und* Nebel; Modellfoto und Einskizziert wiederholten sich selbst zwischen
Handschrift und Baustein (doppelt genannt = doppelt gewichtet). *Eine Bausteintabelle
liest sich anders als der Satz, der daraus entsteht.*

---

## 31 · Der Hausstil — Eigenschaften übernommen, kein Werk

Der Owner hat eine Richtung anhand von fünf veröffentlichten Wettbewerbsvisualisierungen
vorgegeben und dazu gesagt: *„es geht ja nicht ums Nachmachen, sondern wir entwickeln ja
unser eigenes, das sind Referenzen."* Genau so ist `kosmo_standard` gebaut.

Die gemeinsame Handschrift der fünf: bedecktes oder diesiges Licht — nie harte Sonne;
heller, fast weisser Himmel statt Blau; entsättigte Farben; niedriger Kontrast mit weicher
Lichterzeichnung, weder ausgefressen noch zugelaufen; **immer etwas im Vordergrund**, das
Bauwerk steht zurück und posiert nicht; kleine, beiläufige Figuren im Alltag; matte
Materialien; filmische Tonwerte.

**Drei Grenzen, von mir aus gezogen:**

1. **Die Bilder kommen nicht ins Repo.** Fremde urheberrechtlich geschützte Arbeiten. Sie
   ansehen und daraus eine Beschreibung ableiten ist Art Direction; sie speichern ist
   etwas anderes.
2. **Kein LoRA darauf**, auch nicht probeweise — das wäre ein abgeleitetes Werk, und das
   ist die Stelle, an der es später teuer statt nur unsauber wird. Steht als
   ausdrücklicher Riegel im Auftrag.
3. **Kein fremder Name im Code.** Bedecktes Licht, entsättigte Farben und kleine Figuren
   sind fotografische Konvention und gehören niemandem; ein Studioname als Produkt-Preset
   wäre eine Anmassung.

**Der Befund, der bis in den Code reicht: keine der fünf Vorlagen ist 16:9.** Alle sind
quadratisch oder hochformatig. Das ist keine Kosmetik — der vertikale Bildwinkel folgt aus
dem Seitenverhältnis (`kameras.bildwinkel`), und mit ihm der Abstand, aus dem ein Bauwerk
gerahmt wird. Ein hochformatiger Stil in einem 16:9-Rahmen gerechnet ergibt eine Kamera,
die zu weit weg steht. Ein Stil trägt darum jetzt ein Seitenverhältnis, und ein Test
reicht es bis in die Kamerarechnung durch.

`HAUS_STIL` und `MESS_STIL` sind **verschieden**, mit Absicht: Der Hausstil ist
ausdrücklich nicht messtauglich — sein Vordergrundbewuchs ist sein Zweck und genau das,
was die Silhouette verdeckt. *Gemessen wird auf dem Stil, der am wenigsten erfindet;
ausgeliefert der, der aussieht wie das Büro.*

**Und meine eigene Doppelungsregel hat mich erwischt, ohne dass der Test es sah:** Die
Handschrift des neuen Stils sagte „the building set back", der Bewuchsbaustein „the
building set back behind them" — der Test verglich aber nur gegen den
*Kompositions*baustein. *Eine Regel, die nur an einer Stelle prüft, ist keine Regel,
sondern eine Stichprobe.* Jetzt gegen alle sieben Fächer.

Die zweite Hälfte des Hausstils fehlt: **ein Referenzset, das uns gehört.** Der Prompt
sagt, wie man es macht; `stil_qa` prüft gegen ein Referenzset, ob es gelungen ist. Fremde
Bilder können das nicht sein — eine Einbettung ist eine Ableitung des Bildes. Damit ist
auch die Frage des Owners beantwortet, ob gelernter Hausstil oder festes Preset: **Es ist
dieselbe Mechanik.** Ein Preset ist ein mitgeliefertes Referenzset.

---

## 32 · Die Tiefenkarte ging verkehrt herum ins ControlNet

`auf-20260818-13` hat die Frage beantwortet, die im Auftrag **zuerst** stand, weil eine
falsche Antwort darauf alles andere wertlos gemacht hätte.

**Die Polarität war invertiert.** Unsere `tiefe_norm.png` schreibt nah = hell, das
ControlNet erwartet nah = dunkel:

```
mit unserer Karte     |spearman| 0.38 – 0.52
mit umgedrehter       |spearman| 0.79 – 0.85     bei JEDER Stärke rund das Doppelte
```

> Das ist ein Fehler bei uns, nicht am Modell — und die unangenehmste Sorte. Eine
> verkehrte Polarität erklärt einen schlechten Score **vollständig** und sieht dabei aus
> wie ein Problem des Bildmodells. Man sucht wochenlang an der falschen Stelle. Keine
> Modellkarte sagt die Konvention.

Sie ist jetzt ein **Feld** am Backbone, keine Annahme. Gedreht wird nur bei erklärter
Erwartung. Bei `unbekannt` wird **nicht** gedreht — raten hiesse, mit halber
Wahrscheinlichkeit die Geometrie zu spiegeln, und zwar lautlos —, aber es steht als
Hinweis im Ergebnis, **mit der Zahl**. Ein Hinweis ohne Zahl wird als Formalie gelesen.

### Der Backbone wechselt

```
qwen-image-edit-2511   spearman  +0.005   (auf-10, Nullprobe durch die ganze Kette)
z-image-turbo          spearman  -0.853   (auf-13, gleiche Bedingungen)
```

Dazu rund **hundertfach schneller** (1.4 s statt 150 s je Bild), 23.4 GiB resident, und
der Regler wirkt nachweislich — alle sechs Prüfsummen verschieden, wo Qwen dreimal
dasselbe Bild lieferte. Nicht monoton allerdings: **0.80 ist besser als 1.00.**

Gewechselt wird, obwohl der Score mit 0.265 weiterhin durchfällt. Er hängt an `geom_iou`,
und dessen Deckel ist nach `auf-12` eine Sache der Auswahlregel und nicht des Backbones.
*Einen Vorgabewert stehenzulassen, von dem gemessen ist, dass er die Geometrie gar nicht
überträgt, wäre die schlechtere Wahl* — auch wenn der Nachfolger die Schwelle noch nicht
reisst.

**Ein Befund, der die Erwartung umdreht:** Vorschaupfad und Vorgabe sind jetzt dasselbe
Modell. Der Vorschaupfad war als *Kompromiss* gedacht — klein und schnell, dafür weniger
treu. Gemessen ist es umgekehrt gekommen.

Die Geschichte eines einzigen Tests ist die Geschichte dieses Projekts: Er hiess
`…_und_depth_naht` und prüfte genau das; `auf-09` widerlegte es, worauf er
`…_ist_qwen_edit_und_apache` hiess und ausdrücklich festhielt, dass die Vorgabe **keine**
Naht hat — damit die alte Annahme nicht über einen anderen Test zurückkommt. Jetzt stimmt
der ursprüngliche Anspruch wieder, nur mit einem anderen Modell.

**Anerkennung, die ins Protokoll gehört:** Die HomeStation hat eine Querprobe mit der
`auf-12`-Auswahlregel **verworfen**, weil ihr Nachbau die Ausgabe des Moduls nicht traf —
*„ein Vergleich zwischen zwei Zahlen, von denen eine nicht die ist, für die ich sie halte,
ist wertlos."* Ein verworfenes Ergebnis, das als solches gemeldet wird, ist mehr wert als
eine Zahl, die niemand einordnen kann.

---

## 33 · Was offen blieb

- **Zwei Aufträge liegen bereit, keiner ist ausgeführt.** Ursprünglich war es einer;
  der lokale Arbeiter hat in einer Notiz zur HomeStation widersprochen, und zu Recht:
  *„Getrennte Aufträge sind hier besser als einer: Wenn drei Annahmen zugleich brechen,
  weiss niemand welche."* Daraufhin geteilt in
  - `auf-20260818-06` — **Regressionsprüfung** des Multipass auf 5.2, klein und ohne
    GPU. Sie fragt nur: Trägt die verschobene Naht auf der Fassung, auf der sie gebrochen
    ist? Wenn dort wieder 17,9336 / 26,6251 / 11 151 steht, ist sie sauber.
  - `auf-20260818-07` — der **erste echte Render**, ausdrücklich nachgelagert: ohne
    brauchbares `tiefe_norm.png` gibt es keine Konditionierung, und dann misst der Lauf
    nichts. Er bittet zusätzlich um zwei Wiederholungen mit `controlnet_staerke` 0,6 und
    1,0 — drei Punkte sind der Anfang der Schwellenstudie.
- **Eine Zahl fehlt vollständig: der VRAM-Bedarf des Bildmodells.** Die HomeStation-Notiz
  sagt, gleichzeitig resident passt genau **ein** 30B-Q4-Modell (18,6 GB) plus ein
  6-GB-Seher. Qwen-Image-Edit-2511 wäre eine dritte Last. Ob Bildmodell und Sprachmodell
  je gleichzeitig laufen können, entscheidet sich an dieser Messung — `auf-07` bittet
  ausdrücklich darum.
- **Blender 5.2 schreibt weiterhin nur Multilayer.** Das ist hingenommen, nicht gelöst;
  der stdlib-Leser kommt damit zurecht. Sollte eine spätere Fassung wieder einschichtig
  schreiben können, ändert sich nichts — die Weiche prüft Fähigkeiten, nicht
  Versionsnummern.
- **Die zweite Hälfte der Schwellenstudie ist gelaufen** (Kap. 25) — und hat die Frage
  nach der Schwelle vorerst gegenstandslos gemacht. Offen sind jetzt **zwei Dinge davor**:
  (a) der `geom_iou`-Deckel von 0,261 am perfekten Bild — er liegt in der
  Silhouettenauswahl, nicht im Schätzer, und keine der geprüften Hintergrund-Strategien
  hebt ihn; (b) ein Backbone, das die Geometrie überhaupt hält.
  **Was hier bewusst NICHT getan wurde:** eine neue Auswahlregel erfinden. Der Bericht
  benennt die Ursache (eine hineingelegte Bodenebene im leeren Hintergrund), aber ob ein
  Zusammenhangsfilter sie beseitigt, ist ungemessen — und eine geratene Regel wäre genau
  das, wogegen dieses Projekt sonst antritt.
- **Die Stil-Schwelle ist zur Hälfte kalibriert** (Kap. 24). Der Boden ist gemessen,
  ``k = 2`` ist gesetzt. Es fehlen Paare, die stilistisch **ähnlich sein sollen**, und ein
  menschliches Urteil darüber — ohne das sagt keine Messung, wo Ähnlichkeit *anfängt*.
- **Der Hausstil ist beschrieben, aber nicht prüfbar** (Kap. 31). Es fehlt ein
  **Referenzset, das uns gehört** — fremde Bilder können es nicht sein, eine Einbettung
  ist eine Ableitung des Bildes. Es braucht eigene Renders oder eigene Arbeiten des
  Owners. Erst danach sagt `stil_qa`, ob ein Bild den Hausstil trifft.
- **Fünf Behauptungen der Stilanalyse sind angesehen, nicht gemessen** — entsättigt,
  niedriger Kontrast, heller Himmel, feines Korn, nie 16:9. `auf-20260818-14` prüft sie;
  im Auftrag steht ausdrücklich, dass eine **nicht tragende** Behauptung das wertvollste
  Ergebnis wäre. Und ich habe aus **fünf** Bildern verallgemeinert — der Auftrag fragt,
  wie viele es insgesamt sind.
- **Welche der sechs Stile bleiben, ist Owner-Entscheid.** Die Form steht, der Geschmack
  fehlt.
- **Die Polarität von sechs Backbones ist ungemessen** (Kap. 32). Für jeden gilt: Ein
  schlechter Geometrie-Score kann allein daran liegen. Das Feld sagt es, aber es füllt
  sich nicht von selbst.
- **Z-Image ist an einer Szene mit einem Seed gemessen.** Dass es über verschiedene
  Baukörper trägt, ist nicht gezeigt — und der Score fällt weiterhin durch, weil der
  `geom_iou`-Deckel unabhängig vom Backbone ist. **Die beiden Baustellen dürfen nicht
  verwechselt werden**, sonst wird die eine für die andere gehalten.
- **Drei Befunde in Odysseus, die nicht bei uns liegen** (Kap. 26): der nötige Neustart
  nach der Registrierung, der qualifizierte Name mit Server-Kennung statt Namen, und
  `require_admin` auch für unser rein lesendes `check_geometry`. Alle drei gehören dem
  Owner gemeldet, keiner ist von hier aus behebbar.
- **Die Connector-Registry hat inzwischen 40 echte Dateien gesehen** (Kap. 19) — aber
  **alle aus einem einzigen Büro** und damit aus einer eingeschränkten
  Werkzeuglandschaft. Belegt sind IfcOpenShell, ArchiCAD und Revit. Dass **Rhino,
  Vectorworks oder Allplan** erkannt werden, ist weiterhin unbelegt; für diese drei gilt
  der alte Vorbehalt unverändert.
- **Der Owner-Entscheid zu Juggernaut-XL-v9** steht aus: Die Modellkarte untersagt den
  Einsatz „behind paid API services" ohne gesonderte Lizenz. Für einen lokalen
  Arbeitsplatz unerheblich, für einen entgeltlichen Renderdienst nicht. Der Backbone ist
  nur der Rückfall; Qwen-Image-Edit ist Apache-2.0 und davon unberührt.
- **Die Stil-Schwelle 0,30 ist von alledem unberührt** und weiterhin völlig ungeprüft —
  sie stammt zudem aus DINOv3-Läufen, und der Einbetter ist seit Sitzung 06 SigLIP 2.
- **Der Kameravertrag ist entschieden, aber an keinem Bild erprobt** (Kap. 21). Die zwölf
  Kameras rechnen sich und bestehen den Eckentest über fünf Gebäudetypen — dass die
  zwölf Bilder gut aussehen, ist damit **nicht** gezeigt. Genau das war schon der
  schwächste Punkt des Bestands, und wir haben ihn geerbt statt geschlossen. Es braucht
  einen Lauf durch Blender und ein Augenpaar.
- **Der Verdeckungstest hat kein Gegenstück im Runner.** `ziehe_bis_frei` nimmt die
  Sichtprüfung als Funktion entgegen; die Blender-Seite dieser Funktion — der
  Strahlenschuss gegen den Depsgraph — ist noch nicht geschrieben.
- **Frustum-Test und Verdeckungstest ziehen gegeneinander** — der eine schiebt weg, der
  andere holt heran. Im Bestand laufen sie nacheinander, nie in einer gemeinsamen
  Schleife, und ob das schwingt, ist dort nie geprüft worden. Bei uns ebenso wenig,
  solange der Runner-Teil fehlt.
- **Die ControlNet-Empfehlung ist nicht ausgeführt worden** (Kap. 22). `z-image-turbo` +
  Fun-ControlNet-Union steht auf Signaturen im Quelltext von `diffusers v0.39.0`, nicht
  auf einem Bild. Offen bleiben: die Weiche in `lade_modell`, die **Tiefenkonvention**
  (nah = hell — an keinem ControlNet geprüft; invertiert erklärte sie einen schlechten
  Score vollständig), und ob der Single-File-Konverter die 2.1-Fassungen versteht.
- **Drei Registry-Einträge haben ihre zweite Hälfte noch nicht**
  (`qwen-image-edit-2511` braucht keine; `flux2-*` sind ohnehin ausgeschlossen). Sie
  stehen als `UNVOLLSTÄNDIG` da, wo sie es sind — das ist die richtige Auskunft, aber
  keine Erledigung.
- **Die Stil- und Prompt-Inhalte des alten Bestands fehlen im Repo** (`archviz_styles/`,
  die Prompt-Bibliothek). Die Kategorien sind belegt, die Inhalte nicht. **Wenn die
  Renderstile der Demo aus diesem Bestand kommen sollen, muss der Owner sagen, wo diese
  Dateien liegen.**
- **Zwei Module des alten Bestands sind ungeöffnet** (zusammen über 3100 Zeilen,
  ComfyUI-Anbindung und KI-Pipeline) — erste Adresse, falls die ComfyUI-Frage aufkommt.
  Ebenfalls nicht ausgewertet: **KosmoOrbit**, wo die Oberfläche der Demo-Vision
  hingehört.
