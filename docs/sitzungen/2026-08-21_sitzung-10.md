# Sitzung 10 · 21.08.2026

*Auftrag des Owners: bis 22:00 selbständig weiterarbeiten, nach eigener Empfehlung.*

---

## 1 · Ein Tippfehler, der eine Datei erzeugte

Beim Nachsehen, ob `tools/make_test_ifc.py` einen Grössenschalter kennt, schrieb
`--help` eine IFC-Datei **namens `--help`** ins Arbeitsverzeichnis. Rückgabe 0, keine
Meldung: Das Skript nahm das erste Argument ungeprüft als Zielpfad.

Der schlimmere Fall ist nicht `--help`, sondern **`--gelände` mit Umlaut**. Der wäre als
Dateiname durchgegangen und hätte eine Szene **ohne** Gelände erzeugt, die aussieht wie
die gewollte — genau die Art Fehler, die eine Messreihe still verfälscht. Und er hätte
ausgerechnet den Auftrag getroffen, der an diesem Abend hinausging.

Behoben: Unbekannte Schalter enden mit Rückgabe 2, `--help` nennt den Gebrauch, beide
schreiben nichts. Geprüft wird an der **ausbleibenden Datei**, nicht am Text der Meldung.

## 2 · Die Wache hängt am Abholer

Sie lag seit dem 20.08. da und war an nichts angeschlossen. Warum das nicht trivial war:
`verarbeite(auftrag)` **blockiert**. Zwischen Aufruf und Rückkehr hat der Abholer keinen
Moment, in dem er von sich aus nachsehen könnte — also beobachtet ein Faden nebenher,
dieselbe Bauform wie der Herzschlag im Blender-Runner und aus demselben Grund.

Drei Entscheidungen, die nicht selbstverständlich sind:

* **Der schlimmste Befund zählt, nicht der letzte.** Ein Lauf, der zwanzig Minuten stand
  und sich dann fing, *hat gestanden*. Nähme der Bericht den letzten Blick, löschte die
  Rettung in letzter Sekunde genau die Beobachtung, für die das Ganze gebaut ist.
* **`bei_stillstand` wird einmal je Ereignis gerufen**, nicht bei jedem Blick. Eine halbe
  Stunde Stillstand im Zweisekundentakt wären sonst neunhundert Rufe für eine Nachricht.
* **Keine Wache heisst „nicht beobachtet", nicht „lief durch".** Ein unbeobachteter Lauf
  bekommt keine Null in die `timings` — eine Null hiesse „stand nie" und wäre eine
  Behauptung ohne Beleg.

Der längste Stillstand wandert nach `timings.stillstand_s`, einem **Vertragsfeld**, das
`nur_vertragsfelder` übersteht. Wer in der fremden Oberfläche wissen will, warum ein
Auftrag eine halbe Stunde brauchte, findet die Antwort dort, wo er ohnehin nachsieht.

**Korrigierte Fehlannahme (eigene, im Test):** Der erste Anlauf hatte die
Wettlaufsituation im Test selbst — die künstliche Uhr wurde von aussen gestellt, während
der Beobachtungsfaden sie las, und wer zuerst drankam, entschied über das Ergebnis. Zwei
Tests fielen unregelmässig durch. Die Abhilfe war **nicht** längeres Warten, sondern das
Rennen bedeutungslos zu machen: eine Uhr, die bei jedem Blick von selbst weiterspringt.

**Ungeprüft geblieben:** ob eine Wache an einem echten Renderlauf das Richtige sieht.
Alle 26 Tests laufen gegen Verzeichnisse, in die der Test selbst schreibt. `FRIST_S = 300`
ist aus dem Altbestand übernommen und ausdrücklich **nicht** gemessen — steht als eigener
Punkt im PLAN.

## 3 · Der Befund des Abends: meine Normierung ist widerlegt

`auf-20260820-23` kam zurück, während hier gearbeitet wurde. Die Antwort auf die Frage,
die ich am Vorabend gestellt hatte, ist **nein**, und zwar aus zwei unabhängigen
Richtungen:

1. Die beiden Anteilskurven liegen **nicht** aufeinander. 1 m Versatz ergibt bei 29 %
   Geometrieanteil rund 0.40 der Spanne, bei 59.8 % rund 0.92 — Faktor 2,3.
2. Härter noch: Der Score ist **nicht monoton**. 2 m Versatz gaben 0.1191, 4 m gaben
   0.2301. Mehr Fehler, besserer Score.

Der zweite Punkt trifft tiefer, denn er ist durch keine Normierung zu heilen: Eine
nicht-monotone Grösse lässt sich nicht in ein Mass für Abstand vom Richtigen verwandeln,
und der Anteil der Spanne erbt den Knick unverändert.

**Die Ursache ist geometrisch und war die ganze Zeit dokumentiert — als *benannter Preis*,
nicht als Fehler.** `abs()` faltet die Skala in der Mitte: Der schlechteste Wert liegt
danach bei ρ = 0, mitten im Fehlerbereich, und beide Enden sind gleich gut. Eine Grösse
mit einem Minimum in der Mitte kann nicht monoton sein. Der Preis war höher, als der Text
vermuten liess.

## 4 · Die Richtung ist eingebaut — und repariert einen von drei Knicken

Der Ausweg kam von der HomeStation: Die Umkehrung gehört zum **Schätzer**, nicht zum
Bild. Umgesetzt als `geometrie_score(..., polaritaet=...)` plus
`polaritaet_aus_messungen`.

Die Sorgfalt steckt in einer Auflage: Bestimmt werden darf sie **nur an Läufen bekannt
guter Geometrie**. Aus einem fraglichen Lauf bestimmte man die Polarität aus genau dem
Fehler, den sie später fangen soll. Aus demselben Grund genügt ein einzelner Lauf nicht,
und uneinige Läufe bestimmen gar nichts. Das Lexikon trug bereits den Einwand *„nie aus
den Daten erraten"* — diese Auflage ist die Antwort darauf, und beides steht jetzt
zusammen in einem Eintrag.

**Der Gewinn, den niemand gesucht hat:** Eine vollständig invertierte Tiefenkarte
erreichte mit `abs()` den Score 1.0 und **bestand das Tor**. Vorne und hinten vertauscht
ist der grösstmögliche Geometriefehler und sah aus wie der kleinste. Jetzt fällt sie auf
0.0. Das war kein Genauigkeits-, sondern ein Sicherheitsloch.

**Was es nicht löst**, hier nachgerechnet (`docs/POLARITAET_2026-08-21.md`, keine neue
Messung): Von drei Knicken verschwindet **einer**. Die beiden anderen sitzen in `geom_iou`
und nicht in ρ — in Szene B fällt ρ sauber, während die Silhouettenüberdeckung *steigt*.
Und die Stumpfheit bleibt vollständig: Kein Lauf der Szene B fällt unter den Rauschanker,
vorher wie nachher. **Wer die Richtung für eine Antwort auf die Stumpfheit hält, hält ein
Vorzeichen für eine Empfindlichkeit.**

## 5 · Was offen blieb

* **Der neue Verdächtige heisst `geom_iou`, der Verdacht heisst Boden.** Bei 59.8 %
  Geometrieanteil ist über die Hälfte aller Punkte Bodenebene; ein verschobenes Gebäude
  ändert daran fast nichts, und die Silhouetten decken sich weiter, weil sie sich *im
  Boden* decken. Passt zur Nullprobe (Rauschen: 0.7217 auf derselben Szene). **Vermutung,
  nicht Messung** — `auf-20260821-24` prüft sie.
* **Die Polarität der Nullproben ist ungemessen** und entscheidet mit: Liefert weisses
  Rauschen ein positives ρ, fällt der Rauschanker mit der Richtung auf 0 und das Tor
  trennte plötzlich sauber. Dieselbe Zahl ist längst gerechnet, nur nicht gemeldet worden.
* **Welche Frist trägt am echten Lauf?** Die Wache hängt, ihre Frist ist geraten.
* Unverändert offen: die Owner-Entscheide (Freigabe-Token der Brücke, Format gegen
  Vordergrund, Hausstil), die Antwort auf das Übergabeblatt.

---

# Zweiter Teil des Abends

*Nach dem ersten Abschluss ging es weiter. Der Owner hat vier Entscheide getroffen, eine
Fotografierecherche beauftragt und Agenten freigegeben.*

## 6 · Vier Owner-Entscheide

| Frage | Entscheid |
|---|---|
| Gilt der Freigabe-Token der Brücke? | **Nein** — `--fremde-freigabe` bleibt aus |
| Hausstil gelernt oder fest? | **Fest formuliert**, ohne Referenzset |
| Format anpassen oder Vordergrund füllen? | **Vordergrund füllen** |
| Metrik-Umbau jetzt oder nach der Messung? | **Jetzt, additiv** |

Zwei Nebenwirkungen gehören festgehalten. Der Stil-Entscheid **erledigt eine Rechtsfrage,
statt sie zu beantworten**: Ohne Referenzset werden die 74 fremden Bilder nicht gebraucht.
Und der Format-Entscheid ging gegen meine Empfehlung — mit der Folge, dass bodenlastige
Szenen zum Regelfall werden und **die Bauwerksmaske damit von einer Verbesserung zur
Voraussetzung wird**. Die Entscheidung bestimmt die Technik, nicht umgekehrt.

## 7 · Der Maskenweg, additiv gebaut

Zwei Agenten, beide mit Mutationsprobe. `maske.py` (Bauwerksmaske aus dem
Material-ID-Pass) und `geometrie_qa.rho_ueber_maske`. Kein Score, kein Gate — ein Score
aus ρ allein hätte die Antwort auf `auf-25` vorweggenommen, und die lautete dann nein.

**Die heikelste Entscheidung ist die Geländeregel.** Kein Stern an `gelaende`, weil
`gelaende*` **Geländer** fangen würde: ein Handlauf, der lautlos zum Boden erklärt wird.
Und: Greift die Regel nicht, ist die Maske `None` und nicht „Maske plus Warnung" — aus dem
Bild ist *nicht entscheidbar*, ob die Regel danebengriff oder ob es keinen Boden gibt, und
eine Warnung in einer Liste kann man überlesen.

**Nebenbefund, der jede künftige Mutationsprobe trifft:** Zwei Mutationen waren byteweise
gleich lang wie das Original. Pythons Bytecode-Cache prüft nur mtime (sekundengenau) und
Grösse — nach dem Zurückschreiben in derselben Sekunde lief der Test gegen die *mutierte*
`.pyc` und blieb rot, obwohl die Quelle wiederhergestellt war.

## 8 · Das Tor ist offen, und das ist der härteste Befund des Tages

Kette über drei Aufträge:

* **`auf-25`:** ρ über der Maske ist erpressbar. Ein leeres Grundstück (−0.686) schlägt
  weisses Rauschen (−0.521). H1 und H2 unterscheiden sich um 0.0007 — ob das Bauwerk fehlt
  oder 20 m weiter steht, ist an den Maskenpunkten dasselbe.
* **Meine eigene Lücke:** Der Schluss „`geom_iou` darf nicht fallen" ruhte auf einer
  ungeprüften Annahme — weil *ich* angeordnet hatte, ausschliesslich ρ zu messen.
* **`auf-26`:** `geom_iou` fängt die Abwesenheit nicht, es **belohnt** sie. Leeres
  Grundstück 0.9848 gegen 0.9703 beim perfekten Bild, Score 0.9530, Gate bestanden. Das
  Bauwerk war die einzige Stelle, an der Soll und Ist sich unterscheiden konnten.

**Weder ρ noch `geom_iou` erkennt ein Bild ohne Bauwerk.** Übrig ist ein einziger
ungeprüfter Kandidat — die Tiefenkante an der Maskengrenze (`auf-27`).

## 9 · Die Fotografierecherche, und was sie an uns korrigiert

Drei Agenten mit Websuche, je mit der Auflage, belegt von behauptet von gefolgert zu
trennen und zu melden, was sie **nicht** finden.

**Der Kernbefund trifft unseren Code.** `ZIEL_ANTEIL_HOEHE = 0.20` kippt die Kamera um
9,46° — unabhängig von der Gebäudehöhe — und lässt die Vertikalen um rund 9 bis 12 % konvergieren
(bei waagrechter Kamera exakt null). Der Kommentar dort nennt das „den üblichen Griff der Architekturfotografie";
**es ist die Umkehrung der Wahrheit.** Parallele Vertikalen entstehen durch eine lotrechte
Bildebene, HABS/NPS schreibt die Korrektur *bei der Aufnahme* zwingend vor, und der übliche
Griff ist waagrecht halten und shiften. Es ist die einzige institutionell verbindliche
Regel des Fachs, und wir verletzen sie in jedem Bild.

**Und sie verbindet zwei Befunde, die ich für unabhängig hielt.** Eine waagrechte Kamera
ohne Shift legt den Horizont exakt in die Bildmitte: 50 % Boden, per Shift abwärts bis
~4 %. Über 50 % kommt man nur durch Kippen. Unsere Versuchsszene hatte **59,8 %** — und ist
genau die, an der die Metrik zusammenbrach. Die Szenen, an denen die QA versagte, waren
fotografisch nicht gültig. Das entlastet die Metrik nicht, sagt aber, wo zuerst zu
reparieren ist: **an der Kamera, nicht an der Metrik.**

**Zwei Korrekturen an mir selbst:**

1. Ich habe dem Owner Format-oder-Vordergrund als Alternative vorgelegt. Die Praxis kennt
   einen dritten und üblicheren Weg: den **Standort**. Ein 8:3-Bau projiziert sich schräg
   nicht mehr als 8:3.
2. „Vordergrund füllen" ist bei uns **behauptet, nicht umgesetzt**. Vordergrund mit Inhalt
   ist belegt; leerer Boden wird in keiner Quelle als Mittel genannt, sondern als Fehler.
   Unsere Szenen haben nichts, was als Vordergrund taugte.

**Was sich nicht in Zahlen fassen lässt — und das ist ein Ergebnis, keine Lücke:**
Bildpositionsregeln der Form „Bauteil X gehört an Bildstelle Y" gibt es in der
Fachliteratur praktisch nicht. Zur **Stütze**, dem Beispiel des Owners, findet sich keine
einzige Positionsaussage. Belegt sind zwei Konventionen: mittiger Fluchtpunkt innen, und
die Horizontlinie aussen (die nicht gewählt wird, sondern aus der waagrechten Kamera
folgt). Die 2/3-Regel des Owners ist damit eine **Projektsetzung** — verträglich,
unwidersprochen, nicht belegt.

**Nachgerechnet statt geglaubt:** Boden und Decke bekommen exakt gleich viel Bildfläche,
wenn die Kamera auf halber Raumhöhe steht — unabhängig von Brennweite und Abstand (24/35/50
mm, 3/5/8 m, Differenz jedes Mal 0,000000).

**Die grösste Lücke der Recherche, und sie trifft die schriftliche Arbeit:** Die Fachbücher
(Schulz, McGrath, Heinrich) waren nicht im Volltext erreichbar. Alles Belegte stützt sich
auf Normen, Behördenvorschriften, begutachtete Studien und Geometrie — nicht auf die
Lehrbücher des Fachs. Das ist in einer Bibliothek nachzuholen.

## 10 · Eigene Fehler dieses Abends

* **`git checkout` auf eine Mutationsprobe** hat meine eigenen uncommitteten Änderungen an
  `abholen.py` gelöscht. Wiederhergestellt; danach mit einer Kopie im Scratchpad gearbeitet
  und die Auflage an alle Agenten weitergegeben.
* **Eine falsche Alternative** in einer Owner-Frage (siehe 9).
* **Eine Auftragsvorgabe, die die entscheidende Zahl ausschloss** (siehe 8).
* **Eine falsch gerechnete Konvergenz, in vier Dateien veroeffentlicht.** Meine Formel
  benutzte die radiale Entfernung statt der senkrechten zur Bildebene und enthielt die
  Neigung gar nicht - sie lieferte 11,8-21,8 % auch bei **null Grad**, wo die Konvergenz
  zwingend exakt null ist. Aufgefallen ist es dem Agenten, der das Modul baute und meine
  Zahl nicht reproduzieren konnte. Richtig sind rund 9-12 %. **Der Befund stand, die Zahl
  nicht** - und ich hatte sie dem Owner zweimal genannt.
* **"Ueber 50 % Bodenanteil geht nur durch Kippen" ist geometrisch falsch.** Shift wirkt in
  beide Richtungen. Richtig ist der schwaechere Satz: kein gesichteter Text beschreibt einen
  Shift nach unten - nicht unmoeglich, sondern unbeschrieben.

## 11 · Was offen blieb

* **`auf-27`** — die Tiefenkante, der letzte Kandidat für eine Anwesenheitsprüfung.
* **`kameras.py` auf waagrechte Kamera plus Shift umstellen.** Ändert **jede** bisher
  gemessene Aufnahme; nichts, was nebenbei passiert.
* **Es gibt weiterhin gar keinen Innenraum-Modus.** `WANDABSTAND_M = 10.0` macht eine
  Innenaufnahme rechnerisch unmöglich.
* **`komposition.py`** — vom Agenten geschrieben, **ohne Tests**, darum nicht committet.
* Die Stil-QA im Abholer gegen den Belichtungsrahmen statt gegen Einbettungen (neu möglich
  durch den Stil-Entscheid).

---

*Suite am Ende des Abends: 2608 grün. Vakuumprobe: 6 Treffer, unverändert.*

---

*Suite am Ende des ersten Teils: 2521 grün (von 2479 zu Sitzungsbeginn). Vakuumprobe: 6 Treffer,
unverändert. Kein GPU-Lauf hier; alle Messwerte dieser Sitzung stammen von der
HomeStation oder sind Nachrechnungen ihrer Zahlen.*
