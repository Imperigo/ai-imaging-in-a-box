# Sitzung 70 — 23.09.2026, Runde 11: Altposten abtragen, und die erste Antwort zur Tiefe

> **Entschieden:** Ein Auftrag mit Token geht durch **dieselbe** Freigabeprüfung wie jede
> Freigabe (die zweite Tür ist zu); der Abholer glaubt einem «queued» nicht mehr blind,
> sobald ein Tokenbuch liegt. Zwillinge stehen im Urteil. Die iPad-App liefert ihre
> Schriften mit. `pruefe_verdrahtbarkeit` bleibt Wächter, nicht Produktweg (Grund an der
> Funktion). Das neuere ControlNet Union-2.1 ist Apache-2.0 und darf gemessen werden.
> **Gemessen:** HomeStation, `auf-154`: Der Abstand zum Hintergrund hebt geom_iou von 0,35 auf
> 0,92 — **im Endbild**, per Kreuzprobe belegt; bestanden hat kein Lauf. 17 + 1
> Mutationsproben der Runde, alle fallen.
> **Offen:** Ob der Abstand über Startwerte und Körper trägt und ob Union-2.1 besser steuert
> (`auf-158`); die Führung des Edit-Modells (`auf-157`); zwei Entscheide aus `auf-156`
> (beim Owner).

**Grundlage:** `jobs`, `werkzeuge`, `eigene_quelle`, `mcp_schemas`, `contracts`, `abholer`,
`ipad/Visbox.swiftpm`, `docs/PLAN.md`
**Codestand:** Runde 11 mit diesem Protokoll

---

## 1 · Der Auftrag

Owner: «weiter». Bei den Workern lag alles, was sie brauchten; bei uns standen im PLAN 19
ungehakte Kern-Posten aus den Sitzungen 55–69. Eine Prüfung am heutigen Code (ein
Explore-Agent, jeder Posten mit Datei und Zeile) ergab: **elf erledigt, aber nie abgehakt**,
vier halb, vier offen — darunter eine Lücke am Freigabe-Tor.

*Ein Plan, in dem Erledigtes offen steht, versteckt das wirklich Offene.*

## 2 · Abgehakt, mit Beleg

`--im-heimnetz` (`a64bea3`), `homeworker` «unbekannt» (Sitzung 61/65), Sonne/Ausschnitt/
Deckungsgrad/Richtung, Innenansicht über die Mappe und C8/C10-Vertrag (`2c7a081`), Runde-7-
Posten (`a785458`), iPad findet die HomeStation (Entscheid 27, `rundruf.py`), zwei iPads
(Standnummer, `ProjektKollision`). C8/C10 im Einbau-Stand bleiben **bewusst** «unbestätigt»:
Bestätigt war unsere Anzeige, nicht der Vertrag (`auf-142`). `ENTSCHEIDE_IPAD` nachgeführt.

## 3 · Zwei Messdokumente berichtigt

* **R3** behauptete seit dem 19.09., `rho_maske` habe ein *brauchbares* Bild wie Müll
  behandelt. Die Antwort auf `auf-115` (21.09.) sagt das Gegenteil: Das Bild folgte der
  **fremden** Karte besser als der eigenen (−0,155 gegen +0,300). `rho_maske` hatte recht,
  die Schwelle bleibt. Paarweise trennt `geom_iou` auch bei 0,75 in 12 von 12 Fällen.
* **INNENANSICHT** zeigte noch auf `auf-89` als offene Frage; beantwortet: über Eck steht ρ
  fest, frontal wandert es mit dem Startwert um bis zu 0,94 — keine Messung.
* Beide neu nachgesehen; die Innenstudie stellengleich nachgerechnet; `tools/beruehrung.py`
  meldet nichts.

## 4 · Runde 11 — drei Einheiten

| Einheit | Befund | Jetzt | Durchsicht |
|---|---|---|---|
| Einlass | `baue_job` stellte einen Auftrag allein nach der **Form** des Tokens auf «queued» — am Tokenbuch vorbei; `eigene_quelle` glaubte dem Statuswort. Die Geometriefelder standen **viermal** im Code. | Eine Tür (`_freigabe_vollziehen`), ein Schalter (`jobs.FREIGABE_MIT_BUCH`, aus), Abdruck im Auftrag, Buchprüfung im Abholer sobald ein Buch liegt; `contracts.LANE_FIELDS` als einzige Quelle. | **taugt nicht** → selbst behoben (§5) |
| Zwilling | `verdict.reason` schwieg über Zwillinge (sie erbten `bild_png` und wurden übersprungen). | Vorn im Urteil: «Nicht neu gerendert (identische Soll-Karte), nNW». Sonst bitgleich. | taugt |
| Schrift | Entschieden (Nr. 20, 25), nicht gebaut. | IBM Plex Sans (variabel), Plex Mono (3 Schnitte), Instrument Serif — unverändert aus google/fonts, SHA-256 im `NOTICE`, Ressource im Manifest, Registrieren beim ersten Gebrauch mit Rückfall. Lizenztexte `<Familie>-OFL.txt` (drei gleichnamige bricht SwiftPM ab). | taugt |

## 5 · Durchsicht, selbst nachgezogen

* **Die Reihenfolge an der Tür:** Warf das Entwerten des Tokens (Wettlauf, Buch unlesbar),
  stand der Auftrag schon auf «queued» — mit der Meldung «abgewiesen». Jetzt wird zuerst
  entwertet, dann der Satz angefasst. Wächter + Mutationsprobe.
* **Ein Wächter behauptete zu viel:** «Ein in `LANE_FIELDS` ergänztes Feld wird angenommen» —
  gelesen ja, im Auftrag nicht. Docstring und Test sagen jetzt genau das.
* `tools/beweis/07` beschrieb die alte Tür; der Entscheid zu `pruefe_verdrahtbarkeit` steht
  jetzt an der Funktion; ein Kommentar im Abholer nannte eine Zeile bewacht, die es nicht ist.
* **iPad:** `LIESMICH` versprach die Systemschrift auch für zwei Fälle, in denen die App gar
  nicht übersetzt (Playgrounds ohne `Bundle.module`, Playgrounds schreibt das Manifest neu).
  Berichtigt; Abnahmeblatt A4/A5 neu.

## 6 · Antworten der HomeStation

**`auf-154` (Tiefe steuert das Bild?)** — sechs Läufe, je zweimal, stellengleich:

| Lauf | Bedingung | score | geom_iou | Bild |
|---|---|---|---|---|
| B1 | mit Bodenplatte | — | 0,00 | nicht messbar: Platte weitet den Ausschnitt, Bauwerk 2 % |
| B2 | ohne Boden | 0,450 | 0,347 | Fassade an der richtigen Stelle |
| **B3** | **+ Abstand 0,15** | **0,534** | **0,918** | wie B2, fürs Messgerät deutlich anders |
| B4 | nicht umgedreht | 0,296 | 0,881 | nur eine gemusterte Fläche |
| B5 | 256 px | — | 0,00 | nichts |
| B6 | Stärke 0,8 | 0,156 | 0,203 | überladen |

*Lesart:* Der Boden war nicht der Grund (er verändert den Ausschnitt), die Polarität stimmt,
Pixel und Stärke tragen. Der Abstand hilft — **im Bild**, nicht in der Messung (Kreuzprobe).
Bestanden hat keiner. Teil C: Die Inpaint-Klasse verlangt ein neueres ControlNet (Union
2.0/2.1); **Lizenz von uns geprüft: Apache-2.0** (Modellkarte, 23.09.2026).

**Korrigierte Fehlannahme:** «Alle früheren guten Läufe hatten einen Boden, also trägt der
Boden.» Die Platte veränderte vor allem den **Ausschnitt** — der Vergleich war keiner.

**`auf-156`** (von einer anderen Sitzung abgelegt): Beide Klone hielten an der STOPP-Regel.
Zwei Entscheide liegen beim Owner — die ungesicherte Kennwort-Änderung im KosmoPrepare-Klon
(auf `origin/main` steht weiter ein festes Kennwort als Vorgabe) und die nächtlichen
Bildtest-Commits auf einem Nebenzweig von KosmoPublish. **Nicht unser Repo** — gemeldet,
nicht angefasst.

## 7 · Verteilt

* **local — `auf-20260923-158` (Rang 1):** Ansage Runde 11 (Tor, Zwilling, Messschalter);
  Teil B: Abstand {—, 0,05, 0,15, 0,30} × Quader/Hochbau × drei Startwerte; Teil C: **eine**
  Datei Union-2.1-2602-8steps laden, 1.0 gegen 2.1 an zwei Karten, Inpaint-Probe.
* **local — `auf-20260923-157` (Rang 2):** Welche Führung will das Edit-Modell? Vermutlich
  heisst der Regler dort `true_cfg_scale`, und den setzen wir nirgends. Nur lesen.
* **cloud — Nachtrag `auf-155`:** der Zwillingssatz, vorher/nachher wörtlich, Frage F4;
  Block vom 23.09. nachgeführt.
* **ui — Nachtrag `auf-99`:** Ort des Tokenbuchs, Name des Schalters, die geschlossene Tür;
  Block nachgeführt.

## 8 · Offen am Ende der Sitzung

* **Kern (klein):** `enqueue_render`/`query_render` geben die `meldung` eines abgewiesenen
  Tokens nicht zurück — vor dem Einschalten der Buchprüfung bauen. Im Abbruchfall steht in
  `verdict.reason` «… ein Lauf fehlt.; Geometrie None gegen 0.65» (doppelte Satzzeichen,
  «None» sichtbar) — schon vorher so, getrennt prüfen.
* **Owner:** Abnahmeblatt am iPad (jetzt mit A4/A5); zwei Entscheide aus `auf-156`; ob der
  Abstand zur Vorgabe wird (nach `auf-158`, nur mit Ansage).
* **Mac-CI:** Die Schriften sind hier nicht übersetzt — `main` erst nach grünem Lauf ohne
  Warnung.

## 9 · Nachtrag: ein roter Stand auf `main`, und die Mac-CI

* **Mein Fehler:** Ich habe den Kern-Teil der Runde (`ee39f8a`) allein nach `main`
  geschoben, um die HomeStation früher zu bedienen — geprüft war aber nur der Stand **mit**
  dem iPad-Teil. Ohne ihn zeigte `EINBAU_STAND` C23 auf Dateien, die dort fehlten, und die
  README-Zahl stimmte nicht: `main` war rot. Gemeldet hat es die KosmoPublish-Sitzung (im
  Commit zu `auf-159`), nicht ich. *Einen Teilstand schieben heisst, den Teilstand prüfen —
  nicht den ganzen.* Behoben mit dem Zusammenführen unten.
* **Mac-CI:** Der erste Lauf mit den Schriften brach ab — Xcode legt für ein App-Paket kein
  `Bundle.module` an. Umgestellt auf das Bundle der App (fehlt eine Datei, greift der
  Rückfall auf die Systemschrift), Wächter gegen `Bundle.module`. Zweiter Lauf grün, keine
  Compiler-Warnung (nur ein Hinweis von Apples AppIntents-Werkzeug, nicht aus unserem Code).
* **Daneben auf `main`:** `auf-20260923-159` (local, Rang 3) aus der KosmoPublish-Sitzung —
  beide STOPP-Gründe aus 156 sind dort aufgelöst; die zwei Owner-Entscheide aus §6 sind
  damit **erledigt**, nicht mehr offen.

## 10 · Drei weitere Antworten — und die gute Nachricht vom Abend trägt nicht

**`auf-158` B (trägt der Abstand?) — nein.** 24 Läufe, Quader und Hochbau, Abstand {—, 0,05,
0,15, 0,30}, Startwerte 0–2:

* **Quader:** Bei Startwert 1 und 2 zeichnet das Modell fast nie ein Gebäude an die Stelle
  («nicht messbar», gleich welcher Abstand). Nur Startwert 0 gibt Zahlen; dort ist **0,05**
  am besten (score 0,791 — der **einzige** bestandene Lauf der Reihe), ab 0,30 kippt es (der
  Verlauf innen geht verloren, |ρ| 0,03, obwohl die Silhouette sitzt).
* **Hochbau:** Der Abstand ändert nichts. **Jedes messbare Bild trägt die Tiefe verkehrt**
  (ρ +0,50 … +0,54, score 0) — «liest sich wie ein Blick durch eine Gasse».

**Entschieden:** `ferne_abstand` bleibt ein **Messschalter** und wird nicht Vorgabe. Ein
Gewinn, der an einem Startwert und einem Körper hängt, ist keine Eigenschaft der Kette.

**Korrigierte Fehlannahme (meine):** «Der Abstand hebt die Deckung von 0,35 auf 0,92» habe
ich nach **einem** Lauf als Durchbruch gemeldet (SCHRITTE vom Abend). Die Kreuzprobe bewies
nur, dass der Unterschied im Bild liegt — nicht, dass er trägt. *Ein Lauf ist ein Befund
über diesen Lauf.*

**Neu offen, und wichtiger als der Abstand:** Die Polarität ist womöglich **körperabhängig**.
«nah_dunkel» ist am Quader gemessen (`auf-20260818-13`, `auf-154` B4); am Hochbau zeigt jedes
Bild das Gegenteil. → `auf-160` B.

**`auf-157` (Führung des Edit-Modells) — Vermutung bestätigt.** Der wirksame Regler heisst
`true_cfg_scale` (Vorgabe 4,0); `guidance_scale` ist bei diesen Gewichten wirkungslos
(`guidance_embeds false`). Führung rechnet nur mit `true_cfg_scale > 1` **und** einem
Negativprompt — unser leerer wurde zu `None`: **Jeder Lauf dieses Modells ohne eigenen
Negativprompt lief ganz ohne Führung.** Die Modellkarte schickt ein Leerzeichen. → Runde 12.

**`auf-158` C — blockiert, beim Owner.** Den Download des Union-2.1 (eine Datei, 6,7 GB,
Apache-2.0, 2,9 TB frei) hat die **Berechtigungsprüfung des Werkzeugs an der HomeStation**
abgelehnt. Das kann nur der Owner dort erlauben; wir können es von hier nicht und sollen es
nicht. Das Messskript ist an 1.0 stellengleich zur Kette geprüft — ein Lauf, sobald die
Datei da ist.

**`auf-159` (KosmoPublish-Sitzung, fremde Repos):** Beide Klone nachgezogen. Die
KosmoPrepare-Suite ist **rot** (drei Fehlschläge, einer empfiehlt wieder «rm -rf
libs/pyphen*», zwei erwarten pysolar), und der nächtliche Bildtest pusht auf `main`, ohne
vorher zu holen. **Nicht unser Repo** — dem Owner gemeldet, der Folgeauftrag gehört der
KosmoPublish-Sitzung.

## 11 · Runde 12 — das Bearbeitungsmodell führt, und 158 C ist da

**Gebaut (eine Einheit, Durchsicht «taugt», 12 + 1 Mutationsproben, alle fallen):**
`qwen-image-edit-2511` bekommt seinen wirksamen Regler `true_cfg_scale` 4,0 ausdrücklich und
bei leerem Gegentext das Leerzeichen der Modellkarte — mit Beleg im Register (`auf-157`);
eine Angabe ohne Beleg weist das Register ab. Hinweise und `negativ_wirksam` urteilen über
den Regler, der wirkt. Alle übrigen Modelle: Aufrufargumente bitgleich (Schnappschuss).

**Aus der Durchsicht, selbst nachgezogen:** Der Parametersatz nannte die Bestellung auch an
einer Pipeline, die den Regler verwarf — jetzt `fuehrung_regler_angekommen` (das
Gerechnete, wie `modus_gerechnet`); eine Mutation `>` → `>=` an der Schwelle 1,0 überlebte
— jetzt bewacht; die Attrappe hatte 4,0 als Vorgabe, eine Prüfung auf 4,0 bewies nichts —
jetzt ein Wert, der nicht die Vorgabe ist; ein Satz behauptete zwei Durchgänge auch dort,
wo unbekannt ist, ob geführt wird.

**Entschieden, mit Begründung:** Die Führung geht **auch auf dem Abholer-Weg** an, wo das
Bearbeitungsmodell die Vorgabe jeder KosmoOrbit-Bestellung ohne Modellangabe ist. Der Lauf
ohne Führung war kein Entscheid, sondern ein stiller Fehler (leerer Gegentext → `None`); die
Modellkarte will Führung. Der Preis — grob doppelte Diffusionszeit — ist angesagt (local
A1, cloud Nachtrag 2) und wird gemessen (`auf-160` C). Der Zeitdeckel betrifft nur Blender,
es bricht nichts ab.

**Nicht behoben, angesagt:** Der Zwischenspeicher der Mappe kennt den Codestand nicht — ein
alter Knoten dieses Modells kommt weiter ohne Führung aus dem Speicher (local A4).

**`auf-158` C (vollständig, der Owner hat den Download an der HomeStation erlaubt):**
Union-2.1 lädt, **steuert sichtbar viel besser** (ein sauberes, fotografisches Gebäude genau
auf der Silhouette) — und **unsere QA nennt genau diese Bilder «nicht messbar»**
(geom_iou 0), während die skizzenhaften 1.0-Bilder messbar waren. *Die Prüfung fällt beim
saubersten Bild.* Vermutung (ungemessen): Die Vorgabe-Hintergrundwahl nimmt die nächsten
Punkte, egal wo — bei einem sauberen Foto eine erfundene Bodenebene im leeren Vordergrund
(bekannt seit `auf-20260818-12`). Inpaint mit 2.1 läuft; ausserhalb der Maske nicht
bildpunktgleich (mittlere Abweichung 10 Stufen) — für das Hineinskizzieren wäre das
Original ausserhalb der Maske zurückzukopieren. Nebenbei: `from_single_file` holt eine
Bauplan-Datei aus dem Netz, auch auf dem Weg der Kette.

**Verteilt:** `auf-20260923-160` (local, Rang 1): Ansage R12; B Hochbau-Polarität mit 1.0 und
2.1 samt `rho_maske`; C Führung am Bearbeitungsmodell (Zeit, Bild); D die QA an den vier
2.1-Bildern mit vier Hintergrundstrategien; E Laden ohne Netz. Cloud: Nachtrag 2 zu 155 (F5).

**Offen:** Die QA muss saubere Bilder beurteilen können, bevor 2.1 gegen 1.0 in Zahlen
entschieden wird (nach `auf-160` D, Kern). Inpaint-Zusammensetzen (Kern, später). Laden
ohne Netz (nach `auf-160` E).

## 12 · Rundgang für den Owner

**Auftrag des Owners:** «Schick Bild von der UI und UX der Visbox-Software, führ mich durch die
neue Software auf Desktop und iPad — zeig, was alles schon da ist und wie weit sie ist.»

**Gemacht:** Eine Rundgangsseite (`https://claude.ai/artifact/3H2wu1ofgWcWeLyfKiHrz3`,
privat). **Rechner:** echte Aufnahmen der laufenden Fläche (`oberflaeche/server.py`) mit
einer Probemappe aus dem synthetischen Testbau und den vier echten HomeStation-Bildern aus
`auf-20260909-96` samt ihren Prüfwerten. **iPad:** die Blätter der Entwurfsfläche, mit deren
eigenem Darstellungsskript gerendert — ausdrücklich als **Zeichnungen** gekennzeichnet, nicht
als Aufnahmen. Je Bildschirm einer von drei Zuständen: läuft heute · gebaut, am Gerät
unbestätigt · gezeichnet, nicht gebaut. *Keine zweite Entwurfsfläche:* Die Seite ist ein
Bericht über den Stand, die Blätter bleiben auf der einen Fläche.

**Befunde beim Rundgang:**
* **Zwei Gesichter.** Die iPad-App folgt der neuen Gestaltung, die Rechnerfläche nicht — sie
  ist eine funktionsfähige Werkstattfläche; Blatt 10 («HomeStation am Rechner») ist
  gezeichnet, nicht gebaut.
* **Technische Namen** in den Bedienfeldern des Rechners (`controlnet_staerke` …).
* **iPad-Blätter 1 und 2** (Mappe wählen, Standpunkt setzen) sind nicht gebaut; die App
  arbeitet mit der Mappe, die der Rechner offen hat.
* **Markdown-Sternchen in Meldungen.** Ein Satz aus `projekt._modellstand` stand mit `**`
  roh in der Fläche — behoben, mit Wächter. Dieselben Sternchen stecken in rund 15 weiteren
  Meldungen des Kerns (Torwächter, Maske, Herkunft, Abholer …). Offen: Die Flächen zeigen
  `**…**` als Fettschrift, oder der Kern schreibt ohne — ein Entscheid über die Anzeige,
  darum erst am Blatt, nicht im Vorbeigehen.

