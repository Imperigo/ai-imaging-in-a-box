# auf-vis-20260824-13 · Das Geometrie-Tor besteht — bei der richtigen Rahmung

> **NACHGEMESSEN AM 24.08.2026. Die Antwort steht unten unter „Die Gegenmessung".**
> Die Schwelle ist NICHT arithmetisch unerreichbar. Sie ist bei der Rahmung
> unerreichbar, die `cameras: "auto"` erzeugt.

> **STATUS: OFFEN, wichtig** · vom Home-PC-Worker am 24.08.2026
> Beleg: `auftraege/ergebnisse/auf-20260823-35.json`, fuenf volle Laeufe, 15 Kameras.

## Was gemessen ist

* In **allen fuenf** Laeufen ist der gemeldete Geometrie-Score `null`. Das Tor steht auf
  „nicht bestanden" — als **ungeprueft**, nicht als durchgefallen.
* Nur **3 von 15** Kameras liefern ueberhaupt einen Score (0.1991 … 0.2672). Bei den
  uebrigen ist `n_gemeinsam` **0 oder 1**.
* Die gemessene `geom_iou`-Obergrenze liegt bei **0.0158 … 0.1688**.

## Die Rechnung, die daraus folgt

`geometrie_score = sqrt(|spearman| × geom_iou)`, und `|spearman| ≤ 1`. Damit gilt:

| | |
|---|---|
| bester denkbarer Score bei geom_iou = 0.1688 | **0.4109** |
| `SCHWELLE_GEOMETRIE` | **0.6500** |
| noetige geom_iou fuer 0.65 | **0.4225** — das **2.5-fache** des besten gemessenen Werts |

**Das Tor ist arithmetisch unerreichbar.** Selbst ein perfekter Rangzusammenhang
(`|spearman| = 1`) kaeme nicht ueber 0.41.

## Und jetzt die eigentliche Ursache — nicht die Schwelle

Im selben Ergebnis steht `anteil_maske` = **0.0008 … 0.0782**: Das Bauwerk fuellt
zwischen 0.08 % und 7.8 % des Bildes.

Der erste Verdacht war `WANDABSTAND_M = 10.0` — der Code beschreibt am 18.08. selbst,
dass bei einem **Kleinkoerper** nicht der Bildwinkel den Abstand setzt, sondern die
Untergrenze aus Wandabstand. **Dieser Verdacht traegt hier nicht:** L2 ist ein Demohaus
von 15.36 × 10.36 × 6.0 m, also ein richtiges Gebaeude, fuer das die Rechnung stimmt.

Die Zahlen zeigen etwas anderes, und sie zeigen es **monoton**:

| Lauf | Bodenkoerper | `anteil_maske` | `geom_iou`-Obergrenze |
|---|---|---|---|
| **L2** | **keiner** | — | **0.1688** / 0.1002 / 0.1050 (die hoechste) |
| **G4** | Platte 1.4× Grundflaeche | 0.0782 / 0.0496 / 0.0513 | 0.0879 / 0.0550 / 0.0566 |
| **L1** | Platte 20×20 bei 8×5-Haus (10×) | 0.0215 / 0.0121 / 0.0121 | 0.0322 / 0.0158 / 0.0158 |
| **G5** | ganzes Gelaende | 0.0071 / 0.0040 / **0.0008** | 0.0531 / 0.0233 / 0.0313 |

**Je groesser der Bodenkoerper im Verhaeltnis zum Bauwerk, desto kleiner der
Maskenanteil und desto tiefer die Obergrenze.** Der Lauf ohne jeden Bodenkoerper hat die
mit Abstand hoechste.

Nachgesehen, warum: In `kameras.py` (Z. 1091) entsteht `masse` aus der Huellbox, und die
umfasst die **ganze Szene**. Die Kamera rahmt also Gelaende **plus** Bauwerk und stellt
sich entsprechend weit weg. Die Maske deckt aber nur das Bauwerk — und `maske.py` weiss
sogar, welcher Teil Gelaende ist, denn genau danach trennt `GELAENDE_MUSTER`.

**Die Kamera rahmt die Szene, gemessen wird das Bauwerk.** Das ist der Bruch.

## Was gemessen wird — und von wem

**Nicht die Schwelle senken.** Eine Schwelle, die an einen kaputten Bildausschnitt
angepasst wird, macht das Tor nicht ehrlicher, sondern schweigsam.

Die Frage lautet: **Wie gross werden `geom_iou` und `n_gemeinsam`, wenn die Kamera das
Bauwerk rahmt statt die Szene?**

Diese Messung braucht Renderlaeufe und damit die HomeStation — **sie liegt bei mir, nicht
bei euch.** Ich fahre sie und trage die Zahlen hier nach. Was bei euch liegt, ist die
Entscheidung danach:

* Soll die Kamerarechnung auf einer **Bauwerks-Huellbox** stehen statt auf der
  Szenen-Huellbox? Die Trennung existiert bereits — `GELAENDE_MUSTER` in `maske.py` sagt,
  was Gelaende ist. Heute weiss die Kamera nichts davon.
* Was passiert mit Szenen, in denen die Trennung **nicht** greift? Genau dieser Fall ist
  aus `auf-vis-20260824-12` Punkt 1 bekannt: Ein Objekt namens `Gelaende_Hang` faellt
  durch die Regel. Eine Kamera, die sich darauf verlaesst, erbt diesen Fehler.
* Bleibt der Kontext im Bild? Ein Bauwerk formatfuellend zu rahmen heisst, Gelaende und
  Nachbarschaft aus dem Bild zu draengen — das ist eine **Gestaltungsfrage**, keine
  Messfrage, und sie gehoert vor die Umsetzung.

## Was das mit `WANDABSTAND_M = 10.0` zu tun hat

`kameras.py` rechnet Standpunkte um eine Huellbox herum, und `WANDABSTAND_M = 10.0` ist
die Zahl, die den Abstand setzt. Sie ist auch der Grund, warum eine Innenaufnahme
rechnerisch unmoeglich ist. Ob sie hier ebenfalls die Ursache ist, sagt erst die
Messung — **behauptet wird es hier nicht**, aber sie ist der erste Ort zum Nachsehen.

## NICHT tun

* **Keine Schwelle aendern**, in keine Richtung, bevor die Messung da ist.
* Keine neuen Masse erfinden. Diese Woche sind drei Vorschlaege gefallen
  (`kantenanteil`, Variante F, R2 als Tor) — alle drei an Blender-Renders geeicht, alle
  drei am Produktpfad durchgefallen.
* Nicht mehr als eine Groesse je Lauf aendern.
* Nichts an unserem Code aendern.

## Regel 3

Ergebnis als Datei in diesem Repo, keine absoluten Pfade mit Benutzernamen — pruefbar
mit `grep -c "$(id -un)" <datei>` = 0. Nur Zahlen.

## Fertige Anleitung zum Einfuegen

```
Arbeite auftraege/von-homestation/auf-vis-20260824-13.md ab.
Beleg ist auftraege/ergebnisse/auf-20260823-35.json. Gemessen: geometrie_score ist in
allen fuenf Laeufen null, nur 3 von 15 Kameras liefern ueberhaupt einen, und die
geom_iou-Obergrenze liegt bei 0.0158-0.1688. Weil score = sqrt(|spearman| * geom_iou)
und |spearman| <= 1 gilt, ist der beste denkbare Score 0.41 — die Schwelle 0.65 ist
arithmetisch unerreichbar.
Die Ursache ist aber nicht die Schwelle: anteil_maske liegt bei 0.0008-0.0782, das
Bauwerk fuellt also 0.08 bis 7.8 Prozent des Bildes.
Messt darum, wie geom_iou und n_gemeinsam steigen, wenn das Bauwerk den Ausschnitt
fuellt: eine Szene, eine Ansicht, ein Startwert, mindestens vier Stufen bis
anteil_maske grob 30-60 Prozent. Aendert KEINE Schwelle, bevor diese Zahlen da sind.
Ergebnis als Datei im Repo, Regel 3.
```


---

# Die Gegenmessung — gefahren am 24.08.2026 auf der HomeStation

Beleg: `auftraege/ergebnisse/auf-20260824-36.json`. Ein Quader **15.36 × 10.36 × 6.0 m**
auf einer Platte mit **zehnfacher** Grundflaeche, **ein** Startwert, **eine** Ansicht,
vier Kameraabstaende. Einzige Veraenderliche ist der Standort.

| | auto | s30 | s50 | **s70** |
|---|---:|---:|---:|---:|
| Kameraabstand | 90.55 m | 54.98 m | 35.08 m | **26.56 m** |
| Fuellgrad Bildbreite | 0.175 | 0.300 | 0.499 | **0.699** |
| `anteil_maske` | 0.0193 | 0.0565 | 0.1565 | **0.3051** |
| `geom_iou` | 0.000183 | **0.0** | 0.00144 | **0.9323** |
| `n_gemeinsam` | 14 | 0 | 1 196 | **934 105** |
| `geometrie_score` | — | — | 0.0318 | **0.9599** |
| `gerichtet` | +0.388 | **−0.772** | +0.697 | +0.932 |
| Rauschboden derselben Lage | +0.781 | +0.848 | +0.645 | **+0.376** |
| Tor bestanden | nein | nein | nein | **JA** |

**Die Antwort: ja, aber nicht allmaehlich.** `anteil_maske` steigt streng monoton mit
der Rahmung. `geom_iou` und `n_gemeinsam` bleiben bis 50 % Bildbreite praktisch null und
**springen** dann — Faktor **647** beim `geom_iou`, Faktor **781** bei `n_gemeinsam`.
Bei 70 % Bildbreite entsteht zum ersten Mal auf dieser Naht ein Score, der die Schwelle
nicht nur erreicht, sondern mit **0.9599** deutlich uebertrifft.

**Das ist eine Schwelle, keine Rampe.** Wer den Bildausschnitt „ein bisschen" enger
zieht, gewinnt nichts.

## Drei Dinge, die nicht erwartet waren — und die eure Entscheidung betreffen

**1 · Bei 30 % wird es SCHLECHTER als im Bezugslauf.** `geom_iou` ist dort exakt **0.0**
und `gerichtet` **−0.772**. Das Bild erklaert es: Das Bildmodell macht aus der nahen
Plattenkante eine **Backsteinmauer im Vordergrund** und dreht damit die Tiefenordnung
um. Ein halber Schritt in die richtige Richtung ist hier also ein ganzer in die falsche.

**2 · Der Rauschboden wandert mit** — von **+0.848** bei s30 auf **+0.376** bei s70,
waehrend der Messwert steigt. Bei s30 liegt er damit **ueber** der rho-Schwelle 0.80:
**dort liesse das Tor weisses Rauschen durch.** Das ist derselbe Befund wie in
`BEFUND_2026-08-24_ORTSFELD.md`, hier am Produktpfad sichtbar.

**3 · Es gibt zwei Zahlen namens „Fuellgrad".** Der `blender-report` des Bezugslaufs
meldet `fuellgrad` **0.5493** und `vollstaendig: true` — gemessen an der **Szenen**-
Huellbox. Das Bauwerk fuellt dort tatsaechlich 17.5 % der Bildbreite und 2.4 % der
Bildpunkte. **Die Kameraprüfung hat nichts zu bemaengeln, waehrend die Messung dahinter
gar nicht stattfinden kann.** Ein Waechter, der die falsche Groesse prueft, ist
schlimmer als keiner — er beruhigt.

## Was jetzt bei euch liegt

Die Messung ist gefahren, die Entscheidung nicht. Sie lautet nicht mehr „senken wir die
Schwelle", sondern:

* Soll die Kamerarechnung auf einer **Bauwerks**-Huellbox stehen? `GELAENDE_MUSTER` in
  `maske.py` weiss bereits, was Gelaende ist — die Kamera weiss nichts davon.
* Wenn ja: Welcher Fuellgrad wird bestellt? **0.70 traegt, 0.50 nicht, 0.30 schadet.**
  Zwischen 0.50 und 0.70 ist die Messung grob — dort liegt die Kante.
* Was passiert, wenn die Gelaendetrennung **nicht** greift (siehe `auf-vis-20260824-12`
  Punkt 1)? Eine Kamera, die sich darauf verlaesst, erbt diesen Fehler.
* Und die Gestaltungsfrage, die keine Messung beantwortet: Ein Bauwerk auf 70 % der
  Bildbreite draengt Gelaende und Nachbarschaft aus dem Bild. **Ist das das Bild, das
  ihr wollt?** Der Messwert sagt ja, die Architektur vielleicht nein.

## Was diese Messung NICHT sagt

* **Ausdrueckliche Kameras rendern grundsaetzlich ohne Shift.** Der Runner rechnet den
  Shift nur auf dem Weg `abgeleitet`; auf dem Weg `vorgegeben` bleibt `shift_y`
  unberuehrt (0.0 gegen 0.012885 im Bezugslauf), und der fremde CameraSpec hat kein Feld
  dafuer. Ersatzweise wurde die Achse gekippt (1.25/1.96/2.59°). **Dieser Beitrag ist
  NICHT GEMESSEN** — er koennte einen Teil des Sprungs erklaeren.
* Eine Szene, ein Startwert, eine Ansicht. **Nicht** gemessen ist, ob der Sprung bei
  anderen Baumassen an derselben Stelle liegt.
* Insgesamt 13 Posten unter `grenzen_dieser_messung` in der Ergebnisdatei.
