# auf-vis-20260821-01 · Die Geometrie-QA misst auf dem Produktpfad das Falsche

> **TEILWEISE ERLEDIGT — nachgeprueft am 25.08.2026, Frage fuer Frage.**
>
> **Die Marke «DEMO-KRITISCH» im Kopf gilt nicht mehr.** Sie stand fuer *«ohne
> `gelaende_erwartet` baut die Maske nicht»* — und die Maske baut. Der Riegel ist von zwei
> Seiten gefallen: `3e1f051` gab dem Aufrufer den Schalter, `1e2487b` machte die
> Namensregel wortgrenzenfest. Wer den Kopf heute liest, richtet seine Dringlichkeit
> falsch aus.
>
> * **Frage 1 (woher weiss der Abholer von Gelaende) — GESCHLOSSEN, aber anders als
>   gefragt.** Weder aus `render-scene/v1` noch aus der Materialtabelle abgeleitet:
>   `tools/abholen.py:133` bietet `--kein-gelaende` an, `tools/abholen.py:175` reicht es
>   als `gelaende_erwartet` durch bis `src/aiimaging/abholer.py:1297`. **Der Aufrufer
>   erklaert die Lage, statt dass die Maske raet** — begruendet an neun echten IFC-Dateien
>   (`BEFUND_2026-08-24_IFC-LESER.md`). Dazu faellt der urspruengliche Ausloeser weg:
>   `src/aiimaging/maske.py:226` prueft seit `1e2487b` auf **Wortgrenzen**, `Gelaende_Hang`
>   und `Gelaende_vorn` treffen jetzt.
> * **Frage 2 (taugt «wie_soll» fuer erzeugte Bilder) — OFFEN.**
>   `src/aiimaging/tiefenschaetzer.py:172` `HG_WIE_SOLL` ist unveraendert die Vorgabe. Die
>   einzige gepruefte Alternative, `HG_OHNE_RANDBERUEHRUNG`, ist am 20.08. **vor** diesem
>   Auftrag zurueckgenommen worden, weil sie in jeder Szene mit Boden **null Punkte**
>   waehlt. Es gibt seither keinen neuen Anlauf. Haengt an: niemand, es ist schlicht
>   unbearbeitet.
> * **Frage 3 (was sagt der Vertrag, wenn nicht gemessen werden konnte) — GESCHLOSSEN von
>   Commit `48b72db`.** `src/aiimaging/kosmo_szene.py:753-765` unterscheidet jetzt **drei**
>   Lagen statt der einen falschen Meldung: «NICHT BEURTEILBAR (Rahmung)», «NICHT
>   ZUSTAENDIG» und «NICHT GEMESSEN» — jede mit dem Satz, dass `passed: false` hier nicht
>   durchgefallen heisst.
> * **Frage 4 (Maskenweg in den Vertrag) — OFFEN, und der Engpass hat die Seite
>   gewechselt.** `src/aiimaging/kosmo_szene.py:644-650` schickt unter `qa.geometry`
>   weiterhin nur `geometry_fidelity`, `spearman`, `geom_iou`, `threshold`, `passed`,
>   `method`. `rho_maske` steht innen (`src/aiimaging/abholer.py:1190`) und kommt nicht
>   ueber die Vertragsgrenze. **Bei KosmoOrbit sind die Felder inzwischen da** —
>   `packages/kosmo-contracts/src/render-result.ts:194/200/211` fuehrt `rho_maske`,
>   `kantenanteil` und `paarurteil`, alle nullbar. Gewartet wird also nicht mehr auf sie,
>   sondern auf uns.

> **STATUS: OFFEN — seit dem 21.08. DEMO-KRITISCH.** Der Owner hat Weg A gewaehlt: die
> Geometrie-QA ist in der Demo sichtbar. Damit ist dieser Auftrag der kritische Pfad —
> ohne `gelaende_erwartet` baut die Maske nicht, ohne Maske gibt es keine Zahl, und ohne
> Zahl gibt es kein sichtbares Urteil.
>
> **STATUS: OFFEN** · gestellt vom Home-PC-Worker am 21.08.2026
> Gemessen am echten Auftrag `vis-1787245160-895ba3` aus der Demo-Simulation vom 20.08.

---

## Prompt zum Kopieren

```
Auf dem echten Produktpfad liefert die Geometrie-QA GAR KEINE Zahl — nicht eine
schlechte, sondern gar keine. Gemessen am Auftrag vis-1787245160-895ba3 der
Demo-Simulation: spearman null, geom_iou 0.0, score null, n_gemeinsam 0. Der
Ergebnisvertrag meldet daraufhin "Geometrie-Schwelle NICHT kalibriert (keine
Nullprobe)" — obwohl drei Nullproben gerendert danebenliegen.

DIE URSACHE IST GEMESSEN, nicht vermutet. Die Strategie "wie_soll" behaelt so viele
NAECHSTE Punkte, wie das Soll Geometriepunkte hat. Bei einem Modell OHNE Gelaende
rendert Blender das Bauwerk vor leerem Grund — das Bildmodell erfindet aber einen
Vordergrund (Pflaster), der NAEHER an der Kamera liegt als das Bauwerk. Das ganze
Geometriebudget geht an diesen erfundenen Vordergrund:

    Soll-Silhouette : 171996 Punkte, Schwerpunkt y=498, y-Bereich 350..651
    Ist-Silhouette  : 171997 Punkte, Schwerpunkt y=938, y-Bereich 877..991
    Ueberlappung    : 0 Punkte  (100 % der Ist-Silhouette in der unteren Bildhaelfte)

UND DAS BILD IST IN WAHRHEIT GUT. Derselbe Lauf, ueber die Bauwerksmaske gemessen:
rho = -0.9587 (gerichtet +0.9587) gegen eine Schwelle von 0.80 — weit bestanden. Die
Kette wirft dieses Ergebnis weg, weil der Maskenweg auf dem Produktpfad gar nicht erst
zustande kommt: bauwerksmaske_aus_lauf() scheitert an der Gelaenderegel ("passte auf
keinen einzigen Tabelleneintrag; Namen: ['Beton']") und gibt None zurueck. Mit
gelaende_erwartet=False baut dieselbe Maske sofort, 171996 Punkte.

VIER FRAGEN, in dieser Reihenfolge:

1. Woher soll der Abholer wissen, ob eine Szene Gelaende hat? Heute uebergibt er
   gelaende_erwartet nie, also gilt die Vorgabe True, und jede gelaendelose Szene
   verliert stillschweigend den Maskenweg. Traegt render-scene/v1 die Antwort, oder
   laesst sie sich aus der Materialtabelle des Multipass ableiten (eine Tabelle ohne
   jeden Gelaende-Eintrag ist ein starkes Indiz)? Bitte den Weg begruenden, nicht nur
   waehlen.

2. Ist "wie_soll" fuer erzeugte Bilder ueberhaupt die richtige Strategie? Sie setzt
   voraus, dass die naechsten n Punkte des erzeugten Bildes dieselbe Sache zeigen wie
   die naechsten n des Renders. Bei erfundenem Vordergrund ist das falsch, und der
   Fehler ist nicht graduell, sondern total (Ueberlappung 0).

3. Was soll der Ergebnisvertrag sagen, wenn nicht gemessen werden konnte? Heute sagt er
   "keine Nullprobe" — das ist nachweislich falsch, die Nullproben liegen als
   nullprobe_grau.png, nullprobe_rauschen.png, nullprobe_verlauf.png im Ausgabeordner.
   Vermutlich wurden sie gemessen und lieferten selbst kein Ergebnis; dann muss das
   dastehen und nicht "keine".

4. Soll der Maskenweg in den Vertrag? Er hat als einziger eine Zahl geliefert (+0.9587)
   und steht heute nirgends in render-result/v2.

NICHT TUN: keine neuen Renderlaeufe noetig, alle Zahlen liegen vor. Nichts an der
Betriebsseite (Abholer-Dienst, systemd) aendern — das ist meine Lane, sagt mir was ihr
braucht.
```

---

## Belege

### Woher die Zahlen stammen

Auftrag `vis-1787245160-895ba3`, entstanden in der Demo-Simulation vom 20.08. über die
Oberfläche. Ausgabeordner `out/sSE/`. Reproduktion auf der HomeStation:

```python
soll, B, H = bildlesen.tiefen_aus_report(bericht)          # 1600x992, Silhouette 10.8 %
mb = maske.bauwerksmaske_aus_lauf(f"{J}/material_id.png", bericht)                  # -> None
mb = maske.bauwerksmaske_aus_lauf(f"{J}/material_id.png", bericht,
                                  gelaende_erwartet=False)  # -> 171996 Punkte
u  = tiefenschaetzer.qa_gegen_soll(f"{J}/sSE.png", soll, breite=B, hoehe=H,
                                   schwelle=0.65, maske=mb["maske"])
```

### Das volle Urteil ohne Maske

```
n_soll 171996 · n_ist 171996 · n_gemeinsam 0
geom_iou 0.0 · geom_iou_obergrenze 0.1084 · spearman None · score None
rho_maske None · kante None · paarurteil None
begruendung: "Nicht messbar, damit nicht bestanden — ein Score, den es nicht gibt,
              ist kein bestandener Score."
```

### Das volle Urteil mit Maske

```
rho_maske: rho -0.9587 · gerichtet +0.9587 · n_maske 171996 · anteil_maske 0.1084
kante:     roh 0.000304 · n_innen 1747 · n_aussen 1751 · spanne 10.284
paarurteil: bestanden false · traeger "kante" · Schwellen rho 0.80 / kante 0.05
```

### Zusammenhang mit `auf-20260822-30`

Die Kante liegt hier bei 0.0003 — noch unter dem Bereich 0.004–0.006 der dort gemessenen
Bilder. Der Befund von dort gilt unverändert: **die Kante misst weder Anwesenheit noch
Randschärfe, sondern ob die Mehrheit des Umrisses gezeichnet ist**, und bricht unter dem
Median zusammen statt allmählich zu fallen. Als Ja-Nein-Tor für erzeugte Bilder taugt sie
nicht. Der dort vorgeschlagene Ersatz — *Anteil der Grenze mit Kante*, ohne Schätzer aus
dem Bild gerechnet — wäre hier direkt anwendbar.

### Was ich NICHT geändert habe

Nichts. Der Befund stammt aus einem Messlauf; die Entscheidungen oben liegen in eurer
Lane. Betriebsseitig steht alles bereit: Abholer läuft als Dienst mit 30-Sekunden-Takt,
zehn von zehn MCP-Nähten stehen.
