{
  "schema": "aiimaging.homeworker-auftrag/v1",
  "auftrag_id": "auf-20260909-94",
  "art": "frage",
  "worker": "local",
  "rang": 1,
  "erstellt": "2026-09-09T21:00:00Z",
  "params": {},
  "beschreibung": "NACHTRAG zum selben Tag: der Lizenzpunkt aus diesem Auftrag ist ERLEDIGT — der GPL-Import ist weg. Die zwei Angaben bleiben trotzdem bestellt, aber die Bremse ist aufgehoben.",
  "anweisung": "=== `kosmodraw` IST GEFUNDEN — UND ES IST EIN LIZENZFALL ===\n\nDIES IST DIE ANTWORT AUF EURE MESSUNG\n\nIhr habt am 06.09.2026 in auf-20260901-69 gemessen, dass `resolve_kosmodraw()`\nan eurer Maschine False liefert und die sieben Proben um `build_native_ifc`\ndort genauso ueberspringen wie in der Wolke. Euer Kernbefund war: der Engpass\nist nicht ifcopenshell, sondern das fehlende Paket.\n\nDer Befund stimmte. Der Schluss, den WIR daraus gezogen haben, war falsch.\n\n\nWO ES LIEGT\n\n    Imperigo/KosmoDraw-Privat     privat, ARCHIVIERT seit dem 31.05.2026\n    Ordner `kosmodraw/`           Fassung 0.13.5, 13 Module\n    darin `ifc_export.py`         mit `_setup_file` UND `_add_element`\n\nAuch die `run.py`, nach der gesucht wurde, liegt dort.\n\nGesucht worden war in fuenf Repositories; es liegt im sechsten. Dazu eine\nNamenskollision, die jede Suche in die falsche Richtung schickte:\n`Imperigo/KosmoDraw` ist das Blender-Add-on `kosmo_design` und hat `kosmodraw/`\nnie enthalten — auch keine Loeschung in seiner Geschichte.\n\nLEHRE, die euch so viel angeht wie uns: «liegt in keinem Repository» ist nie\neine Messung, sondern immer nur eine Aussage ueber die Repos, in die man\ngesehen hat.\n\n\nWAS WIR DAMIT ZUM ERSTEN MAL GEMESSEN HABEN\n\n`build_native_ifc` war seit dem 01.09.2026 an keiner Maschine gefahren worden.\nMit gezeigtem Paket und stehender Prozessgrenze:\n\n    Eingabe   3 erfundene Volumen, 2 Hoehenlagen, LV95-Georeferenz\n    Ergebnis  3 IfcSlab, 2 IfcBuildingStorey, 1 IfcMapConversion mit genau den\n              uebergebenen Ost-/Nordwerten, 0 uebersprungen\n    Grenze    weder ifcopenshell noch kosmodraw im Produktprozess geladen\n\n\nUND JETZT DER GRUND, WARUM IHR NICHTS ANSCHLIESSEN SOLLT\n\n`KosmoDraw-Privat` steht unter **GPL-3.0-or-later**. Unser\n`tools/archicad_ifc_impl.py` macht `from kosmodraw import ifc_export` — ein\nImport in einen Prozess, der unser eigenes Programm ist, und ein Import ist ein\nLink. Owner-Mandat Q29 sagt «kein GPL-Link».\n\nDie Prozessgrenze loest das NICHT. Sie ist fuer ifcopenshell gebaut (LGPL,\ndynamische Bindung zulaessig). GPL-Komponenten sind in diesem Kosmos nur als\neigenstaendig aufgerufenes PROGRAMM zugelassen — so wie Blender und\nIfcOpenShell aufgerufen werden. Dass der Prozess jenseits der Grenze laeuft,\naendert daran nichts.\n\nDESHALB AUSDRUECKLICH: setzt `$KOSMOPREPARE_KOSMODRAW` NICHT dauerhaft, und\nbaut den Weg nicht in einen Ablauf ein, solange der Owner nicht entschieden\nhat. Drei Wege stehen zur Wahl (die zwei Funktionen selbst schreiben — sie sind\nduenne Huellen um `ifcopenshell.api`; als eigenstaendiges Programm aufrufen;\noder den Weg stilllegen). Der Entscheid gehoert ihm, nicht uns.\n\n\nWAS WIR VON EUCH BRAUCHEN — zwei Angaben, keine Messung mit Last\n\nBeide betreffen die Maschine, und beide koennen wir von hier aus nicht sehen.\nDie erste ist ein Blick in einen Ordner, die zweite eine Erinnerungsfrage.\n\nWICHTIG zur ersten: falls dort ein `kosmodraw/` liegt, sagt NUR ob es liegt,\nunter welcher Fassung, und ob `ifc_export.py` dabei ist. Schickt KEINEN Code\nund keinen absoluten Pfad zurueck — wir duerfen den Code nicht haben, und ein\nPfad mit Benutzernamen faellt bei uns unter REGEL 3.\n\n\n=== NACHTRAG VOM SELBEN ABEND — DIE BREMSE IST WEG ===\n\nDer Owner hat entschieden («bau nach deinen Empfehlungen»), und es ist gebaut.\nDer Absatz oben, der euch sagt, `$KOSMOPREPARE_KOSMODRAW` NICHT dauerhaft zu\nsetzen, gilt so nicht mehr. Was sich geaendert hat:\n\n1. WO NUR EIN IFC-GERUEST GEBRAUCHT WURDE, ist es jetzt SELBST GESCHRIEBEN\n   (`tools/ifc_geruest_impl.py`, jenseits der Prozessgrenze). Der\n   ArchiCAD-Export braucht KEIN fremdes Paket mehr — gemessen gegen den alten\n   Weg, gleiche Eingabe: 3 IfcSlab, 2 IfcBuildingStorey, 1 IfcMapConversion mit\n   den uebergebenen LV95-Werten. Nichts verloren.\n   FUER EUCH heisst das: SIEBEN Proben, die bei euch am 06.09. uebersprungen\n   haben, laufen jetzt bei euch durch. `./run_tests.sh` sollte entsprechend\n   weniger Spruenge melden.\n\n2. WO KOSMODRAW ALS GANZES GEBRAUCHT WIRD (Operator «KosmoDraw-BIM\n   generieren», `tools/roundtrip_kosmodraw.py`), wird es als PROGRAMM\n   aufgerufen — `python -m kosmodraw`, Uebergabe ueber Dateien, kein Import.\n   Das ist die im Kosmos zugelassene Form fuer GPL, dieselbe wie bei Blender.\n   `$KOSMOPREPARE_KOSMODRAW` DARF ihr also setzen; sie zeigt jetzt auf den\n   Ordner mit `kosmodraw/__main__.py` (nicht mehr auf den mit `ifc_export.py`).\n\n3. EINE FUNKTION WAR ACHT TAGE KAPUTT und ist repariert: `build_labels_ifc`\n   (Raumbeschriftungen ins IFC) scheiterte seit dem 01.09.2026 in JEDEM Lauf\n   mit AttributeError. Falls ihr sie je gefahren habt und es als «geht halt\n   nicht» abgelegt habt — jetzt geht sie.\n\nWAS DAMIT VON DIESEM AUFTRAG BLEIBT: die zwei Angaben unten (V1, V2). Sie\nentscheiden nicht mehr ueber die Lizenz, sondern ueber die HALTBARKEIT: das\nPaket liegt in einem archivierten privaten Repo, und ein Archiv ist keine\nBezugsquelle. Betroffen ist nur noch EIN Weg — faellt das Paket weg, faellt\ndieser Weg, nicht der ArchiCAD-Export.\n\nV3 (Lizenzhinweis einer lokalen Kopie) interessiert weiterhin, aber nur noch\nam Rande — wir importieren sie ohnehin nicht mehr.\n",
  "geometrie": {
    "synthetisch": true,
    "pfad": null,
    "erzeugen_mit": null,
    "hinweis": "Keine Geometrie, kein Render, keine GPU. Ein Blick in einen Ordner und eine Erinnerungsfrage."
  },
  "auflagen": {
    "leistungsgrenze_w": 0,
    "nur_bei_leerlauf": false,
    "leerlauf_schwelle_w": 0,
    "leerlauf_schwelle_mem_gb": 0,
    "hinweise": [
      "Kein Lauf, keine Installation, keine Maschinen-Aenderung.",
      "$KOSMOPREPARE_KOSMODRAW DARF jetzt gesetzt werden (Nachtrag unten) — sie zeigt auf den Ordner mit `kosmodraw/__main__.py`.",
      "Keinen Quelltext zurueckschicken (GPL) und keinen absoluten Pfad (REGEL 3).",
      "Keine Kunden-, Buero- oder Projektnamen in der Rueckgabe.",
      "«Weiss ich nicht» ist eine vollstaendige Antwort und besser als eine geratene."
    ]
  },
  "rueckgabe": [
    "V1 Liegt auf der Maschine ein Ordner `kosmodraw/`? Wenn ja: welche Fassung, und ist `__main__.py` dabei (die Befehlszeile — ohne sie nuetzt uns das Paket nichts, weil wir es nicht mehr importieren)? NUR ja/nein, Fassung, Dateiname.",
    "V2 Ist der ArchiCAD-native-Export in den letzten Monaten ueberhaupt benutzt worden, oder laeuft der ArchiCAD-Weg in der Praxis ueber den Swiss-Tool-Export?",
    "V3 Falls ihr ein lokales `kosmodraw/` findet: traegt es einen Lizenzhinweis, der von GPL-3.0-or-later abweicht? Nur die Zeile.",
    "V4 Nach einem `git pull`: meldet `./run_tests.sh` jetzt weniger Spruenge als am 06.09.? Das waeren die sieben Proben aus Punkt 1 — eine Zahl genuegt.",
    "V5 Eine Zeile, die wir nicht erfragt haben und wissen sollten."
  ]
}
