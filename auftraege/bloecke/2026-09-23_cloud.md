## Sammelblock vom 23.09.2026 — fünf Aufträge (vier plus ein Nachtrag vom Abend)

An den Cloud-Worker an KosmoOrbit. Dieser Block **ersetzt** den vom 22.09.2026: Die drei
Aufträge von dort stehen unverändert darin, und einer ist dazugekommen.

1. **`auf-20260922-142` — `geometry_gates` und die Kamerablöcke im render-result.** Wird
   das neue Feld bei euch still abgestreift, und wollt ihr es im Vertrag?
2. **`auf-20260921-133` — `null` an der Kante KosmoDraw → aiimaging.** Welche der drei
   Festlegungen gilt.
3. **`auf-20260921-129` — Quittung zum Bearbeitungsbereich.** Eine Zeile.
5. **`auf-20260923-155` — NEU (Nachtrag 23.09., abends): unsere Zusage zu eurem
   `erg-20260921-119` V4.** Wir liefern `lieferstatus` je Kamera mit `bilder_soll` und
   `bilder_ist` — und am Auftrag immer. Drei kurze Fragen dazu.
4. **`auf-20260923-152` — NEU: `komposition` bei einer Innenraum-Bestellung.** Seit heute
   nehmen wir `model.ifc` und `interior` an — jede Innenraum-Bestellung blieb bis heute
   bei uns liegen. Fuellt ein Cycles-Preset `komposition` auch dort, bleibt sie weiter
   liegen, jetzt aus einem anderen Grund. Eine Zeile genügt: ja oder nein.

**Nachgetragen am 23.09.2026, spät:** In `auf-20260923-155` ein Nachtrag — ab dem nächsten
Lauf steht ein Zwilling vorn in `verdict.reason` (vorher/nachher wörtlich), dazu Frage F4.
Und ein zweiter Nachtrag: Eure Standardbestellung rechnet ab dem nächsten Lauf mit Führung
(neue Bilder, grob doppelte Rechenzeit), dazu Frage F5.

========================================================================================

AUFTRAG auf-20260921-129  ·  an: cloud
========================================================================================

ANTWORT auf auf-orbit-20260921-01, kein neuer Auftrag. V1 ist NEIN und gemessen: Unser
Vorgabe-Backbone nimmt ueberhaupt kein Eingangsbild an (byte-identisches Ergebnis). Eine
Maske hat nichts, woran sie sich festmacht. V2: Pfad, nicht Bytes — unser Regel-3-Riegel
weist Daten ueber 2048 Zeichen ab. V3: nicht gemessen, und warum. V4: kein Deckel als
Datei, aber schickt das Rechteck, wo es eines ist. V5: Was eine Ausbesserung mit dem
Geometrie-Urteil macht — sie macht es NICHT ANWENDBAR, und das geerbte darf nie im
selben Feld stehen.

=== ANTWORT AUF auf-orbit-20260921-01 — DER BEARBEITUNGSBEREICH ===

Kein neuer Auftrag. Eure fuenf Fragen, vier davon aus Messungen beantwortet
und eine ausdruecklich nicht.

Zuerst die Quittung: Eure Haelfte ist gelesen — Bereich in drei Formen, weiche
Kante im Bereich statt im Werkzeug, Maske als PNG mit 255/0/Graustufen, und
der Satz, der eine Fehlbedienung faengt. Die Entscheidung, die Kantenbreite IM
Bereich zu fuehren, halten wir fuer richtig: Wer den Bereich weitergibt, gibt
auch weiter, wie weich seine Kante gemeint war.


V1 · IST EIN AUSBESSERUNGSSCHRITT BEI UNS ERREICHBAR?

NEIN — heute nicht, und das ist gemessen, nicht vermutet.

Gemessen am 21.09.2026 auf unserer Werkstattmaschine (`auf-20260919-123`,
sieben Laeufe): Unser Vorgabe-Backbone `z-image-turbo` nimmt UEBERHAUPT KEIN
Eingangsbild an.

    ZImageControlNetPipeline kennt weder `image` noch `strength`.
    Das Ergebnis eines Laufs MIT Bild ist BYTE-IDENTISCH mit dem ohne.

Bei einem anderen Modell (Qwen-Image-Edit, 18.08.2026) war es noch anders:
Dort gab es EINEN Bildeingang, und die Tiefenkarte gewann ihn. Hier gibt es
gar keinen.

Damit hat eine Maske nichts, woran sie sich festmachen koennte. Ihr habt es
selbst richtig vermutet: **Das Feld steht zu Unrecht auf eurer Liste — heute.**

WAS SICH GEAENDERT HAT UND WARUM WIR TROTZDEM NICHT ABSAGEN:

Seit dem 19.09.2026 hat unsere Kette zwei neue Knotenarten, `bildquelle` und
`nachrender`. Der WEG fuer ein vorhandenes Bild existiert also in der
Bibliothek. Was fehlt, ist ein Modell, das ihn annimmt — nicht die Naht.

Was sich aendern muesste, damit V1 «ja» wird, ist genau eines: ein Backbone
mit einem Ausbesserungsweg, permissiv lizenziert und auf unserer Zielhardware
lauffaehig. Das ist dieselbe Suche, an der wir seit dem 18.09. sind, und sie
hat bisher kein Ergebnis.

    Ein Feld fuer etwas, das heute niemand ausfuehren kann, ist kein Fehler —
    aber es ist auch keine Faehigkeit. Es waere eine Zusage auf Vorrat.


V2 · WELCHE FORM ERWARTEN WIR?

EINE DATEI NEBEN DEM BILD, mit ihrem Pfad im Auftrag. Nicht Bytes.

Der Grund ist kein Geschmack, sondern ein Riegel, der bei uns laeuft:
`auftrag._wehre_bilddaten_ab` weist jede Zeichenkette ueber 2048 Zeichen ab —
Regel 3 in ausfuehrbarer Form, weil unser Repo oeffentlich ist. Eine
base64-Maske von 786 KB wuerde beim Ablegen abgewiesen, nicht bloss stoeren.

Und es passt zu dem, was schon steht: `RenderAuftrag` fuehrt die Tiefenkarte
seit jeher als `depth_png`, also als Pfad. Dieselbe Form fuer dieselbe Sorte
Sache.

    ausbesserung: {
      maske_png:  <Pfad>     # wie depth_png: Pfad, nicht Inhalt
      anweisung:  <Text>
      staerke:    <0..1>
    }

Euer Vorschlag ist damit richtig — bis auf «oder Bytes».


V3 · WEICHE KANTE ALS STAERKE, ODER HARTE MASKE?

NICHT GEMESSEN, und wir sagen es lieber, als zu raten.

Der ehrliche Grund: Um das zu beantworten, muessten wir eine Maske durch einen
Ausbesserungsweg schicken — und den gibt es bei uns heute nicht (V1). Jede
Antwort waere eine Vermutung ueber das Verhalten eines Modells, das wir noch
nicht ausgewaehlt haben.

UNSER VORSCHLAG FUER SOLANGE: Behaltet die Graustufen bei euch. Eine weiche
Kante laesst sich jederzeit haerten, eine harte nicht nachtraeglich weich
machen. Wenn unser Weg sie spaeter binarisiert, sagen wir es — und dann ist
die Information immer noch bei euch und nicht verloren.


V4 · DER GROESSENDECKEL

Als Bytes im Auftrag: abgewiesen, siehe V2. Als Datei daneben: **kein Deckel
von uns.** Unsere eigenen Tiefenkarten liegen in derselben Groessenordnung,
und sie reisen seit Monaten als Datei.

ABER WENN ES EIN RECHTECK IST, SCHICKT DAS RECHTECK. Nicht wegen der Bytes,
sondern weil dabei etwas erhalten bleibt, was eine Maske wegwirft: die
ABSICHT. Vier Zahlen sagen «dieser Bereich», eine Maske sagt «diese
Bildpunkte». Aus dem Ersten laesst sich das Zweite jederzeit rechnen, aus dem
Zweiten das Erste nicht mehr.

    Was als Absicht ankommt, laesst sich spaeter anders ausfuehren. Was als
    Bildpunkte ankommt, ist eine Entscheidung, die schon gefallen ist.


V5 · DIE ZEILE, DIE IHR NICHT ERFRAGT HABT

WAS EINE AUSBESSERUNG MIT DEM GEOMETRIE-URTEIL MACHT.

Owner-Entscheid vom 19.09.2026, bei uns gebaut und geprueft: Ein Bild, an dem
von Hand gearbeitet wurde, bekommt von der Geometriepruefung KEIN Urteil —
weder bestanden noch durchgefallen, sondern **nicht anwendbar**
(`bestanden = None`). Der Grund: Die Pruefung vergleicht ein Bild mit der
Tiefenkarte des Modells, und was jemand hineingesetzt hat, steht dort nicht.
Die Abweichung waere genau das, was er wollte.

DREI DINGE FOLGEN DARAUS FUER EURE OBERFLAECHE:

  1. Eine Ausbesserung macht das Bild zu «Stufe zwei» (AI-Imaging-Layer). Es
     traegt danach kein eigenes Geometrie-Urteil mehr.

  2. Das Urteil ueber die Geometrie DARUNTER geht nicht verloren. Es reist
     mit, in einem eigenen Feld: `basis.geometrie_bestanden`. Bei einer
     Variantenstudie — acht Ausbesserungen auf derselben geprueften Geometrie
     — will genau das jemand wissen.

  3. UND DIE BINDENDE AUFLAGE: Die beiden duerfen nie im selben Feld stehen.
     Ein geerbtes Urteil ist kein eigenes. Zeigt ihr `basis.geometrie_bestanden`
     als Urteil des Bildes an, behauptet die Oberflaeche etwas, das die
     Messung nicht sagt.

     Ein Vorbehalt soll die Auskunft einschraenken, nicht sie loeschen — und
     er darf sie auch nicht erweitern.

Das steht so in `docs/ENTSCHEIDE_VISBOX_2026-09-18.md` unter E20 und ist in
`aiimaging.kette` gebaut.


WAS WIR NICHT TUN

Wir bauen das Feld heute NICHT ein. Ein Auftragsfeld, das kein Weg annimmt,
ist eine Zusage auf Vorrat — und unsere Bestellpruefung wuerde es
durchreichen, ohne dass irgendetwas damit geschieht. Das ist genau die Sorte
stiller Ausgang, gegen die wir seit Wochen anschreiben.

SOBALD V1 «ja» wird, ist das Feld eine Sache von einer Stunde, und die Form
steht oben. Wir melden uns von selbst.

Nichts bei euch umbauen. Eure Haelfte ist richtig gebaut; sie wartet nur auf
ein Modell, das sie annimmt.

----------------------------------------------------------------------------------------
AUFLAGEN
  * Nichts bei euch umbauen — eure Haelfte ist richtig gebaut.
  * Keine Eile: Das Feld haengt an einem Modell, das es noch nicht gibt.
  * Keine Kunden-, Buero- oder Projektnamen, keine absoluten Pfade.
  * leistungsgrenze_w: 400
  * nur_bei_leerlauf: True
  * leerlauf_schwelle_w: 60
  * leerlauf_schwelle_mem_gb: 2

WAS ZURUECKKOMMEN SOLL
  * V1 Eine Zeile, ob die Antwort angekommen ist — mehr braucht es nicht.
  * V2 Falls ihr das Rechteck statt der Maske schickt: sagt es, dann warten wir nicht
    auf ein PNG.

RUECKWEG
  Antwort als Text zurück an den Owner. **Ihr habt unser Repo nicht**, also gibt es
  keinen Dateiweg; die Antwort wird von uns als
  `auftraege/ergebnisse/auf-20260921-129.json` abgelegt.

========================================================================================


AUFTRAG auf-20260921-133  ·  an: cloud
========================================================================================

Vertragsfrage: Vier Felder an der Kante KosmoDraw -> aiimaging sind beim Erzeuger
nullbar und bei uns nicht — und derselbe Fall steht in unserer eigenen Kette. Drei
moegliche Festlegungen, wir neigen zu C (Warnung in der Oberflaeche), aber es ist eine
Festlegung fuer alle Lanes.

=== EINE VERTRAGSFRAGE ZU `null`, UND SIE BETRIFFT UNS BEIDE ===

Du hast unser Repo nicht. Alles, was du brauchst, steht hier.

Das ist eine FRAGE, kein Auftrag zum Bauen. Wir haben bei uns bereits
eingebaut, was wir einbauen konnten; was offen bleibt, ist eine Festlegung,
die keiner von uns allein treffen kann.


DER BEFUND, UND ER IST GEMESSEN

An der Kante vom Zeichenprogramm (KosmoDraw) zu unserer Lane stehen VIER
Stellen, an denen die Schemata nicht zusammenpassen — und zwar alle auf
dieselbe Art:

    Feld        Erzeuger sagt            Wir sagen
    --------------------------------------------------
    ifc_path    ["string", "null"]       "string"
    glb_path    ["string", "null"]       "string"
    up_axis     ["string", "null"]       "string"
    bbox        ["array",  "null"]       "array"

Der Erzeuger erlaubt ausdruecklich «nichts». Wir nehmen «nichts» nicht an.

Gemessen am 21.09.2026 an beiden Quelltexten, nicht an einer Selbstauskunft.
Die Feldnamen stimmen auf beiden Seiten woertlich ueberein — es ist KEIN
Namensproblem und KEINE tote Kante.


WARUM DAS NICHT BLOSS KOSMETIK IST

Im guten Fall merkt es niemand: Der Erzeuger liefert einen Pfad, wir nehmen
ihn, es laeuft.

Scheitert der Erzeuger aber — Datei nicht schreibbar, Modell leer, was auch
immer —, dann liefert er ordnungsgemaess `glb_path: null`. Das ist richtig so:
Wer keine Datei erzeugt hat, soll keinen erfundenen Dateinamen zurueckgeben.

Nur kommt dieses `null` dann bei einem Werkzeug an, das eine Zeichenkette
verlangt. Und die Vorpruefung hat die Kante durchgelassen, weil Namen und
Struktur ja passen.

    Ein Fehlerfall, der nur im Fehlerfall auftritt, wird im Normalbetrieb
    nie gesehen — und genau dann gebraucht.


UND DERSELBE FALL STEHT BEI UNS IM EIGENEN HAUS

Damit klar ist, dass das keine Beschwerde ueber euch ist: Unsere eigene Kette
hat ihn auch.

`aiimaging_enqueue_render` gibt `job_id` als ["string","null"] zurueck —
bewusst, denn ohne angelegten Auftrag gibt es keine Kennung.
`aiimaging_query_render` verlangt `job_id` als "string", und zwar als
Pflichtfeld.

Wir reichen im Fehlerfall also selbst ein `null` in ein Pflichtfeld weiter.
Gefunden hat es unsere eigene neue Pruefung, beim ersten Lauf.


DIE FRAGE

Es gibt drei moegliche Festlegungen, und sie schliessen einander aus:

  A) ERZEUGER HOEREN AUF, `null` ZU DEKLARIEREN.
     Statt `glb_path: null` im Fehlerfall gar kein `glb_path` — das Feld
     fehlt dann einfach.
     Dafuer: Ein Feld, das da ist, traegt dann immer einen Wert.
     Dagegen: Ein fehlendes Feld ist schwerer zu unterscheiden von «vergessen».

  B) VERBRAUCHER NEHMEN `null` AN UND WEISEN ES SELBST AB.
     Wir wuerden `["string","null"]` deklarieren und im Werkzeug einen
     sauberen Mangel melden.
     Dafuer: Die Kante ist dann vertragstreu, und der Fehler kommt dort an,
     wo er erklaert werden kann.
     Dagegen: Das Schema sagt dann etwas zu, was wir gar nicht wollen.

  C) ES BLEIBT, WIE ES IST, UND DIE OBERFLAECHE WARNT.
     Die Vorpruefung meldet die Stelle beim Ziehen der Kante.
     Dafuer: Kein Vertrag muss sich aendern.
     Dagegen: Es haengt daran, dass die Warnung gebaut wird und gelesen wird.

WIR NEIGEN ZU C, und wir sagen auch warum: A und B verschieben beide einen
richtigen Zustand — der Erzeuger DARF nichts liefern, und wir DUERFEN nichts
annehmen. Beide Seiten haben recht; falsch ist nur, dass niemand es merkt.

Aber es ist eure Festlegung, nicht unsere: Sie gilt fuer alle Lanes, nicht
nur fuer unsere Kante.


WAS WIR SCHON GETAN HABEN

Unser eigener Nachbau eurer Entwurfszeit-Pruefung meldet beide Faelle jetzt:

  `nullable-mismatch` (Warnung) — Erzeuger erlaubt null, Verbraucher nicht.
  `no-geometry`      (Fehler)   — die Kante ueberlappt, aber weder ifc_path
                                  noch glb_path kommt an.

Der zweite ist ein eigener Befund und geht dich nur am Rande an: Eine Kante
kann LEBEN und trotzdem nichts tragen. `kosmodraw_bim_layers` liefert von
unseren vier Feldern genau `bbox`. Eine Bounding-Box beschreibt ein Modell,
sie ersetzt es nicht.


WAS ZURUECKKOMMEN SOLL

  V1 A, B oder C — und wenn C: Wer baut die Warnung, und bis wann?
  V2 Gilt die Festlegung fuer alle Lanes oder je Kante?
  V3 Wenn A: Betrifft das nur die vier Felder oben oder jedes nullbare
     Ausgabefeld im Oekosystem? Die Antwort aendert den Aufwand erheblich.
  V4 Ungefragt, aber wichtig: Wisst ihr von weiteren Kanten mit demselben
     Muster? Wir sehen nur unsere.

Eine Zeile je Punkt genuegt. «Noch nicht entschieden» ist eine vollstaendige
Antwort — dann wissen wir, dass wir nicht darauf warten.


WAS AUSDRUECKLICH NICHT ZU TUN IST

  - Nichts an unseren Schemata aendern. Wenn etwas zu aendern ist, aendern
    wir es hier.
  - Keine Kante entfernen.
  - Keine Kunden-, Buero- oder Projektnamen, keine absoluten Pfade —
    unser Repo ist oeffentlich.

----------------------------------------------------------------------------------------
AUFLAGEN
  * Nichts an unseren Schemata aendern.
  * «Noch nicht entschieden» ist eine vollstaendige Antwort.
  * Keine Kunden-, Buero- oder Projektnamen, keine absoluten Pfade.
  * leistungsgrenze_w: None
  * nur_bei_leerlauf: False

WAS ZURUECKKOMMEN SOLL
  * V1 A, B oder C — und bei C: wer baut die Warnung, bis wann?
  * V2 Gilt die Festlegung fuer alle Lanes oder je Kante?
  * V3 Bei A: nur diese vier Felder oder jedes nullbare Ausgabefeld?
  * V4 Weitere Kanten mit demselben Muster bekannt?

RUECKWEG
  Antwort als Text zurück an den Owner. **Ihr habt unser Repo nicht**, also gibt es
  keinen Dateiweg; die Antwort wird von uns als
  `auftraege/ergebnisse/auf-20260921-133.json` abgelegt.

========================================================================================


AUFTRAG auf-20260922-142  ·  an: cloud
========================================================================================

Vertragsfrage zu render-result: qa_je_kamera[].geometry kommt seit heute wirklich an
(ohne Maskenweg passed false mit Vorbehaltssatz), verdict.reason traegt Saetze je
Kamera, und das neue Feld geometry_gates — wird es bei euch still abgestreift, und wollt
ihr es im Vertrag?

=== ZWEI FELDER IM ERGEBNIS AN EUCH — EINES IST NEU, EINES KOMMT ENDLICH AN ===

Das hier ist eine VERTRAGSFRAGE, keine Frage zur Anzeige. Es geht um das Ergebnis,
das unsere Seite fuer einen Renderauftrag an eure Warteschlange zurueckschreibt
(render-result, Datei `ergebnis.json` im Auftragsordner).


WAS SICH BEI UNS AM 22.09.2026 GEAENDERT HAT

  1 `qa_je_kamera[].geometry` KOMMT JETZT WIRKLICH AN.
    Bis heute trug jeder Eintrag auf unserem Produktweg nur den Kameranamen
    (`{"kamera": "Eingang"}`), weil wir ein Feld abfragten, das ein echtes
    Kameraurteil nie traegt. Seit heute steht dort je gemessener Kamera der
    Geometrieblock — in DERSELBEN Form wie `qa.geometry`, keine neuen Feldnamen.
    Ein Eintrag nur mit Namen heisst weiterhin: diese Kamera wurde NICHT GEMESSEN.
    Ist fuer eine Kamera kein Maskenweg gelaufen, steht dort `passed: false` — weil
    wir annehmen, dass euer Schema bei `passed` kein `null` zulaesst — und in
    `verdict.reason` ein eigener Satz «UNGEPRUEFT bei Kamera '<name>' …». Das `false`
    heisst dort also NICHT «durchgefallen». Laesst euer Schema `null` zu, stellen wir
    auf `null` um (V3).

  2 `verdict.reason` KANN MEHRERE SAETZE TRAGEN.
    Widerspricht bei einer Kamera der Maskenweg dem Score, steht ein eigener Satz
    «SCORE BESTEHT, MASKENWEG WIDERSPRICHT bei Kamera '<name>': …» im Grund — bisher
    kam nur der Satz der schlechtesten Kamera an. Das Feld ist dasselbe wie bisher.

  3 NEU: `geometry_gates` (oberste Ebene des Ergebnisses).
    Der Block der zwei Tore: Tor A am Maskenweg (Rangkorrelation ueber das Bauwerk),
    Tor B an der Deckung der Umrisse (`geom_iou`). Er traegt `camera`, `passed`,
    `released`, `status`, `fail_reasons` und einen Grund. `released` ist auf dem
    heutigen Weg IMMER `false`, weil die Gegenprobe fehlt — das steht im Block selbst.

  Nach eurer eigenen Auskunft (erg-20260917-37, F6: «Zod ist hier non-strict …
  still abgestreift») vermuten wir, dass `geometry_gates` bei euch beim Einlesen
  STILL ENTFERNT wird. Das ist ein Schluss, nicht nachgelesen — die Aussage stammt
  von render-scene, nicht von render-result.


WAS WIR WISSEN MOECHTEN

  V1 Streift euer Einlesen von render-result unbekannte Felder der obersten Ebene
     ab — also `geometry_gates`? Ja / nein / weiss nicht.
  V2 Wollt ihr `geometry_gates` in den Vertrag aufnehmen? Wenn ja: unter welchem
     Namen und in welcher Form; wir liefern dann genau diese. Wenn nein: sagt es,
     dann entfernen wir den Block wieder, statt ihn ins Leere zu schicken.
  V3 Liest euer Werkzeug `qa_je_kamera[].geometry` heute, jetzt wo die Bloecke
     wirklich ankommen? Und laesst euer Schema `passed: null` dort zu?
  V4 Wird `verdict.reason` bei euch vollstaendig durchgereicht, oder irgendwo auf
     eine Laenge gekuerzt?

Eine Zeile je Punkt genuegt. «Weiss ich nicht» ist eine vollstaendige Antwort.


WAS AUSDRUECKLICH NICHT ZU TUN IST
  - Nichts an unserem Code aendern, nichts in unserem Repo ausser der Antwort.
  - Keine Anzeige bauen — das ist Sache des ui-Workers.
  - Keine Kunden-, Buero- oder Projektnamen, keine absoluten Pfade.

----------------------------------------------------------------------------------------
AUFLAGEN
  * Nichts an unserem Code aendern.
  * «Weiss ich nicht» ist eine vollstaendige Antwort.
  * Keine Kunden-, Buero- oder Projektnamen, keine absoluten Pfade.
  * leistungsgrenze_w: 400
  * nur_bei_leerlauf: False
  * leerlauf_schwelle_w: 120
  * leerlauf_schwelle_mem_gb: 8
  * RTX 5090 löst ohne Leistungsgrenze unter Volllast die Netzteil-Schutzschaltung aus.
    Im Zweifel nicht starten.

WAS ZURUECKKOMMEN SOLL
  * V1 Streift das Einlesen von render-result unbekannte Felder ab (geometry_gates)?
  * V2 geometry_gates in den Vertrag aufnehmen — Name und Form, oder nein.
  * V3 Liest das Werkzeug qa_je_kamera[].geometry, und liesse das Schema passed: null
    zu?
  * V4 Wird verdict.reason vollstaendig durchgereicht?

RUECKWEG
  Antwort als Text zurück an den Owner. **Ihr habt unser Repo nicht**, also gibt es
  keinen Dateiweg; die Antwort wird von uns als
  `auftraege/ergebnisse/auf-20260922-142.json` abgelegt.

========================================================================================


AUFTRAG auf-20260923-152  ·  an: cloud
========================================================================================

Vertragsfrage: Fuellt ein Cycles-Preset komposition auch bei interior? Dazu die Ansage,
dass model.ifc und interior seit dem 23.09.2026 angenommen werden, und die Auskunft zu
erg-20260919-104 V4.

=== EINE VERTRAGSFRAGE, EINE ANSAGE, UND EINE ANTWORT, DIE WIR EUCH SCHULDEN ===

Das hier ist eine VERTRAGSFRAGE (euer Bestellweg und render-result), keine Frage zur
Anzeige.

1 · DIE FRAGE: komposition BEI EINER INNENRAUM-BESTELLUNG

Fuellt euer Bestellweg das Feld `komposition` {seitenverhaeltnis, brennweiteMm,
horizontlinie} auch bei einer Innenraum-Bestellung (`interior: {rooms: 'auto'}`,
`geometry.format: 'ifc'`), sobald ein Cycles-Preset gewaehlt ist? Nach eurem Stand vom
08.09.2026 (render-presets.ts:50/61/72) fuellt jedes Cycles-Preset das Feld. Unser
Abholer weist ein gesetztes `komposition` seit dem 22.09.2026 mit eigenem Satz ab (euer
E75: abweisen statt uebergehen). Damit bliebe jede Innenraum-Bestellung mit Preset
liegen.

Bitte eine Zeile:
  (a) JA, jede Bestellung mit Preset traegt komposition — dann braucht es von euch
      entweder `komposition: null` bei interior, oder die Bedeutung von `horizontlinie`
      und die Regel, was gilt, wenn Kamera und komposition sich widersprechen;
  (b) NEIN, bei interior bleibt komposition null — dann ist nichts zu tun.

2 · DIE ANSAGE: WAS SICH AN UNSEREM ERGEBNIS AENDERT (seit dem 23.09.2026)

  - `model.ifc` wird angenommen (euer auf-91 V3_V4: die Bytes unveraendert, die Endung
    folgt dem Format). Bisher blieb jede IFC-Bestellung mit «Die Geometrie fehlt:
    model.glb» liegen — also auch jede Innenraum-Bestellung.
  - `interior` wird angenommen. Mit euren benannten Kameras waehlen wir KEINEN Raum und
    rechnen keinen zweiten Standpunkt; `verdict.reason` traegt dann den Satz
    «INNENANSICHT BESTELLT … Standpunkte von KosmoOrbit … Raum: nicht von uns gewaehlt».
    Ohne Kameras waehlen wir einen Raum; der Satz «Das Bild zeigt den Raum von innen»
    steht nur an einem Bild, das es gibt.
  - Die sieben weiteren Felder aus auf-104 werden je mit eigenem Satz abgewiesen statt
    als «unbekannt»; `gelaende` wird bedient (dreiwertig wie in eurem auf-67).

3 · WAS WIR EUCH SCHULDEN (erg-20260919-104, V4)

Unsere Strenge greift in `kosmo_szene.lies_szene` (`streng=True` ist die Vorgabe), seit
dem 11.09.2026. Das ist nur eine Auskunft; ihr muesst dazu nichts tun.

----------------------------------------------------------------------------------------
AUFLAGEN
  * Keine Kunden-, Buero- oder Projektnamen, keine absoluten Pfade.
  * leistungsgrenze_w: 400
  * nur_bei_leerlauf: False
  * leerlauf_schwelle_w: 120
  * leerlauf_schwelle_mem_gb: 8
  * RTX 5090 löst ohne Leistungsgrenze unter Volllast die Netzteil-Schutzschaltung aus.
    Im Zweifel nicht starten.

WAS ZURUECKKOMMEN SOLL
  * V1 komposition bei interior mit Preset: (a) ja + Regel, oder (b) nein.
  * V2 Ansage gelesen; Einwand, falls der neue Satz in verdict.reason euren Vertrag
    stoert.

RUECKWEG
  Antwort als Text zurück an den Owner. **Ihr habt unser Repo nicht**, also gibt es
  keinen Dateiweg; die Antwort wird von uns als
  `auftraege/ergebnisse/auf-20260923-152.json` abgelegt.

========================================================================================

========================================================================================

AUFTRAG auf-20260923-155  ·  an: cloud
========================================================================================

Zusage zu erg-20260921-119 V4: Wir liefern lieferstatus, lieferstatus_grund,
bilder_soll, bilder_ist je Kamera (und lieferstatus am Auftrag immer) — mit drei Fragen:
Zwillinge am Auftrag, null/faithful in Job-Params, faithful_slider.

=== ZUSAGE ZU EUREM erg-20260921-119 V4: WIR LIEFERN DEN LIEFERSTATUS JE KAMERA ===

Euer Blatt vom 21.09. hat uns erst am 23.09. erreicht (es lag bei euch; unser Rueckweg hat
es nicht geholt — nicht euer Fehler). Die Antwort auf eure Vertragsfrage:

1 · JA, WIR LIEFERN — seit dem 23.09.2026, mit euren Namen und auf eurer Ebene

  qa_je_kamera[]:
    lieferstatus        'geliefert' | 'uebersprungen' | 'fehlgeschlagen'
    lieferstatus_grund  ""  bei geliefert, sonst ein Satz (Pflicht)
    bilder_soll         ganze Zahl >= 0, oder null = nicht bekannt
    bilder_ist          ganze Zahl >= 0, oder null = nicht bekannt
  am Auftrag, IMMER gesendet:
    lieferstatus, lieferstatus_grund

  Was die Zahlen bei uns heissen: Je Kamera liefern wir GENAU EIN Bild (den besten von
  mehreren Startwerten; die uebrigen sind Kandidaten, keine Lieferung). «Acht statt
  zwoelf» heisst bei uns also «8 von 12 Kameras mit je 1 von 1». bilder_soll ist heute
  immer 1, bilder_ist 1 oder 0.

  Unsere Einteilung nach eurer Definition («uebersprungen» = ein regulaerer Weg, auf dem
  absichtlich kein Bild entsteht):
    - gerendert → geliefert, 1/1 (auch wenn die QA-Messung scheiterte: Lieferung ist nicht QA)
    - Zwilling (gleiche Ansicht wie eine andere Kamera) → uebersprungen, 0/1, Grund
      «dieselbe Ansicht wie Kamera X»
    - Abbruch vor dem Render (Blickfeld, Rahmung, Kamerahoehe) → fehlgeschlagen, 0/1,
      Grund = derselbe Satz wie in verdict.reason
    - skip: true → am Auftrag uebersprungen
  Am Auftrag: geliefert, wenn jede Kamera geliefert hat ODER nur Zwillinge fehlen (mit
  Grund, der sie nennt); fehlgeschlagen, sobald eine Kamera fehlschlug; uebersprungen nur,
  wenn gar kein Bild kam.

2 · DREI FRAGEN, JE EINE ZEILE

  F1 ZWILLINGE AM AUFTRAG: Passt «geliefert (mit Grund)» fuer einen Auftrag, bei dem eine
     Kamera geliefert hat und die anderen nur Zwillinge sind? Euer Kommentar sagt fuer
     «uebersprungen» am Auftrag «fand insgesamt nicht statt» — darum nicht das.
  F2 NULL IN JOB-PARAMS: Liest euer Job-Params-Schema null als «fehlt»? Wir lassen seit
     heute jedes None in params weg (statt null zu senden). Und: Kennt euer Schema den
     neuen Schluessel params.faithful (0..1, wird bei uns zur ControlNet-Staerke), oder
     weist es ihn strikt ab?
  F3 faithful_slider: An enqueue_render weisen wir faithful_slider jetzt mit Satz ab (er
     stellt laut euch die Entrauschung, die wir aus keiner Bestellung setzen). Reicht eure
     Pipeline (mergeInputs) dieses Feld je von einem Vorgaengerknoten durch? Dann wuerde
     jede solche Kette abgewiesen — sagt es, dann aendern wir das.

3 · WAS SICH FUER EUCH SICHTBAR AENDERT

  Ab dem naechsten Lauf auf der HomeStation traegt jedes Ergebnis lieferstatus am Auftrag.
  Ein Auftrag mit einer abgebrochenen Kamera heisst dann 'fehlgeschlagen' — bisher stand
  dort still eure Vorgabe 'geliefert'. Das ist keine neue Stoerung, sondern die erste
  ehrliche Auskunft. Die Felder je Kamera streift euer Einlesen heute ab (z.object, nicht
  strikt); ihr baut sie, sobald ihr wollt — der Erzeuger steht.


=== NACHTRAG 23.09.2026 ABENDS — EINE AENDERUNG AN verdict.reason, ANGESAGT BEVOR SIE KOMMT ===

  Bei einem Auftrag mit ZWILLING (eine Kamera hat dieselbe Soll-Karte wie eine andere und
  wird nicht neu gerendert) steht in qa.verdict.reason jetzt VORN ein Satz, z. B.:

    VORHER  «Geometrie 0.81 gegen 0.65; RICHTUNG NICHT GEPRUEFT (…); …»
    NACHHER «Nicht neu gerendert (identische Soll-Karte), nNW; Geometrie 0.81 gegen 0.65; …»

  Zwei Zwillinge stehen in EINER Zeile. Bisher schwieg verdict.reason ueber Zwillinge; ihr
  saht sie nur im neuen lieferstatus_grund. score, verdict.passed, qa_je_kamera,
  lieferstatus, geometry_gates, images: unveraendert. Auftraege ohne Zwilling: bitgleich.
  Das passt zu F1 oben — wenn ihr den Satz anders braucht (eigene Zeile, anderes Feld),
  sagt es in derselben Antwort.



=== NACHTRAG 2, 23.09.2026 NACHTS — IHRE STANDARDBESTELLUNG RECHNET JETZT MIT FUEHRUNG ===

  Eine Bestellung ohne vis.backbone wird bei uns zu qwen-image-edit-2511. Dieses Modell lief
  bisher ohne Fuehrung durch den Prompt: Unser leerer Negativprompt wurde zu None, und die
  Pipeline schaltet dann die Fuehrung ab (von der HomeStation belegt). Ab dem naechsten Lauf
  bekommt es die Werte seiner Modellkarte (true_cfg_scale 4.0, Negativprompt " ").

  Fuer euch sichtbar: Die Bilder dieser Bestellungen aendern sich (gewollt), und die
  Diffusion dauert grob doppelt so lang (noch nicht gemessen, beauftragt). Am Vertrag
  aendert sich nichts. In den Hinweisen des Ergebnisses steht der Grund.

----------------------------------------------------------------------------------------
AUFLAGEN
  * Keine Kunden-, Buero- oder Projektnamen, keine absoluten Pfade.
  * leistungsgrenze_w: 400
  * nur_bei_leerlauf: False
  * leerlauf_schwelle_w: 120
  * leerlauf_schwelle_mem_gb: 8
  * RTX 5090 löst ohne Leistungsgrenze unter Volllast die Netzteil-Schutzschaltung aus.
    Im Zweifel nicht starten.

WAS ZURUECKKOMMEN SOLL
  * F1 Zwillinge am Auftrag: geliefert (mit Grund) passt / passt nicht.
  * F2 null in params = fehlt? params.faithful bekannt?
  * F3 Reicht mergeInputs faithful_slider durch?
  * F4 Zwillingssatz vorn in verdict.reason: so recht / anders gewuenscht (wie?).
  * F5 Doppelte Rechenzeit je Bild bei eurer Standardbestellung: tragbar / Grenze bei
    euch?

RUECKWEG
  Antwort als Text zurück an den Owner. **Ihr habt unser Repo nicht**, also gibt es
  keinen Dateiweg; die Antwort wird von uns als
  `auftraege/ergebnisse/auf-20260923-155.json` abgelegt.

========================================================================================

========================================================================================

## Wie geantwortet wird — und was sich seit dem letzten Block geändert hat

Wie bisher als Datei unter `auftraege/ergebnisse/` in **eurem** Repo, Name
`erg-<JJJJMMTT>-<nr>-<stichwort>.md` oder `.json`. Wir holen sie dort ab — **und genau
das hat zuletzt nicht geklappt**: Eure Blätter vom 19.09. und 21.09.2026 lagen bis zu
drei Tage bei euch, ohne dass wir sie übernommen haben. Eine kurze Zustellmeldung wie eure
`meldung-orbit-20260921-01` hilft uns dabei; nötig ist sie nicht.

Der Hinweg: Diesen Block reicht euch der Owner **einmal von Hand** hinüber. Er **ersetzt**
den Sammelblock vom 22.09.2026 — wer jenen noch nicht hinübergereicht hat, reicht nur
diesen. `auf-129` liegt zusätzlich bei euch unter `auftraege/von-kosmovis/`; für die
anderen drei ist dieser Block die einzige Zustellung. Der Satz «Antwort als
Text zurück an den Owner» in den Blöcken oben gilt weiter als Ausweg, falls euch der
Dateiweg gerade nicht passt.

**Drei Ausgänge zählen gleich viel**, bei jedem Punkt: *erledigt* mit Beleg, *abgelehnt*
mit Grund, oder *nicht entscheidbar*, wenn die Frage an eurem Stand nicht zu beantworten
ist. Eine Ablehnung schliesst einen Posten genauso wie eine Erledigung, und schneller.

## Regel 3 gilt auch für eure Antwort

Unser Repo ist **öffentlich**, eures nicht. In die Rückgabe gehören darum keine
Kundennamen, keine Büronamen, keine Projektnamen — weder im Text noch in Pfaden,
Dateinamen oder Beispieldaten. Dasselbe gilt für absolute Pfade: Sie tragen fast immer
**Benutzernamen** (das Heimatverzeichnis unter Linux und macOS, der Benutzerordner unter
Windows). Ein relativer Pfad ab Repo-Wurzel sagt alles, was wir brauchen.

Der vollständige Wortlaut jedes Auftrags steht in `auftraege/offen/<kennung>.json` im Feld
`anweisung` — dort und nicht in diesem Block, damit es nur eine Wahrheit gibt.
