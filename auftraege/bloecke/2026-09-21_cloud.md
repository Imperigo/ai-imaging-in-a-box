# Block für den Cloud-Worker — Stand 21.09.2026

*Zum Pasten in die Sitzung des Cloud-Workers an KosmoOrbit. Deckt `auf-31`, `auf-37`,
`auf-44`, `auf-49`, `auf-63`, `auf-64`, `auf-67`, `auf-68`, `auf-74` und — neu —
`auf-20260921-83`.*

---

Du bist der Worker für **Vertrag und Warteschlange** an KosmoOrbit. Diese Nachricht kommt
von KosmoVis (dem AI-Imaging-Prototyp). Du brauchst unser Repo nicht — alles Nötige steht
hier.

## Zuerst: neun der zehn Kennungen sind kein Rückstand mehr

Der Block vom 06.09.2026 hat dir neun Aufträge vorgehalten, auf die «nie» geantwortet
worden sei. **Das war falsch gezählt, und wir sagen es, bevor wir etwas Neues verlangen.**

Gemessen am 19.09.2026 mit `python3 tools/rueckstau.py`, das beide Ablagen zusammenzählt
statt nur der eigenen:

| Zahl | Bedeutung |
|---|---|
| 33 | in unserer Ablage offen nach Hausregel |
| **9** | davon **im anderen Repo beantwortet** |
| 24 | wirklich offen — **alle 24 bei `local`, keiner bei dir** |

Die neun sind genau: `auf-31`, `auf-37`, `auf-44`, `auf-49`, `auf-63`, `auf-64`,
`auf-67`, `auf-68`, `auf-74`. Deine Antworten liegen im KosmoOrbit-Repo unter
`auftraege/ergebnisse/`; unsere Zählung liest `auftraege/ergebnisse/<kennung>.json` in
**unserem** Baum und sah sie deshalb nicht. Der Fehler ist unserer.

**Was das für dich heisst: Zu diesen neun ist nichts zu tun.** Sie stehen hier nur, weil
die Auftragsdateien bei uns noch offen liegen, solange der Owner nicht entschieden hat,
wie die Antworten aus deinem Baum in unseren kommen. Das ist Buchhaltung, nicht Arbeit.

## Der eine wirklich neue Auftrag: `auf-20260921-83`

Drei Punkte, alle drei Entscheidungen. **Keine Messung, kein Lauf, keine GPU, kein Code.**

### 1 · Sechs Bestellfelder stehen nicht in deiner Karte

Deine Karte der bekannten Felder (aus deiner Ansage vom 11.09.2026): oben
`schema`/`geometry`/`render`/`style`/`vis`/`cameras`/`out`; `geometry`
`path`/`format`/`up_axis`; `render` `resolution`/`faithful`/`samples`/`sun`; `style`
`prompt`/`mode`/`refs`; `vis` `backbone`/`skip`/`upscale`.

Sechs Felder, die unsere Bestellung senden **kann**, fehlen dort:

| Feld | Bedeutung |
|---|---|
| `komposition` (oben) | Bildaufbau-Vorgabe: Bildausschnitt und Anordnung |
| `render.environment` | Umgebungs- oder HDRI-Angabe für die Beleuchtung |
| `render.himmel` | Himmelsmodell: Stärke, Luft, Staub |
| `render.belichtung` | Belichtung und Farbraum (AgX / Filmic / Standard / Raw) |
| `render.rauschschwelle` | Cycles-Rauschschwelle als Zahl statt als Profil |
| `vis.research_only` | Lizenz-Kennzeichen: Forschungslauf, kein Produkt |

Keines reist unbedingt mit — alle sechs hängen hinter einer Bedingung und erscheinen nur,
wenn die bedienende Person sie gesetzt hat. Eine Standardbestellung trägt keines. Dein
Riegel bricht also nicht den Betrieb, sondern genau die Läufe, in denen sich jemand Mühe
gegeben hat.

**Gebraucht wird: je Feld eine Zeile — aufnehmen oder ablehnen.** Eine Ablehnung ist uns
so viel wert wie eine Aufnahme; dann entfernen wir das Feld bei uns. Was weder
aufgenommen noch abgelehnt ist, bleibt eine Falle.

### 2 · Ein siebtes Feld kommt, und wir sagen es vorher

Der Owner von KosmoOrbit hat am 07.09.2026 entschieden, die Innenraum-Perspektive über
`interior` zu fahren. Im dortigen Vertrag steht es schon:

```
interior: { rooms: 'auto' | [ "Raumname", ... ] }   optional
innenansichten: boolean   (geduldeter Zweitname, bedeutet interior.rooms = 'auto')
```

Beide sind hart an `geometry.format = "ifc"` gebunden. Gesendet wird es heute noch nicht;
sobald der Render-Bestellweg auf IFC umgestellt ist, kommt es — und nach deiner heutigen
Karte würdest du es abweisen.

**Gebraucht wird:** `interior` (und den Zweitnamen) in die Karte aufnehmen, oder sagen,
in welcher Form du die Innenraumbestellung stattdessen haben willst.

### 3 · Deine Ansage vom 11.09.2026 ist in keinem sichtbaren Zweig gebaut

Wir hatten dich um eine Zeile gebeten, wo deine Strenge scharf ist. **Diese Bitte ziehen
wir zurück — wir haben es selbst gemessen**, und das Ergebnis kehrt die Richtung um:

* `lies_szene` in `src/aiimaging/kosmo_szene.py:423` hat die Signatur
  `lies_szene(fremd: dict) -> dict`. Es gibt keinen Parameter `streng`.
* `grep -rn "streng" src/ tools/ tests/` findet keinen Treffer, der diese Prüfung beträfe.
* Über **alle sechs** Zweige, lokal und auf `origin`:
  `git grep -c "streng=False\|streng: bool" <zweig> -- src/aiimaging/kosmo_szene.py` → 0.
* Laufende Gegenprobe statt Textsuche: drei erfundene Felder hinein (oben, unter `render`,
  unter `vis`) — `maengel` kommt **leer** zurück.
* `kosmo_szene.py` wurde zuletzt am 02.09.2026 geändert (Commit `57e519b`); der Kopf des
  Zweigs ist vom 19.09.2026.

Kein Vorwurf: Es kann in einem Zweig liegen, den wir nicht sehen, an einem anderen Eingang
sitzen, oder geplant und nicht gebaut sein. Alle drei sind harmlos. Aber der
Owner-Entscheid E75 («behaltet das Abweisen») ruht auf einem Riegel, den wir nicht finden,
und das gehört richtiggestellt.

**Gebraucht wird:** eine Zeile, welcher der drei Fälle zutrifft. Falls gebaut: Zweig und
Funktionsname.

## Was ausdrücklich nicht verlangt wird

* Kein Code, kein Umbau, kein Lauf.
* Keine Begründung für Verzögerungen — der Hinweg war bis zum 19.09. Handarbeit.
* Keine Nacharbeit an den neun alten Kennungen. Sie sind beantwortet.
* Keine Sammelantwort über die sechs Felder. Sechs Zeilen sind kurz; eine Zeile über alle
  sechs lässt offen, welches gemeint war.

## Wie geantwortet wird

Als Text zurück an den Owner, mit der Kennung im ersten Satz (`auf-20260921-83`). Wir
legen die **Ergebnisdatei** unter `auftraege/ergebnisse/auf-20260921-83.json` an; bei uns
gilt ein Auftrag als offen, solange sie fehlt.

Und eine Bitte: Unser Repo ist **öffentlich**. Keine Pfade mit Benutzernamen, keine
Projekt- oder Kundendaten, keine Bilddaten. Zahlen, Feldnamen, Urteile und Text genügen.
